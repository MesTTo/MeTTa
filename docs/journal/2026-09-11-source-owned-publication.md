# Source-owned publication

Goal: pay source ownership and fixed runnable and typing setup at publication while preserving exact withdrawal, transaction visibility, ordered effects, callbacks and error traces.

Constraint: preserve the parity estimator, upstream commit and every existing allowance. Keep changes to the concurrently owned lifecycle and translator units as explicit integration patches.

## 2026-09-11

Tried: `ai-tmp/publication-d9d1201b/ai-parity-measure.py` calls the parity lane's `prepare_artifacts` and `measure` against `b1d175f13b67baf1090f74f309407b763d421744`. The fixture produces 21 QLF artifacts. Accepted observations are functionremovalspec 12613500 instructions / 11932 inferences and plntest 32701204 / 28408. The pristine cut reports 12613617 / 11932 and 32727710 / 28408 respectively. Casenew and holfunctions have stable inference counts of 4791 and 17360, but their instruction observations were refused by the existing null-resolution check. Raw samples and refused verdicts remain in that directory.

Tried: the same estimator against upstream `ae66fa8e41dcd5539d614706bd4e5cfb34f9608d` accepts functionremovalspec 10540162 / 12790, casenew 4498703 / 7170, holfunctions 17059997 / 22389 and plntest 30021210 / 39839.

Tried: `swipl -q -s ai-tmp/publication-d9d1201b/ai-profile.pl -- <checkout> <program>` retains predicate call counts. Functionremovalspec calls `record_source_assertion/1` 89 times and the source-recompile owner reader 117 times. Plntest calls the recorder 114 times and interprets 455 shipped expected-type patterns in 91 calls to `typing_rule_expected_resolved/3`. Both profiles finish below the sampling period, so they supply call counts, not a time attribution.

Rejected: delaying a whole source's translation until publication, because a runnable changes what the next form compiles against. Revisit only if the language removes source-prefix effects.

Rejected: suppressing ownership rows or tuple retry callbacks. The existing withdrawal tests and the waiver journal's throwing callback control require their behavior. A list accumulator published only at source completion also repeats the August 19 design that added 80410 save-load inferences.

Decided: preserve indexed individual ownership rows and their immediate visibility. Probe hoisting the owner decision into the existing source, owner-pin and recompile scopes; batch publishers can then reuse that decision without changing reference identity or database ordering. Group retirement must still invoke each ordinary erase and callback in its established order, which gives an output-sensitive lower bound of one operation per retained reference.

Decided: use the existing shipped typing-rule term expansion as the basis for compiled expected-type witnesses. Keep user pattern normalization and rule order at query time. The relevant prior art is SWI's `library(record)` declaration expansion and `library(apply_macros)` compilation of fixed control structure at `fc7ef84b949378b729052c3ade79c90ce5416abb`; both are already in the local research corpus.

Open: measure a factored runnable boundary before choosing its final interface. Source observation must retain expression locations through the factored closure. Complete accepted cut controls and verify MORK execution before implementation.

Tried: the ordinary loader, source-observation, reference-loading, metadata and compiled-typing suites pass. The Python MORK suite executes all 28 cases with no skips. The runnable-boundary probe adds eight inferences per form and has no resolved instruction saving: functionremovalspec reads 12449329 / 11972 against its paired probe control 12422175 / 11932, with overlapping instruction ranges. Rejected: shipping this extra dispatch layer as a performance change. Revisit if a different compilation boundary removes a measured cost.

Tried: functionremovalspec's profile reaches specialization invalidation and equation retirement but never `withdraw_source_load/3` or `rollback_source_load/1`. A faster source rollback alone cannot explain a reduction in that program. Retirement work must reach the installed equation's artifacts as well.

Decided: implement the independent installed-witness lever first. Extend the existing declaration expansion with `shipped_typing_rule_expected/2`; the expected-pattern openness decision is fixed at declaration compilation. Preserve the user-tier normalization, rule order, directed matching and final cut in `typing_rule_expected_resolved/3`. An independent copy of the registry interpreter compares constrained and relational family queries, free expected values, shared variables and user rules. Runtime `get_type_rule/2` calls and tuple retries remain required observations.

Open: instruction comparisons are delegated to the integrator's release measurement window on the merged tree. The latest refused attempts retain their raw values and host load, which reached 54 on 32 logical CPUs. Exact inference controls are sufficient to continue implementation; no instruction allowance changes.

Tried: the compiled-pattern implementation passes the eight typing differential tests, four typing-scope tests and structural-alias suite. Its expected patterns still sit in clause bodies, which retains a linear scan of a family's patterns. Decided: emit indexed expected-pattern heads for bound inputs, and retain directed compiled checks for initially free inputs. The latter must test openness after family matching because the family and expected value can share a variable. Alias edit controls use unshipped tags so a shipped metatype cannot mask failed user normalization.

Tried: `sh engine/test.sh suites/typecheck/compiled_typing_rules.plt suites/typecheck/typing_rule_scope.plt suites/typecheck/structural_aliases.plt` passes 8, 4 and 32 tests plus 8 structural-alias subtests after indexed witness publication. `jscpd` scans both changed Prolog files with the Prolog tokenizer and reports zero clones over 685 lines and 5594 tokens. The earlier comma-separated file glob selected no files and supplies no clone evidence.

Tried: the committed typing change measures 11912, 4791, 17240 and 27498 inferences for functionremovalspec, casenew, holfunctions and plntest, respectively. The unchanged estimator accepts casenew at 5670877 instructions and holfunctions at 20246386; it refuses the functionremovalspec and plntest null controls. Host load starts at 13.78 / 16.68 / 24.40. All samples remain in the receipt directory.

Tried: a standalone compiled-writer scope passes nested owners, transaction rollback, a new thread, `wrap_predicate/4` observation and 1000 inference budgets. The scope records its previous first clause before effects, registers cleanup before assertion, and restores that prefix after exit or interruption. SWI's `copy_predicate_clauses/2` copies the already compiled fallback for each new thread or engine; its implementation at `src/pl-proc.c:4081` in `fc7ef84b949378b729052c3ade79c90ce5416abb` avoids decompilation and compilation.

Decided: compile artifact, stored-atom and support-group journal writers in the existing source, owner-pin and recompile scopes. The indexed journal rows and each assertion callback keep their order and immediate visibility. Operational context clauses follow the existing nontransactional import-flight and SWI operator-stack pattern, so rolling back an outer transaction cannot resurrect a scope already unwound. Preserve the original recorder bodies as the unscoped fallback for existing direct context users. Recompilation still overrides only artifact and support ownership; a nested source pin supersedes it unless its marked load matches the nearest recompile context.

Rejected: a cached owner closure fetched and called for every reference. It retains a per-reference lookup and helper dispatch where a compiled journal writer can directly assert the row. The minimum required cost remains one write and callback per owner/reference pair; this change removes repeated owner selection, not those effects.

Tried: making the public recorders thread-local passed nine ownership tests, 60 reader tests, 38 reference-loading tests and six metadata tests, but lost source maps in the source-observation suite. That suite subsequently printed `ERROR: Received fatal signal 11 (segv)`. A paired first-example probe gives the complete source map at the pristine cut and only runnable coverage plus `source-not-observed` in the changed tree. The standalone SWI reproduction records `[local(plain),facade(traced)]`: tracing bypasses a thread-local predicate's wrapper.

Decided: preserve static public recorders and put the compiled writers behind them. The host-workaround ledger and standalone reproduction name `swi-thread-local-wrapper-trace`. The same five suites then pass 9, 60 (+3 sub-tests), 38 (+46), 32 (+5) and 6 (+22) tests, including the entire source-observation suite, with exit 0. The facade costs one call per recorded reference and preserves the existing observation boundary.

Tried: `jscpd --max-lines 100000 --max-size 5mb` scans the two reader implementation files and the publication suite, 3924 lines and 30386 tokens. Its sole clone is the existing nine-line runnable-form body shared by the direct and loader doors. That is the envelope lever's shared boundary. The first scan's default 1000-line ceiling excluded both implementation files, so that partial scan is not evidence for them.

Tried: commit `20d1d9ee713fcb54288ef250a41e205e9192b021` measures 12052 / 5056 / 17773 / 28251 inferences, regressions of 140 / 265 / 533 / 753 against the typing-only state. The estimator accepts instructions 12764425 / 5903619 / 20959826 for the first three and refuses plntest's null. Per-scope clause generation and exact prefix restoration exceed the owner-probe work they remove on these fixtures.

Rejected: allocating executable journal clauses per lexical scope. The measured setup cost invalidates that design for the requested programs. Decided: retain one trailed publication context containing the active load, nearest recompile context and their resolved artifact/stored owners. Existing static journal entry predicates read that installed witness and perform each original assertion directly. This is the same dynamic-extent representation already used by `with_metta_module/2` and the fuel scope, with no executable resource to create or retire. The owner selection is still paid once per scope. Replace the private raw-context test setup with the existing owning-source API; context inspection remains available through the original predicates.

Tried: a non-backtrackable ordinary exit restore leaves an outer publication context behind under a nested inference limit. The sweep reports `source_context(load('$metta_owner_pin'(publication_limited)),none,[publication_limited],publication_limited)==source_context(none,none,[],none)` as false. Using `b_setval/2` for both entry and exit, exactly as `engine/metta/registration.pl:with_metta_module/2` does, passes all 1500 budgets and the reader, reference-loading, observer and metadata suites. The metadata fixture also needed an explicit module qualification for the exported owner scope; its first unqualified call raised `Unknown procedure: plunit_translator_metadata_projection:with_owning_source_load/2`.

Decided: the final context representation needs neither dynamic writer clauses nor their tracing workaround. The exploratory wrapper defect remains documented with its raw reproduction in `ai-tmp/publication-d9d1201b/ai-thread-wrapper-repro.pl`; its temporary ledger entry and tracked gate are removed with that rejected implementation. The publication context uses the existing `swi-cleanup-window` ledger entry.

Tried: a runnable-envelope microprobe compares identical translated goals through the original executor and a statically compiled fixed envelope. Over 1000 repetitions the single-answer arithmetic, mixed Empty, unknown-head, error and let cases have equal inferences; three answers save 6000, and an empty search adds 3000. Answers and variable sharing agree. Unlike the earlier probe this one does not charge a post-translation rewrite; it unpacks the already generated envelope at execution. SWI's runtime compiler at `src/pl-vmi.c:I_CALL1` compiles dynamic control terms on each call, while these fixed controls can live in ordinary reader clauses.

Decided: retain the complete load and recompile stacks in the publication context. `current_owning_source_load/1` skips pins to find an older real load, and constrained context queries can select older rows. A latest-context-only representation would narrow those relations. The journal writers read their cached owners directly; the original context predicates retain stack enumeration. The new real-load test checks a pin above a real source, nested recompile enumeration and a constrained older-owner query.

Tried: the stack-preserving context passes 12 publication tests, 60 reader tests (+3 sub-tests), 38 reference-loading tests (+46), 32 observer tests (+5) and 6 metadata tests (+22). The publication tests include immediate journal callbacks, reentrant and throwing listeners, two transaction rollback shapes, thread/engine isolation and the 1500-budget cleanup sweep.

Tried: commit `f543d2fa391b0bd39f67ad630874f27eea517d9e` measures 11813 / 4753 / 17050 / 27334 inferences. These remove 99 / 38 / 190 / 164 inferences from the typing-only state. The estimator accepts functionremovalspec at 12586540 instructions and casenew at 5519976; holfunctions and plntest retain their null-resolution refusals and recorded loads.

Decided: execute the fixed runnable envelope from static reader clauses, shared across sources. Translation still runs immediately before each form, and `call_goals_in/2` retains the pragma and observation boundary. The executor consumes the translator's existing envelope as data, avoiding an edit to the concurrently owned lowering unit and preserving its printer and naming contract. Its independent oracle executes that original term directly; source observations compare ordered answers, effects, variable identity and complete error-frame rows.

Tried: the ten kernel cases, 32 observer tests (+5), 12 fuel tests and 60 reader tests (+3) pass. Four source differentials initially compare loader banners containing different anonymous-space names, and one also compares PLUnit's host backtrace addresses and stack depths. Decided: compare explicit program output with the loader's existing silent setting, and disable only the optional host backtrace while retaining every MeTTa exception and source-frame row. Those host identities differ between two unchanged executions as well.

Tried: the envelope oracle passes all 15 cases after explicitly loading the optional backtrace flag's library and reading serialized host exceptions back into terms for variant comparison. The final command is `sh engine/test.sh suites/reader/source_runnable_envelope.plt`, exit 0. The reader and test clone scan with `jscpd --formats-exts prolog:pl,plt --format prolog --max-lines 100000 --max-size 5mb` covers 2279 lines and 16746 tokens with no clones. Two earlier scans omitted the extension mapping and selected no files.
