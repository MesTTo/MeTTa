# One door for host listeners
Goal: no registration of a SWI event listener can order an engine mutex before a channel's event-list lock, and the next lock-order cycle of any kind is refused by the gate rather than found as a hang.
Constraint: the engine pays nothing per acquisition in production; no site is exempted by name; a registration is reachable from inside a caller's transaction.

## 2026-09-13: the fourth hang was the same lock
Found: SWI `src/pl-event.c` at `fc7ef84b949378b729052c3ade79c90ce5416abb`: `LOCK_LIST` is a recursive mutex (64-67); `link_event` takes it to register (99-110); `call_event_list` holds it across `PL_call_predicate` for every callback (415-470); `add_event_hook` returns from inside it after a named replacement (145-159, the `swi-named-listener-replacement-lock` entry). So a registration made while holding mutex M orders `M -> event_list(Channel)`, and a callback taking M orders the reverse.
Found: four journal entries are that cycle through four mutexes: 2026-09-06 boot-inference-determinism (a run-time `prolog_listen/3` replacing the handler the collector was inside), 2026-09-07 every-fact-has-a-token (`frame_finished` registered from each worker), 2026-09-08 a-folder-per-concept (the first transactional bound writer blocked on the list lock), and the classes worktree's 2026-09-12 hang (`'$metta_typing_policy'` held by the loader while it registered a `frame_finished` listener, the main thread inside that channel's callback waiting for the policy mutex). Each was repaired at its site; no rule or lane held the class.
Rejected: a door that refuses to register while the calling thread holds any mutex, because plunit holds `plunit` across `run_tests_sync` (`library/ext/plunit/plunit.pl:732`), so every registration a test triggers would be refused. Revisit if the refusal can name only mutexes some callback takes.
Rejected: a static walk from each `with_mutex/2` goal to a registration, because the 2026-09-12 cycle passed through a meta-called goal (`with_typing_policy_stable(spaces:transaction(...))`) that no walk sees. Revisit if `prolog_walk_code/1` follows a goal bound at the call site.
Rejected: a lock-order door around `with_mutex/2` in the engine, because a wrapper adds inferences at each of the 138 acquisition sites and the benches and twins pin inference counts. Revisit if those counts stop being pinned.
Measured: `library(prolog_wrap)` wraps `system:with_mutex/2`, `mutex_lock/1`, `prolog_listen/2,3` and a predicate defined after the wrap; `flag(K, 0, 1)` fails without writing when the flag is 1; inside an engine `thread_self/1` names the owner `mutex_property/2` reports; a compound `flag/3` key collapses to its name and arity; command=`swipl -q -g main -t halt ai-tmp/ai-wrap-and-flag-probe.pl`, `ai-engine-mutex-owner-probe.pl`, `ai-listener-semantics-probe.pl`.
Measured: inside a wrapper body `context_module/1` answers the wrapper's module, a bare predicate channel passed through to the original registers against that module and never fires, and the closure arrives qualified with the caller's context (`m1:cb`); `predicate_indicator` of a frame omits the module for a predicate its context sees unqualified; command=`swipl -q -g main -t halt ai-tmp/ai-ctx-probe.pl`.
Decided: three pieces. `engine/host_listeners.pl` exports `metta_listen/2`, which registers once per process through an atomic `flag/3` claim, qualifies a bare predicate channel with the closure's module, takes no name, offers no removal, and holds no mutex of its own; `tests/prolog/static_checks.pl` refuses any other `prolog_listen/2,3` or `prolog_unlisten/2` under `engine/`, `lib/` and the seats' binding halves; `tests/prolog/lock_order.pl` wraps the mutex and listener primitives in the plunit lane only, records each acquisition order once with the goal it was first taken for, models `event_list(Channel)` as one more mutex held across each callback, and fails the lane on any cycle. Every site converts: the arithmetic-expansion guard, the seam table, the atom-hook watchers, materialization's erase listener (its mutex now guards only the standing engine's creation), the receipt engine's two listeners, the reference watch (one process listener reading each thread's own pending frames, in place of one registration and removal per transaction), and the Python seat's bound watch.
Tried: completion by `thread_wait/2` on the door's row -> `thread_wait/2: No permission to thread wait ... (in transaction)` from `function_free_materialization:overlapping_owned_publications_leave_one_image` and 24 of 25 `references` tests, because materialization and the reference watch register from inside a caller's transaction, where an ordinary assertion is also invisible to other threads and undone by rollback. Decided: the row is `'$notransact'` and a waiter polls it at 0.5 ms; the window is one `prolog_listen/2` call.
Tried: the recorder attributing an edge to the nearest calling frame through `prolog_current_frame/1` and `prolog_frame_attribute/3` -> `sh engine/test.sh` lost 105 of 118 suites to signal 11 at a C-stack depth of 4476 nested `frame_finished` callbacks, because SWI marks every frame those two inspect for `frame_finished`, and the recorder's own callback wrapper then recorded the events its attribution had raised, without end. Rejected: any frame inspection in the instrument. Decided: an edge is attributed to the goal the lock was taken for, which the `with_mutex/2` wrapper already holds, and to the closure for a registration or a callback; the held stack is written before the edges so a callback that fires while they are written sees the lock held. Revisit if SWI offers a frame query that does not arm frame_finished.
Measured: `sh engine/test.sh suites/spaces/receipt_frames.plt` under the corrected recorder reports 16 mutexes and 24 acquisition orders with no cycle.
Tried: the whole lane under the recorder -> 24 distinct cycles, most through SWI's own `'$flag'` mutex. Probed with the held stack printed on every edge from `'$flag'`: the stack on `main` held 56 entries that were never popped, `event_list(...)` and `'$flag'` among them, because the receipts callbacks re-run an interrupted completion from a `thread_signal/2` to self that throws at the next call port, and that port was inside the recorder's cleanup. Decided: every write to the held stack runs under `sig_atomic/1`. Probed again on `trailed_scopes`: no stack deeper than six and no unbalanced pop.
Tried: the closure registered as `lock_order:callback(Channel, Closure)` -> `reference_loading` died at signal 11 in cell 13-4, the crash reporter printing the closure as garbage, which is the `swi-query-frame-discarded-on-engine-destroy` path: the host builds a compound closure on the frame it is delivering for and resolves an atom closure to a procedure first. Decided: a generated stand-in predicate per registration, registered by name; its clause and the tables that map it are `'$notransact'`, because a registration is reached from inside a transaction and the host keeps it whether that transaction commits or not.
Tried: `thread_wait/2` completion and the reference watch registered on the first tracked transaction -> `'$metta_typing_policy' -> event_list(frame_finished)`, `'$metta_specializer' -> ...` and `'$metta_metta_exec' -> ...` registration edges, each first taken for the reference closure: the first tracked transaction runs under whatever mutex its caller holds. Decided: the reference listener is registered from a load directive.
Found: with the recorder correct, the lane reports 25 distinct cycles and every one is real in source: the space-release path takes `'$metta_typing_policy'` under `'$metta_metta_exec'` (`lifecycle.pl:1447` onward) while compilation takes `'$metta_metta_exec'` under the policy (`ensure_metta_exec_module_locked/2`); `metta_ensure_compiled/1` takes `metta_deferred_translation` under both and both under it; the specializer chain `'$metta_specializer' -> metta_deferred_translation -> '$metta_typing_policy' -> '$metta_specializer'` violates the order `specializer.pl` documents; the two receipt channels nest in both orders (`event_list(frame_finished)` <-> `event_list(spaces:metta_receipt_marker/2)`); a transaction frame finishing under the policy mutex delivers `frame_finished`, whose reference refresh takes the policy, the shape that hung the classes branch; and the receipts completion, re-run from a thread signal at the next call port, enters `'$metta_occurrence_receipts'` and `'$metta_metta_exec'` from inside SWI's `flag/3` critical section, so `'$flag'` closes eleven cycles on its own. Command: `env -u DISPLAY sh engine/test.sh`; log `ai-tmp/door/plunit-full3.log`.
Rejected: repairing the twenty-five inversions in this thread, because every one lives in files the classes and libraries branches rewrite (type_rules, lifecycle, receipts, references) and the classes branch already moves reference completion out of the frame callback. Decided: the recorder inventories them (`known_cycle/2`, after `scope_setup_backlog/5`): a new cycle fails the lane, an inventoried cycle a whole run shows nowhere fails it too, so the list only shrinks; the burn-down with the lock hierarchy that resolves it is section I of `ai-code-organisation-and-fixes.md` at the workspace root. Revisit at the merge, where the reference-completion and policy-scope changes land.
Measured: the recorder costs 22 inferences per `with_mutex/2` call against 2 without it (`swipl -q -s lock_order.pl -g` over 1000 calls). Three ratio tests whose missing path takes mutexes their present path does not moved from four or five times to ten: `memo_space_isolation` 73 against 12, `source_observation` 78 against 15, `filereader_import_lifecycle` 81 against 20; the hundredfold searches they exist to catch stay two orders away.
Measured: `GATE_ONLY=1 sh tools/check.sh` on the final tree -> 157 lanes green, 13 red. Against a provisioned control at trunk `c67397ec0` (`ai-tmp/wt-trunk`, `sh tools/worktree.sh`): extcost `add-atom-no-claims-on-the-space` 38,129, benchmarks `alpha-unique` 4,211,487 and `annotated-relation` 1,102,808, and instructions `sort-atom` and `space-name` read the same on both trees, so those reds are stale pins that predate this thread, as do memory-scale-gate, twins, parity-perf, door-order and the c, mork and node benches (2026-09-11-the-end-of-wave-battery). Two are this thread's: engine-bench `boot` 316,926 against the control's 315,381, +1,545 inferences for seven door registrations at boot and the reference callback now running on every marked frame that finishes there; and instructions `py-method-call` 2,450,021,146 against 2,443,703,481, +0.26%, the same callback invoked by the host once per transaction frame, about a thousand instructions each, invisible to the inference pins. Both go when the classes branch lands its transaction-cleanup completion and the reference listener with it; the boot pin is re-measured then with the rest of the owed pass. pytest's one red was the host-service scoreboard, which now records `metta_listen/2` as a door; process-bounds refused the audit's unbounded `swipl`, now under `bounded`.
Open: one SWI listener per channel dispatching to a `seam:frame_finished/1` event seam would replace the three `frame_finished` listeners (receipts, references, the Python bounds) with one host callback per event and clause calls for the rest, which is where the per-transaction instruction cost sits; it touches the seat's generated `provides` half, so it waits for the merge that settles which listeners remain.
Open: an engine that waits for a mutex held by the thread that resumes it deadlocks without a cycle, because the engine borrows that thread; the recorder does not see it, and `engine/materialize.pl` states the rule above `source_owner_erased/1`.
Tried: the plunit lane under the recorder with its held stack a non-backtrackable write inside `setup_call_cleanup/3` -> `spaces_receipt_limits` 130 s of CPU against 1.8 s without the recorder, 96 s of it inside `nb_setval/2` over 1.5 million calls and 30 s in 194,619 collections; a depth probe read the stack at 134, 282, 414, 562 and 694 entries across that suite's five tests, `event_list(frame_finished)` throughout. The receipts tests trip inference limits inside callbacks, and a limit that strikes between the Setup and its cleanup registration is the `swi-cleanup-window` entry: the push happened and no pop was owed. Refuted: the reading above that SWI's `'$flag'` mutex closes cycles through the receipts completion's thread signal; `'$flag'` was a leaked entry, and so were the `event_list(frame_finished)` entries under the eleven cycles that named it.
Decided: the held stack is a trailed write after `metta_with_trailed/3`: `b_setval/2` before the goal, the previous stack written back after it, nothing in a Setup, so a goal the host abandons unwinds its own entry. `spaces_receipt_limits` runs in 4.4 s under the recorder and shows no cycle; the whole lane shows six, all real in source: four are a watched transaction frame finishing under `'$metta_typing_policy'`, `'$metta_specializer'` or `'$metta_metta_exec'` while the reference refresh that `frame_finished` runs needs that mutex (`['$metta_typing_policy',event_list(frame_finished)]`, `['$metta_metta_exec',event_list(frame_finished)]`, and the two three-cycles through the policy), and two put deferred translation on both sides of the policy (`['$metta_typing_policy',metta_deferred_translation]` and `['$metta_specializer',metta_deferred_translation,'$metta_typing_policy']`). The inventory holds those six; the nineteen artefacts are deleted, which the audit demanded by name. The first four go with the classes branch's transaction-cleanup completion.
