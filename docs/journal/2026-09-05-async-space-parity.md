# Async space construction and handwritten mirror parity
Goal: give AsyncMeTTa.space the synchronous construction behavior and detect parameter drift in every async counterpart.
Constraint: retain the generator structure and Space worker target; change MeTTa.space only to pass the private creation site through.

## 2026-09-05
Tried: read MeTTa.space, Space._new_space, AsyncMeTTa.call, Space.metta, and both mirror gates at 2a26737045154820189436f970655932f9f4f089. The async constructor independently implements allocation and provider registration. Its parameter-name drift is space (journal/schema/sync missing) and subscribe (callback missing).
Rejected: retain the async constructor's validation and acquisition implementation, because every later synchronous behavior would need another manual copy. Revisit only if a worker boundary prevents delegation and a regression test demonstrates it.
Decided: capture the creation site before the worker crossing; pass it through MeTTa.space into Space._new_space. Delegate through m.metta.space because the worker's m is a Space, not a MeTTa. Space.metta creates a borrowed context, so its weak-reference append executes but that temporary context does not retain an owner list. AsyncMeTTa continues to own the worker only.
Decided: preserve the requested parity definition, ordered positional parameter names and unordered keyword-only names, across both Space and MeTTa. Keep the existing generated signature/default/annotation/docstring checks. Check ledger replacement shapes too, and remove the separate subscribe callback exception from the runtime presence gate.
Decided: use seven construction test functions, including a provider preservation control. A working provider is expected to pass before and after; making that control fail artificially would not test preservation. Exercise failed acquisition at validation, provider registration, journal construction, and registration after journal acquisition.
Prior art: [SQLAlchemy's proxy generator](https://github.com/sqlalchemy/sqlalchemy/blob/a2ba6e5c719d9e2095a7e06e4a5490861e70839e/tools/generate_proxy_methods.py) copies argument specifications and overloads into marked regions. [Psycopg's converter](https://github.com/psycopg/psycopg/blob/28660001e656c56c1eea98d1e28080c0222a697c/tools/async_to_sync.py) converts the AST and checks regenerated-file differences. These support preserving generation plus checking every manually maintained counterpart. Separate handwritten parity tests in those projects were not verified.

Tried: the seven construction tests on the unchanged implementation -> 9 failed, 1 passed across 10 cases. The provider preservation case passes already. The anonymous equation-home case returned `(async-context-equation)` instead of `41`; the invalid provider case left an extra anonymous name registered. Logs: ai-async-space-before.log and ai-async-drift-before.log in the worktree scratch directory.
Tried: create `child = context.self.metta.space()`, then `context.close()` -> `context.closed=True`, `child.dropped=False`, and the child's name is absent from `MeTTa('&self').self.space_names()`. The engine cascades the space release, while the Python handle remains apparently live. The borrowed context is not identical to the owner, but its self handle is identical.
Decided: pin that observed lifetime behavior and leave it unchanged. Borrowed contexts never read their own `_minted` list because MeTTa.close consults it only under `_owns_self`. The borrowed context owns neither its home nor the returned child handle; the caller holds the child. This is intended borrowed-context ownership, not a pending defect. The test also closes the borrower first and confirms the child remains registered and undropped.

Tried: passing grants directly through delegation -> both a named and an anonymous constructor raised `RuntimeError: no running event loop` when the iterable inspected its caller event loop. Decided: retain the existing caller-side tuple snapshot; this captures input before crossing and duplicates no validation. Both forms have a regression pin.
Tried: the reference generator check -> two stale pages, metta-aio.md and metta-space.md. Regeneration changes only those two approved artifacts. reference.signature reproduces node.args and has no parameter filter; existing private passthroughs use the same underscore-prefixed keyword-only spelling.
Tried: the first direct full suite before worktree setup -> 3 failed, 2965 passed, 87 skipped, 1 xfailed. Failures were stale reference pages, a twin at 3813 inferences against a 3592 pin, and the missing built handle.so fixture. sh tools/worktree.sh linked two MORK artifacts, built the local ignored native artifacts, and warmed the engine. No tracked engine files changed.

Tried: delete journal from the actual handwritten async signature -> the gate failed with `DRIFT space: MeTTa.space`; restore -> 1 passed. Remove subscribe from DIVERGENT -> the gate failed with `DRIFT subscribe: Space.subscribe`; restore -> 1 passed. The committed tests retain both controls.
Tried: AST comparison of MeTTa.space with the added parameter and forwarded keyword removed -> identical to the base method. AsyncMeTTa.space's parameters/defaults/annotations match after normalizing only the inherits operand from AsyncMeTTa to Space. The method shrank from 48 to 33 lines including its signature and docstring.
Tried: the focused async, mirror, and documentation suite -> 135 passed. The required `CHECK_PY=$VENV/bin/python sh extensions/python/test.sh` -> 3005 passed, 52 skipped, 1 xfailed, exit 0. Ruff, generated mirrors, reference freshness, and git diff --check passed.

Tried: explain the four skips above the supplied baseline with `test.sh tests -rs` -> all four Node cross-language cases reported `run npm ci in extensions/node to fetch swipl-wasm`. The worktree setup does not provide Node artifacts. `npm ci` and `npm run build` in extensions/node both exited 0 and changed no tracked Node file.

Tried: the required full command after Node setup -> 3009 passed, 48 skipped, 1 xfailed, 5 warnings in 101.45 seconds, exit 0. This is the supplied baseline plus 16 added passing cases, with identical skip and expected-failure counts. No tracked Node files or third reference page changed.
