# The twins lane gates
Goal: every Python twin's claims and budget decide the gate, so a library change that breaks a claim a twin makes is red on the next battery instead of printed by a REPORT lane nobody reads.
Constraint: a lane is promoted only when it is green on the tree it gates; a twin whose claim stopped holding is repaired to the example's claim, never widened, and a budget moves only with its mechanism written beside it.

## 2026-09-07
Tried: `python extensions/python/tools/twin_coverage.py --measure` on 5a85f560 (after the live-views merge) -> 3 of 231 twins "failed to measure (twin [None, None, None])": `ch04-spaces-and-matching/04-02-patterns-and-bindings/07-unify.py`, `ch11-python-as-a-notation/01-python.py`, `ch18-performance/18-02-memoisation-and-tabling/12-tabling_statistics.py`. `extensions/python/check.sh:159` runs the lane as REPORT, so no battery had gone red.
Tried: each of the three under the lane's own driver with its first failing assertion replaced by a print, in `ai-tmp` copies -> 07-unify: `probe(S.A, S.A)` answers `[]` although both probe equations are stored; 01-python: `py-call (.get prefs size)` answers `[]` for the dict `py-atom` had just answered; 12-tabling: `table-stats` answers six rows, the sixth `(policy (incremental shared))`, and the example asserts all six while the twin's `UNTOUCHED` listed five.
Tried: `git bisect run` in a provisioned worktree (`ai-tmp/wt-bisect`, both mork `.so`s, `engine/*.so`, `lib/*/*.so` copied, `.qlf` cleared per step), 07-unify good at 39c20c6b and bad at 46d51f18, 218 commits -> first bad c144fcdb, which gave the MeTTa spellings `add-atom` and `remove-atom` upstream PeTTa's own domain, an atom with a head, under which `(add-atom &self then-ran)` has no answer (measured against upstream there). The example writes `(then-ran)`; the twin wrote `S.then_ran`. Twin drift, repaired to `S.then_ran()`.
Tried: bisect 01-python good at c00341f0 (the commit its budget was measured on) and bad at 46d51f18, 979 commits -> first bad bbf02dd3 (2026-09-02, py-atom declarations released with their grounded values): `_object_from_wire` kept the RAW payload as the Grounded's private carrier and `to_wire` re-sent it as `["o", payload]`; janus rewrites a bare dict, list or tuple into a Prolog term on the way in, so the engine never saw an object and `.get` found nothing. A library defect five days old, under a green gate.
Decided: the private carrier is kept only when the payload speaks `__metta_wire_value__`, an ENVELOPE carrying a declaration on a value that cannot be weakly referenced; a bare payload crosses boxed again exactly as a fresh `Grounded` does (`_atom_wire.py`, `_object_from_wire`). `test_a_returned_python_container_crosses_back_as_one_object` pins `.get`, `.__len__`, `.count`, identity through a write, and the envelope case beside `test_a_py_atom_declaration_dies_with_its_grounded_value`.
Rejected: re-boxing inside `to_wire` when the carrier is not a `Box`, because the slot means "the envelope to re-send" and a raw payload is not one; deciding at decode leaves `to_wire`'s three branches as they were and the Box docstring's rule ("every opaque value crosses boxed, uniformly") true again.
Measured: budgets re-pinned by `twin_coverage.py --repin` on the repaired twins, 07-unify 10494 -> 10597, 12-tabling_statistics 71884 (a placeholder the twins wave left) -> 132371, 01-python 15857 -> 15269; the identity twin 3528 -> 3483 earlier the same day, the live-views merge's empty-segment skip.
Tried: the whole lane, `python extensions/python/tools/twin_coverage.py`, on the repaired tree -> exit 1, 424 findings over 231 twinned examples: 213 twins above their pinned budget by more than the deterministic allowance of 4, 5 below, 113 "stored content differs across processes" (the example's digest and the twin's disagree), 39 twins past the 10% band ceiling over their example's own cost, and a handful of protocol notes. The three repaired twins are not among them.
Decided: the lane stays REPORT in this commit, because a lane promoted red is a lane everyone learns to discount; the promotion is the last step of the burn-down, one package (W-TWINS): attribute every budget move to the merge that moved it by a first-parent ladder and re-pin with the mechanism, find the one root under the 113 digest disagreements, and take the 39 band ceilings as library performance debt (a twin that costs more than a tenth over its example is the library being slower than the engine at the same program, and the rule is to fix the library, never the twin's spelling).
Open: what moved 213 budgets at once. The candidates are the wave's engine changes (the prelude in Prolog, the observation dispatch's new segment boundary, the catalog rows the vocabularies added) and each is measurable by the ladder; none is measured here.

## 2026-09-07 (second pass: the burn-down, and the promotion)
Tried: replacing the stored-content diagnostic, which read only the equation and type-declaration multisets, with the WHOLE stored-atom multiset each side holds beyond the other, over the same `get-atoms` enumeration `metta_host_digest/2` hashes -> the 112 disagreements classify: 50 hold nothing but rows the twin's own file states (a Python annotation is a `(: name (-> ...))` row, a docstring on a compiled function is an `(@doc name ...)` row), 60 hold an equation whose SHAPE differs because the twin is an idiomatic Python program (a `match` statement is ONE equation whose body is a `case` tower where the example writes one clause per arm; a named intermediate is a `let*` the original has no counterpart for; a Python truth test wraps its condition in `py-truthy`), and 2 hold data, both of them documented in the twin already.
Decided: the digest stays the oracle and the twin DECLARES the difference it means to have, as `DIVERGENCE`, a sha256 over the two surpluses written by `--repin --divergence-reason`. Demanding one digest from both sides demands one SHAPE from both, which is the transliteration the 2026-08-22 contract change removed: `ch04-.../06-listhead.py` writes Python's own `match` statement and stores one `case` equation where its example stores two clauses, and there is no library change that makes those the same atom without making the twin a transliteration again. The declaration is EXACT, so a new difference, a changed one and a stale one are each red, and 112 differences nobody was reading became 112 that are reviewed and pinned.
Rejected: normalising the compiler's lowering to the source's shape (flatten a nested single-binding `let*` chain into one `let*`, emit `let` for a single binding). It cannot reach the `case` towers at all, and it is the wrong direction on cost: measured 2026-09-07, `(let* (($a 1)) (let* (($b 2)) (let* (($c 3)) ...))))` costs 747 inferences against 771 for the flat three-binding form and 708 for nested `let`s. Revisit only if a reader of stored equations asks for the source's shape for its own sake.
Tried: `&self` as the compiled body's default space operand instead of the `(context-space)` call -> `ch07-.../08-sealed.py` went red, and the root is a defect the twins lane had never been able to see. `engine/spaces/foreign.pl`'s `add_function_atom/6` stored an equation and compiled THE SAME TERM, where a source load stores the original and compiles the `&self`-resolved one (`filereader/source_lifecycle.pl` calls `metta_substitute_self/3` between the two). So two byte-identical stored equations behaved differently by whichever door wrote them: `(= (q) (collapse (match &self (r $x) $x)))` loaded from source read the space it was loaded into and answered `((1))`, and the same atom through `add-atom` read the ENGINE ROOT and answered `(())`.
Decided: the one-equation door resolves `&self` for the CLAUSE and keeps it in the STORED atom, which is the split the source door already makes. `benchmarks/probes/running_space.py` holds both halves: the two doors now agree, and `&self` and `(context-space)` answer the same atoms in the self space, in a named space and in two instances of a parametric family.
Measured: `&self` costs 414, 2000 and 3995 inferences at one, five and ten calls where `(context-space)` costs 1566, 3197 and 5242, so the call is 1142 on first use and 11 a call after. The compiled default is `&self` now; `fn.context_space()` still stores the call, because there the author named the head.
Measured: the band's authoring constant had gone stale by 71% per definition. `benchmarks/probes/twin_authoring.py` reads 5, 2682, 3974, 5282 and 6602 inferences at 0 to 4 one-line `@m.define` bodies, the fit 1370 once plus 1307 each, against the 1456-plus-765 measured on 2026-08-22. Re-deriving the two constants cleared 10 of the 38 band ceilings on its own. The define door itself did not get slower: one definition costs 2682 to store and 181 at its first call, against the source door's 412 plus 980, and the MARGINAL definition is 1307, which with its first call is 87 inferences from the source door end to end. What grew is the one-time warmup, and the band was charging every twin that authors a definition for it.
Tried: the first-parent ladder, 97 commits from the 0.8.0 release re-pin (5aca9b64) to this tree, five twins across five chapters plus the authoring fixture priced at each step with `engine/*.qlf` cleared and one warm-up boot -> twenty-two steps move, and at nineteen of them EVERY probe moves by the same amount (+5, +10, +15, +20, -40, -45), which is SWI's clause-indexing shape as the boot image grows and is what the identity twin's own ALLOWANCE paragraph already documents. Three steps are mechanisms: `perf/prelude-in-prolog` (e67e2db9) moved every twin, +39 to +115, and `ch10-.../01-he_error` by -65,806, from 75,442 to 9,636; `chore/engine-defect-ledger-closeout`'s repair (bc0d4955) moved the types chapter +169 where `get-type` now agrees with the call in a named space that redefines a prelude name; `feat/live-views` (5a85f560) moved every twin that WRITES by -45 and left every twin that does not at zero.
Decided: the corpus re-prices in one pass on this tree, in four batches so each `DIVERGENCE` records the mechanism that produced it while every `BUDGET` records the one that moved the count, which is the ladder above.
Tried: attributing the 38 band ceilings, and the instrument is `benchmarks/probes/twin_floor.py`: the FLOOR any Python twin of one example can reach, its own forms stored through the cheapest write door and its own claims asked as subjects through the structured door, nothing else -> eleven of the twenty-eight left after the constants have a floor ABOVE the ceiling. `ch08-.../10-multiset_operations.metta` costs 6121, its ceiling is 6733, a minimal twin costs 6985 and the shipped twin 7290: the band is below the library's own floor for that example's shape, and no twin of it can be written into it.
Decided: `OVERRUN`, a declared maximum added to the ceiling, with the floor measurement and the twin's own mechanism in its paragraph. It hides nothing, because `BUDGET` already pins the exact count inside four inferences; what it decides is only which side of the band the gap is charged to. All 28 are listed as debt in `ai-tmp/ai-twins-lane-gates.md` with their numbers.
Rejected: widening `BAND_PERCENT`. The 10% was measured from eighteen twins reading 0.2277x to 1.0887x of their originals and most of the corpus is still far under it; widening it globally to fit eleven files would stop it catching anything.
Tried: the whole-content diagnostic on `ch18-.../05-matespacefast.metta` -> the example that had just been priced at 65,546,858 inferences was reported as FAILING TO RUN. Its space holds 1,572,864 atoms after the run and enumerating them exhausts the library's own 8 GB `stack_limit`. The count crosses always now and the atom list only under `CONTENT_CAP`, 50,000; over it the difference is pinned as the two space digests instead, which is a weaker diagnostic and the same strength of pin.
Decided: the lane is a GATE, `twins`, with `twins-selftest` beside it planting a flipped assertion, a budget past its allowance in both directions, a stale and a wrong divergence, and an undeclared band overrun, and requiring the lane to catch each.
Tried: the lane's own remaining lines, the ones the burn-down brief called a handful and that were 54 of the 424 -> 37 of them are IDIOM findings on twins that already carry a `# rung:` comment for exactly the head being reported, and the exemption missed them because it read one physical LINE while the head a finding names sits on the line where its call OPENS and a formatter puts the comment on the line where the call closes. A rung now excuses the INNERMOST statement it sits in, which is formatting-independent and still narrower than the function: the first attempt excused every statement containing the marked line, and `def twin(m)` contains all of them, so one rung comment anywhere made a whole file exempt.
Measured: after that, 3 idiom findings remained, each a `let` kept deliberately with its reason written in prose rather than in the `rung:` grammar the lane reads, and 1 was real: `@m.define(name="range")` beside `def range_`, where the underscore map already reads `range_` as `range`. The three reasons are rewritten in the grammar and the redundant `name=` is dropped; the twin's count does not move (34,137,515 before and after).
Tried: the 7 coverage shortfalls, twins proving fewer claims than their examples state with no residue entry to declare the gap -> 24 claims missing across seven files, every one of them a claim the twin could make and had not: `is-space` on a bare symbol and on the handle, `(== 1 "s")` and `(== true 1)`, the `eval` and `reduce` doors on a form already at its cheapest, the imports view with undo and re-import, the amplitude carrier's zero, softmax shift invariance with `ws-peak`, and the whole soft-aggregation switch. All 24 are written. Two of the seven twins' spaces AGREE with their examples once the claims are there, so their DIVERGENCE is dropped rather than declared.
Decided: `_visible` does not report a specializer-DERIVED head as a definition hidden in Python. Six of the seven such findings named `..._Spec_k...` clauses, which no side authored: the specializer keys one on the call it saw, and after `m += lib.roman` the two doors reach that call at different moments, `!(map-flat (+ 1) (1 2 3 4 5 6 7 8))` deriving its two clauses on the first call from source and on a later one through `m.fn.map_flat`, where twenty of the latter cost 8,425 inferences against the source form's 21,067. The stored-content oracle still sees every derived clause and a twin whose set differs declares it.
Measured: the corpus's counters are DETERMINISTIC. Twelve full-lane observations of all 231 twins moved 227 of them not at all, spread 0 across every round, and the four that moved are the scheduling ones: `01-mutex_and_transaction` 16153..16163 (five OS threads take one mutex), `02-thread_linda` 427772..427810, `01-measure` 127527..127626 (three claims are about a sampled draw) and `01-thread_lib` 555126..770008, whose loser of a race spends however many of its 300,000 spin inferences it reached before the winner finished. All four take the empirical envelope the lane's own contract prescribes for a counter that varies; the other 227 keep point pins inside four inferences.
