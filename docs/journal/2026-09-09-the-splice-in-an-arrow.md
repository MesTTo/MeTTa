# The splice in an arrow
Goal: one run spelling in patterns and arrows, positional type refusals, and arbitrary-arity held keyword pairs.
Constraint: upstream PeTTa governs fixed arrows; their arity refusal and presentation remain unchanged.

## 2026-09-09

Purpose: apply one element contract to a sequence of any length while retaining each element's position.
Existing mechanism: `present_type_chain/3` already generates the finite arrow for the arriving arity. Runtime refusal and builtin data masks still read the written parameter list.

Prior art considered: regular-expression repetition separates a repeated element from its cardinality; XML content models check each child; compiler parameter packs expand at instantiation; database row validation preserves column positions; symbolic sequence patterns bind zero or more arguments. The common transfer is to generate one positional view and let existing consumers check it.

Read: LeaTTaRevised `docs/journal/2026-09-05-segments-derived.md`, `ai-notes/ai-derivation-verdict-segments-2026-09-05.md`, `corpus/correct_semantics.metta` F and I15, `corpus/variadic_arrow_signature.metta`, `corpus/variadic_arrow_faces.metta`, and `stdlib.metta:110-135`. These supply the derivation and acceptance cells, not authority over fixed-arrow behavior. The type-layer family has one member for an arriving arity, so no cut ordering changes here.

Read: [Wolfram BlankNullSequence](https://reference.wolfram.com/language/ref/BlankNullSequence.html) and [Mathics BlankNullSequence.match](https://github.com/Mathics3/mathics-core/blob/40b9d54d2394c71ea9288a070c12dc510177b5be/mathics/builtin/patterns/basic.py). Both distinguish zero-or-more cardinality from the per-element restriction. Existing local expansion already implements that separation; no dependency is added.

Tried: `sh run.sh ../libaudit/rest_refusal2.metta` at `c0ad1038f36227f05029c6607d7066881b8b4b99`, exit 0. The run resolved that main-checkout probe to its absolute path. Wrong rest element answers `()`, empty sum `(0)`, three-element sum `(6)`, fixed wrong element `((Error (fixed2 1 vflag) (BadArgType 2 Number Bool)))`.
Found: `metta_operation_parameters/6` compares the unexpanded parameter count to the argument count after translation has expanded it. The refusal therefore has no parameter list. `data_head_masks/3` and `builtin_argument_mask/4` likewise assume a fixed list. Zero-argument observers and the prelude arrow constructor also retain the old spelling.

Decided before implementation:

- Keep `present_type_chain/3` as the arity-indexed family generator, with final `(:seg T)` replacing the retired marker. Fixed-arrow expansion retains its existing path. Expansion is linear in the produced parameter count, the output-size lower bound.
- Reject retired, malformed and non-final arrow splices at declaration boundaries, with the offending type and a remedy. Validate nested arrow types as well. Raw storage and `get-type` retain the written declaration.
- Feed runtime and shallow argument diagnostics the presented chain. Project the written chain at the same arity when reporting aliases, preserving element positions without inventing TypeExpansion for ordinary repetition.
- Preserve splice structure in mask-only projections and derive each arriving mask through the same presentation. Fixed data-mask lookup keeps its existing branch.
- Collapse only `Kwargs`, six rows to one. `nop` and the prelude `->` already mean any arity and are respelled. `new-space` has creation overloads at 0/1/2 inputs; `py-atom` has resolution/metatype overloads at 1/2; `map-atom`, `filter-atom`, `foldl-atom` have closure/binder overloads; `add-translator-rule!` has optional metadata. `take-atom`, `peek-atom`, `await-atom`, `space_take`, `space_await`, `space_take_where`, `space_await_where` have optional timeout overloads. `get-doc` has ambient/explicit-space overloads. Repeated `once`, `and-then`, `or-else`, `get-type` rows occur in distinct library/spec surfaces and retain their fixed contract. None denotes arbitrary repetitions.
- Port both cells into chapter 9 with native `test`, the engine's Bool-valued effects, and a caught fixed-overapplication control. Python twins build declaration/equation atoms through existing factories because compiled Python variadic parameters remain a separate surface.
- Add regression coverage for positions, empty runs, held elements, mixed prefixes, runtime heads, aliases, shared variables, malformed declarations and keyword pairs beyond six. Record measured costs and regenerate owned surfaces.

Rejected: renaming only `rest_parameter/2`, because it leaves the silent refusal and keyword masking defects. Rejected: enumerating more keyword arities, because no finite ceiling expresses the contract. Rejected: a new matching engine or cache, because this package already has the required finite expansion and compilation reuse.

Necessary integration outside the brief's initial file list: `engine/metta/terms.pl` owns refusal parameter reads; `engine/translator/lowering.pl` owns builtin/data masks; `engine/metta/{types,runtime,prelude,type_aliases}.pl` own observer spelling and type syntax; declaration admission lives in `engine/filereader.pl` and `engine/spaces/lifecycle.pl`. Existing tests and phrasebook generators naming the retired spelling must migrate with them.

Open at design freeze: implementation, final measurements and verification receipts below.

Admission detail settled before code: the ordinary source filter and alias source-normalization walk validate before effects; individual declarations and batch preflight validate before storage. Alias-installed write observers retain the same validation. The validator owns no state and adds no check to fixed-arrow presentation.

### Implementation and focused evidence

Implemented the planned admission, parameter and mask readers. Existing alias syntax checks retain their earlier position: `acyclic_term/1` must refuse before a recursive splice walk. `sh engine/test.sh suites/typecheck/variadic_arrows.plt`: exit 0, 11 tests plus 9 subtests. The same suite checks shared variables at arities 0 through 12, refusal positions 1 through 8, dynamic heads, aliases, malformed declarations, zero-run observers and held Kwargs at 0/1/7/12 pairs. `sh extensions/python/test.sh tests/ch09_types/test_variadic_arrows.py`: exit 0, 5 tests including 80 generated runs, plus source/file/batch refusal with no partial storage.

The source cells group each arity or position family in one tuple assertion. Their Python twins parameterize those same families rather than enumerate repeated assertions. `python extensions/python/tools/twin_coverage.py examples/ch09-types/22-variadic_arrow_signature.metta examples/ch09-types/23-variadic_arrow_faces.metta`: exit 0, 4/4 and 9/9 claims, identical stored content. Signature costs 10,632 MeTTa / 10,651 Python inferences; faces cost 24,138 / 26,266, including its Python-authored fixed definition. The original individually asserted cells cost 11,064 and 24,849; grouping removes assertion-wrapper work while retaining every operand and expected answer.

Tried: writing held expected expressions directly caused `(2 4)` to be compared to `((+ 1 1) (+ 2 2))`; expected operands evaluate too, so the cells now use `noeval`. A caught Prolog exception prints like a MeTTa expression but is a different representation; the cell compares `repr`, and plunit separately asserts the exact compound exception. The first Python import used nonexistent `E`; the public constructor is `Expression`.

`python tests/checks/check_cumulative_syntax.py`: exit 0, 320 programs, 277 constructs, permanent negative control still caught, no new introduction row. `example_parity.corpus()` measures 315 runnable programs; `check_llms_names.source_counts()` measures 320 total non-fixture programs and five skips. Documentation counts use those readings. `phrasebook.py --markdown`: exit 0, no findings; `fngen.py --write` regenerates the reflected signatures. `jscpd --reporters ai --noTips --min-lines 8` over the changed parameter/mask readers and new Python files: exit 0, no clones.

### Presentation cost controls

The cut control is a detached checkout of `c0ad1038f36227f05029c6607d7066881b8b4b99`, with matching native artifacts. Before and after use `statistics(inferences, ...)` around 100 calls to `once(translator:present_type_chain(Arrow, Arity, _))`, after one warm call. Fixed arrows contain Arity Number parameters; splice arrows contain one final run of Number. The wrapper contributes two inferences to each total.

| arity | fixed, before = after | splice, before = after |
|---:|---:|---:|
| 0 | 2,002 | 1,702 |
| 1 | 2,302 | 2,002 |
| 2 | 2,502 | 2,302 |
| 3 | 2,702 | 2,602 |
| 16 | 5,302 | 6,502 |
| 128 | 27,702 | 40,102 |
| 1024 | 206,902 | 308,902 |

The same process loads the cells' numeric fold and held-run definitions, using `%Rest%` at the cut and `:seg` afterwards. One warm evaluation precedes 100 `findall` evaluations in the declaration's module. Numeric arities 0/1/2/3 cost 58,202/12,802/68,002/72,602 before and 62,602/12,802/72,402/77,002 after. The wrong second element changes from no answers at 43,602 to the required BadArgType at 104,202. Held arities 0/1/2/3 remain 14,602/12,102/25,502/30,702. The first probe used `sum`, which collided with an available Prolog arity and raised `Type error: list expected, found 1`; a distinct probe head removes that instrumentation collision on both arms.

`swipl -q -g 'metta_bench:bench_run(translate)' -t halt engine/bench.pl` measures 313,726 at the cut and 317,610 after. Positive controls restore the old readers in a fresh process after boot: restoring diagnostic readers removes 3,768; restoring mask projection removes 116; together these explain all 3,884. Restoring only data/builtin mask lookup or declaration validation removes zero. The unchanged fixed presenter is therefore distinguished from additional compilation checks. The previous pin already fails at the cut: translate 313,726 versus 313,715; evaluate 559,106 versus 559,101, both with allowance four.

Rejected: a separate cached or indexed variadic diagnostic path to avoid these checks, because it adds another declaration-derived state lifetime and invalidation contract. The shared presenter fixes all arriving arities and preserves the existing type-policy relation. Its linear allocation meets the output-size lower bound. Counter movements are attributed and pinned; empirical envelopes remain the integrator's observations.

### Integration corrections

The first full engine run failed only the module-interface check: callers reached unexported `translator:rest_parameter/2` and `translator:validate_type_splices/1`. Exported both from `engine/translator.pl`; the focused layering suite then passed all seven tests. The pristine cut's full engine suite passed. The full shell corpus passed all 315 runnable examples.

A fixed builtin mask must retain exact-count selection independently of user `arrow-arity` rules. Universally calling `present_type_chain/3` changed that policy: a two-argument `new-space` mask disappeared after a user refusal of arity 2. The existing builtin mask index now stores a fixed list or `variadic(Types)`, classified from the final formal while building the index; only the latter expands at a call. No additional index or invalidation lifetime is introduced. The regression passes with this change and fails when the initial mask reader is loaded as a process-local negative control. The expanded focused suite passes 12 tests plus nine subtests.

The first policy test fixtures were unsuitable: `get-doc` has no builtin mask entry, and an empty named space has no local DontEvalType marker for `map-atom`'s Expression parameters. The final fixture uses `new-space`'s literal Atom parameter, whose masking rule is independent of those declarations.

An evaluation positive control restores only the old diagnostic readers and changes the benchmark from 559,079 back to the cut's 559,106. Presentation now rejects non-fitting arities before the old origin walk, saving 27 inferences in this workload. Restoring mask projection or admission changes none of that row. The numeric fold and held-run measurements above separate this from the additional checks in translation.

### Inherited native arity collision

The second focused Python run, seed 4264610935, reduced the numeric property to `[0, 0]`: `sum` called CLP(FD)'s imported `sum/3` and raised `Type error: list expected, found 0`. The earlier description of this as an instrumentation collision was insufficient: the same name is valid in a program. Source registration records visible native arities, and both compiled and runtime dispatch choose such an arity before the retained segment equation.

Decided before editing: correct `resolve_dispatch/4`, the shared target-selection boundary. A native goal inherited from outside the retained equation's owner cannot implement an arriving segment arity; route that call through the existing segment dispatcher. A local predicate still supplies ordinary and segment clauses together. A host registration without retained equations and a seam-selected goal keep their existing paths. This uses existing metadata and adds no derived state or cleanup lifetime.

Rejected: removing every imported arity from registration, because `engine/metta/registration.pl:register_prolog_arities/1` records that this loses legitimate library predicates such as `length/2`. Rejected: dispatching every segment-bearing function through the segment-only interpreter, because mixed ordinary and segment equations must retain both answers at a locally compiled arity. Revisit the registration model only as a separate ownership redesign covering explicit host imports.

Stopped the in-progress budget repin with SIGINT to PID 3080744 after confirming its cwd was this worktree. It exited 130. Its partial budget edits will be discarded and measured again against the finished code.

The first mixed-rule assertion expected both answers at an ordinary equation's different written arity, but observed `[99]`, not `[3, 99]`. The existing additivity contract in `segment_equations:segment_and_ordinary_rules_remain_additive_in_source_order` covers equations sharing their written arity. The regression preserves both controls: a local ordinary predicate at arity two, and additive ordinary/segment clauses at their shared written arity one.

The full engine suite rejected placement inside `resolve_dispatch/4`: the native Prolog call ceiling rose from below 30 to 32.0015 inferences, also failing all four materialization-dispatch size controls. Moved the shared ownership predicate behind the existing no-retained-equations branch in both compiled and runtime policy dispatch. Native calls consequently never enter it. This supersedes the placement decision above; the ownership decision is unchanged. SWI-Prolog's `predicate_property/2` and `current_predicate/1` manuals distinguish a visible inherited predicate from a local implementation: https://www.swi-prolog.org/pldoc/man?section=examineprog.

Interrupted the four obsolete repin partitions with SIGINT after verifying their worktree cwd: PIDs 3312887, 3312889, 3312893 and 3312894. Each subprocess reported signal status -2; the partition driver exited 1. Their partial edits are discarded before final measurement.

### Total syntax validation

Final source review found two structural inputs the recursive validator must preserve: an open list supplied by Prolog must not gain bindings, and a cyclic type must refuse before traversal. The throwaway inference-limited probe bounds only that known recursion defect. Decided: check `acyclic_term/1` once at admission using the existing `acyclic_type_syntax` error, then walk without instantiating variables or open tails. Requiring a complete two-element splice list prevents validation from inventing its missing shape. Ordinary source expressions and shared element variables retain their existing representation.

The probe returned `cycle=inference_limit_exceeded` after 10,000 inferences and bound an open tail to `[]` before the correction. The expanded suite passes 16 tests plus nine subtests, including a custom dispatch-seam target. The Python properties pass all five tests with the explicit `[0, 0]` example and 80 generated runs. Native host and materialization-dispatch cost controls pass unchanged.

The prelude spec already has a Purpose contract. Removed the redundant newly added evidence lines from that checksum-sensitive fixture: `engine/bench.py:stamp` hashes all bytes, so pinning their WORKTREE text after functional commit A would invalidate the measured fixture hash in provenance commit B. Its existing header stays, and the engine's prelude header and `prelude_spec` suite carry the changed declaration's evidence.

### Compiling the arriving segment family

The acceptance question now compares a variadic call with a fixed call whose body does no work. `ai-tmp/ai-variadic-call-cost.py trailing 2` measures 129.101 inferences per call over 2,000 warmed loop iterations; `fixed 2` measures 29.101. `two 4` produces five answers and costs 199.101. The identical loop and public stats boundary occur in both arms. `metta_segment_dispatch/4` rebuilds both plans on every call at a new arity; `metta_seq_consume/3` searches prefixes even for a final gap. Neither is a constant-work list tail.

Purpose: execute the finite family selected by a call shape without rediscovering that family at each call. Candidate mappings were prepared SQL plans, template arity instantiation, regular-expression compilation, difference-list tails, pattern-matrix compilation, and partial evaluation with static list spines and dynamic elements. The last mapping fits the existing specializer's publication, recursion and invalidation machinery. LOGEN's `request_clause/1` generalizes inputs, looks up a variant, and registers a pending residual predicate before unfolding: https://github.com/leuschel/logen/blob/0ea806f54628162615e25177c3ed98f6b2c27935/cogen.pl#L539-L614. Its mechanism supports separating known shape from unknown values; no dependency or source code is copied.

Decided before editing: memoize a generated predicate by module, function and arriving arity using the existing specialization ownership and support graph. Publish its identity before compiling bodies, so same-shape recursion targets that predicate. A different recursive shape stays at the runtime dispatcher until it arrives. Source transactions own the generated clause references; source invalidation and space release retire them through the existing specialization action. Derived clauses are executable artifacts, not additional source equations or space atoms.

Generate the shallow head's finite cuts once, preserving the engine's shortest-first ordering and repeated-variable runtime equality. Bind a first variable occurrence directly to its argument; keep comparisons of repeated occurrences as residual goals. A trailing run becomes a compile-time list of argument variables. Splices over those runs are substituted before compiling the body. Patterns whose nested shape is still unknown retain their parsed matcher and body plan; arity alone does not supply their inner list lengths. Preserve ordinary/segment selection at the existing written arity and all dispatch-policy and seam choices. A no-match family compiles to a failing clause. No fixed twin budget is widened to absorb the old interpreter cost.

Rejected: caching only the parsed plan, because every call would still execute the hedge interpreter; adding an arity row to the original predicate, because generated clauses would mingle with retained source and their removal ownership; and inlining bodies recursively at every call site, because recursive equations need a pending named target. The existing specialization support graph supplies that target and its lifetime.

Stopped obsolete repin partitions only after verifying each cwd was this worktree: PIDs 3475219, 3475223, 3475224, 3475227, 3475230, 3475233, 3475236, 3475239, 3475241, 3475246, 3475252, 3475253, 3475254, 3475258, 3475261, 3475262, 3475272, 3475278, 3475285, 3475290, 3475291, 3475292, 3475293, 3475299, 3475300, 3475302, 3475303, 3475306, 3475308, 3475310, 3475311 and 3475312. SIGINT produced -2 for each partition and exit 1 for the driver. Discarded their partial budget edits after the driver exited.

The generated trailing clause reaches 29.101 inferences per call at arities zero and two. The two-splice arity-four family reaches 57.101, equal to the five-answer fixed twin. The 20-test variadic suite plus nine subtests passes with a positive control that executes the former interpreter and exceeds the same direct fixed-callee cost. The comparison deliberately isolates callees: an earlier fixture compared a typed expression's complete runtime checks against an untyped fixed call and was rejected. The public stats benchmark separately measures identical compiled loops.

The first seam regression observed `[3,77]` instead of `[77,77]`: specialization had bypassed compiled target selection. It now consumes `resolve_dispatch/4` and specializes only its unchanged direct target. The module check named the newly crossed private predicates; they are explicitly exported from the translator. A probe briefly raised `specializer:maybe_specialize_call/4: Unknown procedure: specializer:resolve_dispatch/4` before the caller was qualified. The compiler takes each source clause's arrival type group from `dispatch_meta_clauses/3`, avoiding an unproved shortcut from another overload's declarations. Recursive generation, transaction rollback, source removal, repeated-variable equality and literal marker data are covered. The first recursive test wrote the undefined symbol `eq` rather than `==` and reached `Stack limit (1.0Gb) exceeded`; correcting that fixture makes both same-shape and growing-shape recursion terminate. `jscpd` over the changed compiler units and regression suite reports zero clones.

The broader corpus caught `kind 7` answering no values after `kind (row a b)` had compiled the cache. The written-arity selection probe reused its `Head` variable in the generator, binding the ordinary clause to the first segment head. Gave those independent variables separate names and added the two-call regression. The full engine suite also detected a choicepoint in the existing specialization verifier; committed the single split of its proper argument list before selecting the reference call.

`metta_bench:bench_run(translate)` now measured 319,499 against 317,883 before shape compilation, a 1,616-inference tax on ordinary compilation. Decided before changing dispatch: compile the already-known written arity inside the original segment clause, using the same family compiler and an ordinary disjunction. Only a different arriving arity needs a generated predicate and specialization ownership. Enter that generator from the existing segment-only arity branch and inherited-native collision branch. Remove the universal specializer hook. The source compiler already knows which heads contain segments, so another check on every fixed call duplicates that decision.

## 2026-09-09: final call presentation and controls

The revised dispatch reads 317,884 translate inferences in all three fresh processes; evaluate reads 559,089 in all three. `python engine/bench.py --counter-only translate evaluate parse-prolog` exits 0. The shape compiler adds no fixed-call lookup. The fixed presenter remains 20/23/25/29/37 inferences at arities 0/1/2/4/8; the splice presenter is 17/20/23/29/41. These are 100-call totals minus the two-inference harness, divided by 100. Both presenters match the pre-change algorithm at every measured arity through 1024.

`python -m benchmarks.variadic_calls --matrix` installs both no-work programs, checks their complete bags, warms both compiled loops for 10,000 iterations, then counts one loop with public `Space.stats()`. Each instruction figure is the minimum of three fresh processes using perf's controlled `instructions:u` window. Both arms have the same fixed parameter values, declaration element types, setup order and warmup. The earlier separately installed fixtures were rejected after an arity-eight cut case differed by 8.25% in native instructions despite equal inference counts; the paired fixture removes the different heap histories.

| family | run length | before variadic inferences/call | final variadic | final fixed | final variadic instructions/call | final fixed |
|---|---:|---:|---:|---:|---:|---:|
| trailing | 0 | 121.0196 | 29.0202 | 29.0202 | 37971.77 | 37987.83 |
| trailing | 1 | 37.0196 | 29.0202 | 29.0202 | 37264.89 | 37264.93 |
| trailing | 2 | 125.0196 | 29.0202 | 29.0202 | 36903.48 | 37008.34 |
| trailing | 4 | 129.0196 | 29.0202 | 29.0202 | 45638.11 | 45638.14 |
| trailing | 8 | 137.0196 | 29.0202 | 29.0202 | 45203.80 | 45203.74 |
| leading fixed + trailing | 0 | 135.0196 | 29.0202 | 29.0202 | 37349.09 | 37349.12 |
| leading fixed + trailing | 1 | 40.0196 | 29.0202 | 29.0202 | 36807.85 | 36807.87 |
| leading fixed + trailing | 2 | 139.0196 | 29.0202 | 29.0202 | 40243.19 | 40263.18 |
| leading fixed + trailing | 4 | 143.0196 | 29.0202 | 29.0202 | 43569.15 | 43706.57 |
| leading fixed + trailing | 8 | 151.0196 | 29.0202 | 29.0202 | 50887.92 | 50888.17 |
| two runs | 2 | 68.0196 | 45.0202 | 43.0202 | 45938.21 | 46189.89 |
| two runs | 4 | 195.0196 | 57.0202 | 57.0202 | 60250.45 | 60250.45 |
| two runs | 8 | 295.0196 | 85.0202 | 85.0202 | 73989.47 | 73989.80 |

The leading argument is 99, so that family's total arity is run length plus one. A two-run call yields N+1 answers; its fixed twin has N+1 zero-returning clauses and consumes the same bag. At written arity two, the generated disjunction costs two more Prolog inferences than three separate clauses; its native count is lower. This is alternative selection, with no per-call family expansion. Other arities use separate specialization clauses. The class is output-linear in N+1; a single trailing run binds the already-known argument spine during compilation and has constant callee work.

The final generated trailing clauses are variants of the fixed clauses at 0/1/2/4/8 after renaming the predicate. `vm_list/1` shows matching instruction sequences. An earlier written-arity clause normalized its result twice; reusing the complete single-member clause removes that extra branch and output unification. Already normalized multi-member outputs can share the clause output only when the variable is absent from the input parameters. A stale QLF initially hid the repair; clearing engine and library QLF files exposes the final clauses. An unnecessary probe import raised `source_sink 'library(prolog_vm)' does not exist`; removing it leaves the working autoloaded `vm_list/1` call.

The source-level cell probe now costs 65/120/164/208 inferences for numeric runs at 0/1/2/3 and 58/113/178/213 for held runs, after subtracting the 100-call harness. The mistyped run returns its position-two `BadArgType` and costs 1042. The former silent failure cost 436, which did not supply the required answer.

Verification: `sh engine/test.sh suites/typecheck/variadic_arrows.plt suites/reader/segment_equations.plt suites/translator/specializer.plt` exits 0; the variadic unit has 21 tests plus nine subtests. `ruff check extensions/python/benchmarks/variadic_calls.py` exits 0 after fixing S603, TRY003, EM102, RUF005 and D103 findings. `jscpd` over the four changed compiler files reports zero clones. A guessed suite path failed with `engine/test.sh: no such suite: suites/specializer`; the command above uses the actual translator suite.

## 2026-09-09: fixed-program cost classification

The no-work call matrix passes, but the whole twin lane reports 223 findings against 108 on the cut. Of the 208 point-pin findings, 187 are above and 21 below their pins. The initial code therefore cannot justify a uniform repin. The full engine and shell suites pass; the Python suite has 5405 passes, 75 skips and one cost failure: the identity twin reads 3738 against 3696 with allowance 20, at seed 1720573037. The combined requested gates fail only engine-bench and twins. These runs precede the following correction.

Positive controls restore one reader at a time in separate processes, leaving the checkout untouched. Restoring the original diagnostic parameter readers removes 529 inferences from the specializecyclic twin, 5594 from the fingertree twin and 648 from the Newton twin. Restoring admission validation removes 60 from fingertree and 13 from Newton. Restoring dispatch removes 20, 124 and 14 respectively. The 229-inference alternative in memo-per-arity appears with either reader state and is not a declaration-walk cost.

Decided before editing: the diagnostic reader must not invoke the full arrow-arity policy merely to obtain positional parameter types. Its old `append(Parameters, [_], Types)` and `same_length(Parameters, Arguments)` traversals can be one relation that consumes the parameter and argument spines together, expanding only a final splice. Argument-origin calculation still receives the complete presented parameter list, preserving shared type variables. The standalone presenter remains unchanged. This is the same simultaneous list traversal used by SWI's `same_length/2`, https://www.swi-prolog.org/pldoc/man?predicate=same_length%2F2, composed with removal of the final result.

A throwaway comparison over 1000 calls reads old/fused totals 5002/3002, 7002/4002, 9002/5002, 13002/7002, 21002/11002, 37002/19002 and 261002/131002 at arities 0/1/2/4/8/16/128. The wrapper contributes two inferences. The fused walk also preserves the shared splice variable at 0/1/2/4/8. Move the data-mask splice classification into `index_masking_data_heads/0`, as `index_builtin_call_masks/0` already does, so fixed data calls retain their old lookup and same-length walk.

The fused diagnostic reader reduces translate to 313645 and evaluate to 558942. The remaining native-collision guard adds exactly two inferences per ordinary query: match is 268602 against cut 267402 across 600 queries, and match-skew is 208142 against cut 208102 across 20. The existing annotated-arrow dispatcher supplies the appropriate pattern: a name-indexed dynamic clause installed only for the names that need it. Retained segment metadata now owns those compile/runtime guard references per source occurrence; ordinary removal, clear and source rollback erase them. The check remains inside annotated-call verification. No global per-call segment lookup is added.

The indexed guard restores match 267402 and match-skew 208102 exactly to the pristine cut. Translate reads 313370/313371/313371 and evaluate 558932 in all three samples. Their old pins now fail as unpinned improvements. The new source-ownership regression passes. The first fixed-cost regression reached `setup_call_cleanup/3: Unknown procedure: metta_engine:call_cost/2`; qualifying the test-owned helper repairs that fixture. All 23 variadic tests and nine subtests then pass, including unchanged dispatch costs before and after installing an unrelated segment equation.

## 2026-09-09: fuse admission and preserve explicit dispatch ownership

The readers do not mark splice-bearing declarations. `reader.c:parse_sexpr` builds ordinary lists; `declaration_pair` produces `Name-Type`, as `filereader.pl:source_summary_of_forms/3` does for the Prolog reader. `prepare_parsed_forms/1` also accepts host-rewritten terms, and direct host declarations bypass text parsing. Reader-only metadata would leave those admission paths unchecked. The selected fallback retains validation inside the existing source-declaration visit and shares storage admission's annotation scan instead of walking the type again to classify it.

The throwaway `ai-tmp/ai-variadic-admission-fold.pl` adds inline marker recognition to the annotation walk and invokes positional/nesting validation only after finding a marker. Comparing against `spaces:metta_annotated_type/1` over 1000 calls adds exactly three inferences per fixed declaration at arities 0/1/2/4/8/16/128. The fold handles open terms without binding them, and retains the acyclic-term refusal before walking. Source-preflight cost is a separate measurement; the storage-only constant does not establish that cost.

Rejected memoizing the syntax classifier with SWI variant tabling. The primary mechanism is described at https://www.swi-prolog.org/pldoc/man?section=tabling-preds. A warm call costs six inferences, but a cold prepare/admit pair adds roughly 48 over the existing annotation scan and retains per-thread answer tables. The shared traversal is shorter and has a smaller cold constant, with no new retained resource.

The full engine check exposed `spaces_execution_modules:an_engine_emitted_name_cannot_be_taken` and `test_every_engine_emitted_name_is_protected_by_derivation` failures: importing a dynamic `dispatch_policy_execute/5` no longer makes redefinition fail. The fix uses the existing alternative documented by `tests/prolog/static_checks.pl`: a concretely module-qualified emitted goal cannot be captured. Both emitters and the tail-normalization pattern will name `translator:dispatch_policy_execute/5`; its unqualified protected-name row is then unnecessary. Explicitly qualify the installed translator clauses as well, resolving the static check's emitted-private `inherited_segment_goal/3` finding without adding an export.

The two earlier empirical rounds became obsolete when admission changed. Sent SIGINT only to observation driver PID 499182 after checking its cwd was this worktree and its exact argv was `python extensions/python/tools/twin_coverage.py --observe --rounds 10`; its executor finished worker cleanup and reported `KeyboardInterrupt`. No partial observations are used for envelopes. The instrumented matespacefast feature census hit its runner's `timed out after 300s under loadavg 134.41`; an unbounded retry through the same generated twin source succeeds with seven compiled call sites and no fixed declarations or segment heads.

The source-preparation control exposed the remaining repeated traversal: the untabled fold adds N+10 inferences per declaration at fixed arity N, independently of corpus width 1/2/4/8/16. That is not the required constant. Revisited memoization under a narrower condition: table only the context-free marker-presence predicate, as `parser.pl:metta_symbol_writable/1` already tables a syntax property. Do not table validation or annotation validity, because `metta_arrow_type_shape/5` reads the live determinism/effect vocabularies. A private variant table shares the first syntax scan between preparation and storage admission; later variable bindings select a different variant, and marker-bearing types always run live validation. The table belongs to the calling Prolog thread, with no source-dependent invalidation state.

`ai-tmp/ai-variadic-preparation-cost.pl` measures both `refuse_untypable_from_summary_in/3` and `prepare_parsed_summary_in/5` against the pristine cut. With the marker table warm, both add exactly nine inferences per fixed declaration at arities 0/1/2/4/8/16/128 and widths 1/2/4/8/16. The source pass retains its existing visit; it adds no second declaration traversal. Cold shape creation is measured separately. The first detector missed a marker inside an expression-valued type head: `validate_type_splices([['%Rest%','Number'],'Atom'])` printed `accepted`. Traversing compound heads repairs it, while discarding their annotation flag preserves the old annotation classifier's treatment of expression-valued heads. The regression includes that shape and a root type variable.

The final traversal design folds marker placement and annotation classification together. Cache the pure syntax analysis, returning an annotation flag and an ordered list of refusals or live annotated-arrow checks. A plain fixed arrow produces an empty check list. A plain splice's final position is decided from the remaining input spine, without splitting and copying the parameter list. Annotated splice owners retain `metta_arrow_type_shape/5` as a live check, so vocabulary changes remain observable after a cache hit. Refusal records preserve the old left-to-right order, including a malformed annotated owner preceding its element's error. This replaces the marker scan followed by a second positional scan with one syntax traversal per type variant; preparation and storage consume the same analysis. C acyclicity and proper-list boundary checks remain explicit.

The completed syntax fold passes 20,000 randomly generated finite type trees against the preceding validator, preserving the exact exception term. The variadic suite passes 29 tests and ten subtests, including later variable binding, left-to-right refusal priority and a vocabulary member added and withdrawn after the syntax cache is warm. The vocabulary test initially tried adding to closed `determinism`; it now opens that vocabulary through `vocabulary-open` and removes both test rows during cleanup.

The source preparation probe now adds exactly eight inferences per fixed declaration at arities 0/1/2/4/8/16/128 and declaration counts 1/2/4/8/16. The separate cold probe compares prepare-plus-admission with the old annotation visit under the same single-call counter: cold totals 51/52/53/55/59/67/179 against 7/8/9/11/15/23/135, a constant 44 per newly encountered type variant. Warm prepare-plus-admission is 16 at every arity. The counter excludes its one-inference statistics crossing. Initial separate validation made an arrow split and a type-tree walk at preparation, repeated both at admission, then made the old annotation walk: five semantic passes. The final fold makes one semantic syntax pass per type variant, shared by both doors. Acyclic-term, proper-list and variant-key processing still inspect representation in C; those are not claimed absent.

Explicit qualification inside an asserted clause did not settle the static constructed-goal check: SWI removes redundant owner qualification when storing the clause, while the check's file-backed call census misses a dynamically installed private helper. The two installed guards already know the metadata owner. Inlining their three remaining structural conditions removes the private helper and its duplicated owner lookup and global segment flag. There is no new public export and no change to ordinary fixed dispatch.

## 2026-09-09: final admission and lane controls

The full engine suite passes 98 suite runs, 2595 tests and 1518 subtests. The full shell corpus passes. `sh check.sh llms llms-selftest examples parity petta docs` exits 0; parity compares 315/315 examples. The static check also exits 0, retaining its planted unreachable-emitter control. `jscpd` reports zero clones in the changed compiler units and variadic suite; the Python benchmark and property suite pass Ruff.

The original 208 point findings were 187 increases and 21 decreases, median +77, minimum -586 and maximum +181596 against their pins. Regressing those deltas on actual fixed-declaration admissions D and compiled call sites C gives intercept 596.271, D coefficient 76.326, C coefficient 15.699, R-squared 0.01187. Including fixed-arrow presentation visits P gives intercept -95.317, D 4.479, P 32.585, C 1.069, R-squared 0.99493. The extra presentation walk was a real cost, so the fixed diagnostic and mask repairs replace it; these numbers did not authorize a uniform repin.

The final full-lane observation before pins has 249 findings: 221 point findings, seven protocol mismatches and 21 source-relative bands. Comparing the 270 pre-existing point twins with the pristine serial control gives 178 increases, 84 decreases and eight equal counts; median +142, range -27203 to +4216. Its D/C regression gives intercept 64.014, D 13.578, C -2.119, R-squared 0.01115; adding P gives intercept 138.544, D 24.796, P -4.968, C 0.305, R-squared 0.97028. These are descriptive whole-program comparisons between a full-lane round and serial control minima, including the existing 229-inference alternative. They are not the isolated fixed-arrow cost, which the arity probes establish directly. The diagnostic coefficient has changed from a tax to a saving.

The authoring probe reads 7/2841/4423/5563/6944 for zero through four definitions, fitting warmup 1466 plus 1368 each. The pristine control reads 7/2857/4210/5579/6960, fitting 1482 plus 1368 each. The second-definition sample is the existing +229 alternative; the other nonzero rows differ by -16. Tighten only DEFINITION_WARMUP to 1466; leave the slope and every retained overrun unchanged. The obsolete ifsimple overrun was removed under the separate ruling. Exclude every example with a pristine-control finding from the point-repin selection.

The final paired no-work matrix preserves 29.0202 inferences per iteration for both trailing and prefixed families at 0/1/2/4/8; the shared loop and stats boundary account for the fraction. Two-run families remain 45.0202 versus 43.0202 at arity two, and equal 57.0202 and 85.0202 at arities four and eight. The final generated trailing clauses are variants of the fixed clauses at all five arities, with matching VM sequences. Native counts are retained with their three-sample ranges: they measure the entire loop, and equality of every hardware sample is not claimed. For example trailing arity two has variadic samples 376830430/377428016/378216984 versus fixed 378697219/377908251/367751470 over 10000 iterations; their ranges overlap despite different minima.

## 2026-09-10

The actual admission-door probe compares `metta_add_atom/3` and `spaces:metta_add_atom/4` against the pristine cut, five samples per arity and temperature. At arities 0/1/2/4/8/16/128, every cold delta is exactly +37 for both doors. `/4` reads 44+N before and 81+N after; `/3` adds one to each. With the syntax shape cached, `/4` reads 46 at every arity and `/3` reads 47, so the warm delta is 2-N. The complete prepare/admit syntax probe's +44 includes its separate preflight wrapper; it is not substituted for the measured public-door constant. The probe erases only its owned syntax-table variant outside the cold counter and releases every temporary space.

The full Python suite finishes with 5405 passes, 75 skips and one failure at seed 3508372787: identity costs 3724 against its old 3696 budget with allowance 20. The awaited-future and static module tests pass in this run. The pristine control independently reproduces the earlier discarded-Cursor warning in the same awaited-future test, at seed 3649069646. The identity pin is part of the authorized remeasurement; its allowance is unchanged.

The complete source-preflight plus public admission probe reads 68+N at the cut and 113+N cold on the branch, exactly +45 at arities 0/1/2/4/8/16/128. Warm admission reads 78 at every arity. These are actual door costs, distinct from the narrower syntax-region comparison of +44 above. Both controls use five identical samples per arity and release each temporary space.

Point repricing selected 127 newly moved paths while excluding every path with a pristine-cut finding. Eight `twin_coverage.py --repin --reason` partitions exit 0, change 123 pins and change no stored-content divergence. The signature and faces pins are 16435 and 36514, including cold family generation. The identity pin is 3724 with its allowance unchanged. The subsequent full Python run at the same seed passes 5406 tests and skips 75 in 812.42 seconds. The explicit MORK selection passes 28 tests with no skips.

The requested combined gate passes prolog, prolog-static, provenance-pin-selftest, llms, llms-selftest, examples, parity, petta and docs. Its evidence lane initially reports `extensions/python/tools/twin_coverage.py:328: measured: carries no YYYY-MM-DD date, so the claim cannot go stale`; adding the measurement date makes evidence and provenance-pin-selftest pass. Engine-bench retains boot 298441 against the standing 301230, match 267402 against 266202 and match-skew 208102 against 208062. The cut reproduces the exact match and skew counts; its boot is 296767 at the control path, so the branch also has its own +1674 boot cost. The accepted fixed-call repairs remain translate 313362/313363/313363 and evaluate 558931/558932.

The combined twin lane reports 150 findings before the empirical protocol update. Thirty-nine point findings are on paths without a cut finding. Thirty-six have observed full-lane minima within four of their current pins, including the 229-inference and library-load alternatives. Three unusual readings, soft +793, nars_tuffy -15 and nars_derivation_control -23, did not recur in any of the ten subsequent full-lane observations. Keep those measurements explicit; do not widen a deterministic allowance to license them. Source-relative bands and the pristine cut's debts remain for integration.

Ten rounds of `python extensions/python/tools/twin_coverage.py --observe --rounds 10` complete under full-lane/279/workers=32. Six empirical twins produce ten costs each. Channels/pools produces nine costs and one round-nine `AssertionError`; the failed round is not a measurement. The seven mappings use exactly the successful samples' extrema and observation counts, with dated RE-OBSERVED paragraphs. The point `--repin --reason` command reports that an envelope budget does not admit point repricing. No margin or overrun is added.

Ownership audit: the private syntax table retains its keys for the calling Prolog thread's lifetime, including an opaque stream key in `[->, Stream, Atom]`. This is the declared cache lifetime, not a source-owned resource. A direct `current_table/2` probe confirms the retained key. Python weak-reference probes retain their object both at the pristine cut and on the branch, even after abolishing the syntax table, so they do not attribute an additional Python leak to the table. A census with the wrong module context raises `janus_swi.janus.PrologError: '$c_call_prolog'/0: Arguments are not sufficiently instantiated`; querying `current_table/2` inside translator supplies the correct census. No unsupported claim of immediate host-object release is made.

The standalone complete twin lane after the first re-observation exits 1 with 99 findings over 279 examples: 87 point findings all on pristine-debt paths, eleven source-relative bands and one empirical boundary. The two new cells separately exit 0 with zero findings. Channels/pools reads 96793, four below its observed minimum 96797; empirical bounds do not receive the deterministic four-inference tolerance. This triggers another exact ten-round full-lane re-observation. Its prior round-nine AssertionError is also recorded in the cut file, which reports one such failure in twenty earlier rounds. The executable twin body is unchanged.

The final merge audit confirms both `add-atom/3` clauses are byte-identical to the cut and `engine/spaces/foreign.pl` has no diff. No pristine-debt point budget changes. Across all modified twins, no allowance or stored-content divergence changes; the sole overrun change is the instructed removal of ifsimple's 67. The final eleven source-relative excesses are ifsimple 2, roman 84, if_decons_expr 1, decons_and_substitution 6, fingertree 1, memo_aggregate 36, memo_variant_nonground 4, memo_dependency_invalidation 39, memo_stats 39, tabling_fib 22 and newtons_method 19. They remain unrelaxed.

The ratio controls distinguish source-ceiling movement from twin overhead. Roman's source/twin deltas are -1497/-1512; fingertree's are -579/-633; if_decons_expr's are -18/-18; decons_and_substitution's are -7/-1; memo_aggregate's are -7/+13; memo_variant_nonground's are -1/+3; newtons_method's are -51/-52. These seven new bands do not share a uniform offset that would authorize changing their overruns. The existing source-derived formula and newly measured authoring warmup therefore remain unchanged after the comparison.

The second observation was interrupted externally before it wrote any samples. Its process no longer existed and its receipt was empty on resumption; the existing-session wait returned `write_stdin failed: Unknown process id 44861`. The identical ten-round command was restarted. No partial observation is included in the empirical evidence.

The final baseline audit found that `engine/bench.py --counter-only --update-baseline` also updates advisory `wall_seconds_per_operation` fields. Restore the cut's evaluate and translate wall observations; only their measured inference pins are part of this repricing. `engine/bench.py:observe` labels wall time advisory, and it does not decide the gate. Native instruction pins and all allowances remain unchanged.

After restoring those advisory fields, `python engine/bench.py --counter-only translate evaluate parse-prolog` exits 0. Three fresh samples per row are translate 313363, evaluate 558932 and parse-prolog 3517109. The respective pins 313362, 558932 and 3517109 remain unchanged. The resumed evidence and provenance self-test lanes also exit 0: zero unsupported claims and zero defects over 35 planted placeholders.

The restarted exact ten-round full-lane observation exits 0 with 279 rows. Channels/pools again raises `AssertionError` in round nine; all other examples produce ten costs. Pool both completed observation runs and all three other final-code complete-lane receipts under full-lane/279/workers=32. Their seven empirical bounds are mutex 17027..17107, thread_lib 283715..344675, thread_linda 135783..136106, channels/pools 96793..97949, Prolog rung 122319..124124, git_import 26070..26145 and measure 130377..130672. Each has 23 successful costs except channels/pools, which has 21 costs and two failures. The derivation adds no margin and preserves every sample with its receipt in `ai-variadic-envelopes-land.json`.

`python ai-tmp/ai-variadic-replay-envelopes.py` exits 0: the unchanged `_budget_findings` accepts all 159 successful pooled costs and rejects all 21 planted controls, one below the minimum, above the maximum and wrong protocol for each twin. This is counter-only replay, not another semantic lane. It resolves the recorded 96793 boundary failure; the last complete standalone lane still records 99 findings, of which 98 remain after applying the new empirical bounds: 87 pristine-debt point paths and eleven source-relative bands. Future scheduler observations are not guaranteed to stay within measured extrema.

The required `twin_coverage.py --repin --reason` pass after pooling exits 0 for all seven empirical twins: `re-pinned 0 and settled the stored-content divergence of 0 of 7 twins`. Each reports `envelope budget, point re-pin does not apply; widen with --observe`. The mechanically derived bounds remain intact; no executable body or divergence changes.
