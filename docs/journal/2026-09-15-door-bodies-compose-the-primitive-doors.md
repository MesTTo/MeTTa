# Door bodies compose the primitive doors

Goal: each owned body composes the primitive that owns its engine crossing, and source-declared dependencies retain their identity through door analysis.
Constraint: preserve public answers and exact inference costs; classes files, binding Prolog, engine code and door generators belong to integration. Keep open callbacks, mixed crossings and cycles visible.

## 2026-09-15

Tried: `extensions/python/tools/doororder.py --json` at `efac42a31055f59a2ff0580f1ff138072c946fab` after deleting generated `.qlf` files. Result: exit 1; 227 doors, 94 mixed, 167 open, 77 recursive; 42 numbered, comprising 22 at order 0, 16 at 1 and 4 at 2. Evidence: `ai-tmp/ai-doors-order-0.json`.

Decided: inspect shared sites before callers, in this order: `seam.Point._expect` (62 cited doors), `_atoms/wire.py` (72), `_binding/json.py` (65), `_declare/declarations.py` (62), `_atoms/registry.py` (58), `_atoms/namespace.py` (57), `_atoms/templates.py` (55). `_DISPATCH` already declares string values; analysis must read that declaration before a body rewrite is justified.

Decided: then process door families in `_spaces/lifetime.py` (101), `_spaces/store.py` (96), `_spaces/profile.py` (56), `_spaces/query.py` (55), `_observe/diagnostics.py` (56), `_spaces/results.py` (42, shared), `_spaces/scope.py` (27), `_spaces/execution.py` (24), `_spaces/context.py` (12), `_spaces/cursor.py` (11), `_spaces/intents.py` (9), `_atoms/calls.py` (13). The numbers count doors citing each module, not independent defects.

Decided: finish the owned remainder: `_declare/prolog.py`, `events.py`, `foreign/_persistent.py`, `_catalog/arrow.py`, `subscribe.py`, `structures.py`, `_observe/debug.py`, `_observe/trace.py`, `_history/world.py`, `_atoms/library.py`, `library/_lock.py`, `_catalog/meaning.py`, `_catalog/documentation.py`, `_catalog/cast.py`, `integrate.py`, `_compile/facts.py`, `_catalog/declarations.py`, `derivation.py`, `_history/saga.py`, `spaces.py`, `live.py`. Record other-member findings as handovers. Review canonical names, guard budgets/truth, cursor refill and documentation extraction when reaching their owning families.

Open: known excluded dependencies include `_binding/runtime.py:runtime`, `_atoms/model.py:Expression.to_wire`, `_atoms/model.py:explicit_metta_atom`, `_atoms/mentions.py:callable_mention`, `_spaces/handle.py:SpaceHandle.__init__`, `_spaces/evaluate.py`, `_spaces/source.py`, `_catalog/annotations.py`, `_catalog/project.py`, `_declare/define.py`, `_atoms/factories.py`. Their precise body changes belong in the integration table after tracing the final residual graph.

### Source-declared containers

Tried: the unchanged analyser passes all 18 existing door-order tests. Added declaration witnesses expose a native callback and a recursive callback both incorrectly numbered 0, qualifiers treated as receiver classes, dropped list callbacks and tuple destructuring that binds the tuple instead of its fields. First witness run: 11 failed, 20 passed; two failures were a malformed expected set in the new test, corrected before using that assertion as evidence. Logs: `ai-tmp/ai-doors-order-tests-before.log`, `ai-tmp/ai-doors-declarations-witness-before.log`.

Decided: preserve container allocations and their contents in finite source-site slots, including writes through aliases. Tuple positions preserve declared receiver identity; mutable sequences and mappings conservatively join their possible elements. Unimplemented container operations remain open. Qualifiers describe a binding and cannot erase its concrete initializer. The transfer follows [PyCG's source allocation and content pointers](https://github.com/vitsalis/PyCG/blob/8d5dc40837803beef1d8d379fbf2cdad6cd94641/pycg/processing/postprocessor.py); [Python's qualifier contract](https://docs.python.org/3.14/library/typing.html#typing.Final) supplies no runtime receiver.

Rejected: treating every mapping lookup as host work, because a planted table value can invoke a native operation or recurse. Revisit only with a proof that the looked-up value is never called. No seam body change is needed for its existing string table.

Tried: 42 declaration witnesses pass, including literal keys, alias writes and builtin container copies. The first projection battery caught synthetic AST nodes lacking source locations; allocation sites now handle those nodes. Mypy from the seat root passes after annotating the call-result set. The second projection battery passes every requested lane and companion selftest. Its Griffe traversal printed `Timeout (0:03:00)!` as a diagnostic dump, then completed all 69 door-sync selftests successfully. Logs: `ai-tmp/ai-doors-declarations-witness-final.log`, `ai-tmp/ai-doors-analysis-mypy-seat.log`, `ai-tmp/ai-doors-projections-2.log`.

Measured: 94/167/77 mixed/open/recursive before, 103/168/101 after; numbered doors change from 42 to 41 (22/15/4 at orders 0/1/2). `Point._expect` disappears from every open finding. The graph now sees callable values carried by builtin copies, including provider length callbacks and additional helper cycles; these findings remain visible. Evidence: `ai-tmp/ai-doors-order-0.json`, `ai-tmp/ai-doors-order-3.json`. No runtime door body changes in this commit; inference cost probes accompany the body repairs.

### Declared supplied contracts

Decided: refine the 2026-09-08 gate verdict by the public signature. A supplied `Callable`, `Iterator`, `Iterable` or caller-implemented `Protocol` can re-enter any door, so it remains unordered and its owning site stays in `open`. A door depending solely on such a door also stays unordered. Both pass the gate. A registry callback, undeclared attribute, unresolved receiver or dynamic import remains a defect. Native crossings combined with supplied calls remain mixed. The catalog keeps its independent open and dependency reasons; no integer or allowlist replaces these facts.

Tried: the four required planted controls retain a registry callback as a defect, retain a native-plus-callback body as mixed, accept a declared callback while keeping its number absent, and accept its dependent door while retaining `blocked_by`. The existing unresolved-callback and supplied-iterator witnesses remain unchanged. Initial run: 3 failed, 43 passed. The first implementation exposed non-serializable `ContractCall` records, repaired by explicit dataclass projection. Additional protocol controls exposed an undeclared iterable method being accepted and premature abstract receiver seeding; declarations now identify their operations after import resolution. A malformed quoted test annotation was corrected. Evidence: `ai-tmp/ai-doors-contract-witness-before.log`, `ai-tmp/ai-doors-contract-witness-after.log`, `ai-tmp/ai-doors-contract-protocol-before.log`.

Tried: all 55 planted and existing witnesses pass; configured-seat mypy passes for the two analyzer modules, and Ruff passes for all four changed Python files. An initial mypy command including the standalone tool reported `Cannot find implementation or library stub for module named "doorgen"`; the tool is checked by its actual CLI witnesses and Ruff. Evidence: `ai-tmp/ai-doors-contract-protocol-after.log`, `ai-tmp/ai-doors-contract-mypy-seat.log`.

Measured: legacy mixed/open/recursive counts stay 103/168/101. The refined report identifies 167 defect-open doors and one door unordered solely by contract. The numbered distribution remains 22/15/4. Evidence: `ai-tmp/ai-doors-order-contract.json`.

Tried: the final 55 witnesses and all thirteen required projection lanes with their companion selftests pass. `door-sync-selftest` completes all 69 checks in 718.58 seconds. Evidence: `ai-tmp/ai-doors-contract-final.log`, `ai-tmp/ai-doors-projections-contract.log`.

### Definite wire decoding

Tried: both public decoder paths raise `RecursionError: maximum recursion depth exceeded` for 5000 nested undefined wrappers; three ordinary/deep/malformed controls pass. Evidence: `ai-tmp/ai-doors-wire-before.log`.

Decided: the complete-answer decoder recognizes the one outer undefined wrapper, then delegates to the definite atom decoder. That lower decoder rejects an undefined wrapper immediately. Expression decoding and leaf validation keep their existing implementation, including the dominant symbol path.

Tried: all 411 tests in chapters 03 and 06 pass; Ruff passes both changed Python files. Evidence: `ai-tmp/ai-doors-wire-chapters.log`.

Measured: the representative wire call remains 7/7/7 inferences with the same expression and undefined answer. The other fixed probes remain 55/55/55 for JSON rendering, 104/104/104 for matching and 437/437/437 for evaluation. The wire decoder component disappears from every cycle finding. Legacy mixed/open/recursive counts stay 103/168/101 because affected doors retain other cycles. Evidence: `ai-tmp/ai-doors-costs-before.log`, `ai-tmp/ai-doors-costs-wire-after.log`, `ai-tmp/ai-doors-order-wire.json`.

Tried: the projection battery found one stale generated reference page, `website/reference/metta-convert.md`; its dependent door-sync and ledger selftests refused the same drift. Regenerated it through `doorgen.py --write`. All thirteen requested lanes and their companion selftests now pass, combining the unchanged passing lanes with the repaired door-sync, ledger and reference run. Evidence: `ai-tmp/ai-doors-projections-wire.log`, `ai-tmp/ai-doors-wire-regenerate.log`, `ai-tmp/ai-doors-projections-wire-repaired.log`.

### Literal fields and completed declarations

Tried: the contract pair had identical aggregate counts but 100 differing open-site rows. Empty generator facts became permanent object receivers, and missing fields became permanent unknown receivers before their declarations arrived. Literal `object.__getattribute__` calls also hid dynamic names and descriptor crossings. Five new controls failed while 57 passed. Evidence: `ai-tmp/ai-doors-attributes-before.log`.

Decided: propagate source string literals through aliases and setter parameters. Store instance fields separately from class declarations, so instance-held functions receive no implicit receiver and only class descriptors bind. Preserve the data-descriptor priority specified by [Python 3.14's descriptor protocol](https://docs.python.org/3.14/howto/descriptor.html#invocation-from-an-instance). Missing declarations are reported after value propagation, without becoming permanent provisional receivers.

Tried: three additional controls exposed a lost literal name in a delegating setter, a spurious receiver on an instance-held callback, and an instance-held descriptor being invoked. After repair, 66 controls passed. Two full reports still differed on six doors. The next two planted controls exposed a helper's premature optional annotation and a broad return annotation hiding a callable. Evidence: `ai-tmp/ai-doors-fields-before.log`, `ai-tmp/ai-doors-fixedpoint-before.log`.

Decided: settle caller values before completing absent helper annotations in a batch. Source return values and initializer values retain their identities; declarations refine opaque external results. Tuple positions remain empty until their own values arrive. A pure read of the completed graph records missing boundaries. Sorting a worklist alone was rejected because it would retain the same provisional facts under one arbitrary schedule.

Tried: an initial opaque-result repair exposed the existing ContextVar declaration control; its declared receiver now refines the opaque constructor result. All 68 controls pass. Seat-root mypy and Ruff pass; invoking mypy from the repository root instead produced 45 package-resolution errors. Evidence: `ai-tmp/ai-doors-fixedpoint-after.log`, `ai-tmp/ai-doors-fixedpoint-refined.log`, `ai-tmp/ai-doors-fixedpoint-mypy-seat.log`, `ai-tmp/ai-doors-fixedpoint-ruff.log`.

Measured: 103/168/101 mixed/open/recursive before, 105/180/104 after; 179 doors have defect-open findings, one is unordered solely by contract, and 25 are numbered (22 at order 0, three at order 1). Complete reports under `PYTHONHASHSEED=123` and `456` agree on every row. The newly visible callable returns and descriptor paths remain findings. No runtime body changes occur in this analysis repair. Evidence: `ai-tmp/ai-doors-order-fixedpoint-123.json`, `ai-tmp/ai-doors-order-fixedpoint-456.json`.

Tried: a further control after that measurement, `ai-tmp/ai-setter-pair-probe.py`, failed with `AssertionError`. `SpaceHandle.__setattr__` only forwards `(self, name, value)` to `object.__setattr__`; the context-insensitive graph joined every caller's name into one parameter slot and every value into the other, so each of its thirteen fields held every value assigned through the setter, and `self._rt.must(...)` reported `builtins.NoneType.must`, `builtins.bool.must` and five more false open receivers at 72 sites. Evidence: `ai-tmp/ai-doors-setter-pair-before.log`, `ai-tmp/ai-doors-open-reasons-dirty.txt`.

Decided: a body whose only statement forwards its own parameters, each a plain name, to a callee resolving to exactly one attribute intrinsic (`getattr`, `hasattr`, `setattr`, `object.__getattribute__`, `object.__setattr__`) is that intrinsic under an argument permutation. Its callers apply the intrinsic to their own paired actuals and never join them into the forwarder's slots; its body is not scheduled from those sites. Recognition resolves the callee expression through the analyser's own lookup, so a module alias of `object.__setattr__` qualifies and a helper named `__setattr__` does not by name. A re-entered forwarder (a setter forwarding to `setattr`) falls back to the ordinary path, which terminates and, faithfully, never stores the field. Module scopes are scheduled before class bodies and entries so an alias is bound before its first use. This is the case Hybrid Inlining calls a context-critical statement propagated to its callers ([Liu et al. 2022](https://arxiv.org/abs/2210.14436)); the general call-site-sensitive cures (k-CFA, cloning) were rejected because one body shape carries the whole loss here and the analysis runs inside every boot.

Rejected: recognising forwarders with additional statements by propagating only their intrinsic call, because the remaining statements still need the joined parameters and the body's own intrinsic call would have to be skipped. Revisit if a seat setter carries statements beside its intrinsic call; today `SpaceHandle.__setattr__` is the only forwarder and `Library`, `Atom` and `Namespace` setters raise.

Tried: seven planted controls (paired fields under the default and a reversed scheduler, alias plus keyword actuals, native statement, class data descriptor, dynamic member name, `setattr` recursion). The recursion control first asserted a native crossing and was corrected to the faithful outcome: no field write, an open finding, termination. All 75 door-order controls pass; seat-root mypy and Ruff pass. Evidence: `ai-tmp/ai-doors-unit1-mypy-seat.log`, `ai-tmp/ai-doors-unit1-ruff.log`.

Measured: `Space()` boot plus one `run` takes 3.91s at the cut in a detached control worktree (`ai-tmp/wt-doors-control-efac42a31`) and 34.58s on the dirty tree, because `doors.publish` runs this analysis once per process. `doororder.py` wall: 3.2s at efac42a31, 12.96s at 15e4ceec3, 17.93s at 07976cf8b, 17.19s at cfe153315, 41.0s dirty. The profile recorded 336,754,856 calls: `_bases` recomputed every MRO, `_symbol` re-resolved every import at every read, `ast.unparse` ran per finding message, `Reference.__hash__` ran 32 million times. Evidence: `ai-tmp/ai-doors-time-*.log`, `ai-tmp/ai-doors-analyzer-fields.prof`.

Decided: memoize `_bases`, `_symbol`, `_annotation` and `_parameter_contract` against the exact store slots each computation reads; a hit replays the reader registration and a growth of any read slot evicts the entry; a computation that read a narrowed slot is not kept; memos clear at each phase change. Import symbols resolvable at index time (functions, classes, modules, external roots) are stored resolved, so `_references` rarely rebuilds a set. Unparsed source and literal keys are cached by node identity while holding the node. `Reference` caches its hash. Result: 17.8s, 24,902 evaluations of 2,282 scopes, and byte-identical rows against the unmemoized run under seeds 123 and 456. Evidence: `ai-tmp/ai-doors-analyzer-memo.prof`, `ai-tmp/ai-doors-analyzer-memo2.prof`, `ai-tmp/ai-doors-order-unit1-123.json`, `ai-tmp/ai-doors-order-unit1-456.json`.

Measured: 105/180/104 mixed/open/recursive with the dirty draft, 104/180/103 after the forwarder repair; 179 defect-open, one contract-only, 25 numbered (22 at order 0, three at order 1). The seven false receiver families on `_rt` reads are gone; 72 `builtins.NoneType.must` sites remain because `runtime()` returns `_STATE.runtime`, declared `Runtime | None`, which no local narrowing covers. The families that keep the other 100-door cluster open are typed in the next section.

Open: the boot cost class. A 17.8s analysis in every process is still the wrong place for a source projection; the next section moves it to a generated table.

### Door orders as a generated projection

Tried: `doors.publish` runs at every boot and derives each door's `door-order`/`door-unordered` term from `orders(rows)`, the whole-tree analysis, cached only per process. `Space()` plus one `run`: 3.91s at the cut, 34.58s on the dirty tree, 18s after memoization. The cost is paid by every pytest worker, every CLI invocation and again on every door registration after boot, which invalidates the cache. Evidence: `ai-tmp/ai-doors-time-*.log`, `ai-tmp/ai-doors-boot-table.log`.

Decided: the catalog publishes five facts per door (number, mixed, open, recursive, dependency), a `Verdict`; `doororder.py --write` renders every declared door's verdict into `metta/doors/_orders.py`, a generated module in the shape `layergen`, `facegen` and `doorgen` already use, and the `door-order` lane refuses a missing or stale table beside its defect findings. `orders(rows)` reads the table and analyses only rows outside it, so a provider door registered from an unknown package is still derived live from the core plus its own module. `Order` keeps every site for the report. The table cannot be declared in `tools/artifacts.py` from this lane (classes-held); the integration table records that row.

Rejected: pickling the solved graph to disk keyed by a source hash, because a second cache layer with its own invalidation, permissions and concurrency is longer than reading a 227-row literal and the drift lane already exists for generated projections. Revisit if runtime-registered doors from unknown packages become common enough that the live path's 18s matters; an incremental extension of a retained core graph is the next step then.

Tried: `Space()` plus one `run` in three consecutive processes: 0.61s, 0.57s, 0.72s, with `_snapshot.cache_info()` reporting no analysis. Door-order controls: 79 pass, including a missing/stale-table refusal, a hypothesis round trip of rendered verdicts, a no-analysis boot over every declared row and a live analysis for a planted door outside the table (`space:planted-probe`; the first draft named it `first`, which collided with the shipped `space:first` and read the table). `tests/repository/test_door_rows.py`: 59 passed in 261.75s. Seat-root mypy and Ruff pass. Evidence: `ai-tmp/ai-doors-boot-table.log`, `ai-tmp/ai-doors-unit2-door-rows.log`, `ai-tmp/ai-doors-unit2-mypy-seat.log`, `ai-tmp/ai-doors-unit2-ruff.log`.

## 2026-09-16

### Declared structure the analysis must read

Tried: attribution of every defect-open door to the files owning its blocking origins at 903d23fbc (`ai-tmp/ai-doors-attribution.py`): 180 blocked doors, 30 blocked by owned files alone, one 150-member helper cycle reaching 109 doors with 42 members in the algebra package, 39 in classes-held files and 69 in owned ones. The dominant open families were analysis losses, not body defects: `attribute of an external value` (26,265 sites from 377 origins, `os.environ.get`, `inspect.Parameter.empty`), `member X of untyped builtins value` (1,859 origins: `list[Atom]` and `Iterable[Atom]` reduced to `builtins.list`/`collections.abc.Iterable`, `*more: Any` holding every caller's arguments), `undeclared member supplied parameter` (13,544 origins: `target: Callable[[], _R] | Any` read as a callable only), `result of stdlib` (`threading.RLock()` results open when used). Evidence: `ai-tmp/ai-doors-families.py` over `ai-tmp/ai-doors-order-unit3d-123.json`.

Decided: read each declaration where the source already states it. A subscripted annotation keeps the elements, keys, positions or class it declares, classified through `typing.get_origin` and the ABC hierarchy as `metta._catalog.annotations` classifies runtime annotations, and its container's admitted operations are the declared type's own (`Iterable[int]` has no `__getitem__`). A generic base names its class. A declared parameter or return type refines an opaque, external or `object` actual. `*args` and `**kwargs` bind as a tuple and a mapping. `Class.__dict__["slot"].__set__` and `.__get__` reach the field a `__slots__` sequence or documenting mapping declares. A `UserList`/`UserDict`/`UserString` base stores its constructor's elements in `data`, per Python's documentation. With overloads, a door's public contract is the overloads' alone, and an `Any` arm is a value of unknown structure. `callable(x)` and an early exit narrow as a type checker does; a supplied contract survives a positive `isinstance` only where the tested class implements every operation it declares, read from the MRO's own namespaces because `hasattr(str, "__call__")` answers about the class object. A standard-library result is host work; a member no standard-library type declares is open; what a supplied callable answers is one derived reference per parameter that carries the contract.

Rejected: summarising every unmodelled standard-library call by its arguments (invoke every callable argument, return what was passed), because `callable(target)` then invoked the supplied callback and `os.environ.get("RUNTIME")` returned its own key as a `str` receiver. Revisit never; typeshed states which parameters are invoked. Rejected: computing a class's descendants by walking every class's bases under the memo, because each walk read every base declaration in the tree and recursed through the attribute lookups those declarations need; the 2026-09-16 00:12 report ran 9.5 hours at 99% CPU, was interrupted by the integrator, and grew to 18.8 GB before SIGKILL (`ai-tmp/ai-doors-order-unit3e.err`). `_bases` now records each class under its bases as it computes them, and every class is indexed once per phase. Rejected: a fresh `"result of ..."` reference per contract call, because members and calls of it chained into an unbounded domain (30 million values in 140 seconds, `ai-tmp/ai-doors-runaway-probe.log`).

Decided: the standard-library parameters that invoke a callback are derived from mypy's typeshed by `doororder.py --write` into `metta/doors/_invocations.py` (607 callables; version-guarded stub blocks are read for the running interpreter), and the door-order lane refuses its drift beside the verdict table's. The core names no third-party library: the tool reads typeshed, the analysis reads the shipped table. The listed `map`, `filter`, `reduce` and `key=` cases are subsumed.

Tried: 39 new controls; 114 door-order controls pass; seat-root mypy, Ruff and the policy inventory lane pass (thirteen exemptions were placed adjacent to the closed sets the new code reads). `_call` was split at the external branch to keep its complexity under the seat's ceiling. Evidence: `ai-tmp/ai-doors-unit3-mypy-seat.log`, `ai-tmp/ai-doors-unit3-ruff.log`, `ai-tmp/ai-doors-unit3-policy.log`.

Measured: at 903d23fbc the report read 104/180/103 mixed/open/recursive, 179 defect-open, 25 numbered (22 at order 0, three at 1), one contract-only, 111,877 distinct sites in 25s. After this unit: 111/176/112, 159 defect-open, 29 numbered (25 at order 0, four at 1), nine unordered solely by contract, 116,732 sites in 47s. The report now lists each site once with per-row indexes; the previous form repeated every site per door and reached 963 MB. Evidence: `ai-tmp/ai-doors-order-unit3g-123.json`, `ai-tmp/ai-doors-order-unit3-write.log`.

Open: the 150-member helper cycle and the `Any`-typed `under` selector on `eval`, `answers`, `match` and `stream` (15,632 sites each) are body work, in classes-held `_spaces/evaluate.py` for two of them and owned `_spaces/query.py` for the other two; the next section takes the owned ones.

### Declarations are authoritative

Tried: at 89713be41 the remaining owned-only doors were blocked by `runtime()` answering `Runtime | None` (its `-> Runtime` declaration ignored while the flow-insensitive union kept the module state's `None`), by `self._finalizer.detach()` behind an attribute `is not None` test the narrowing did not read, by `getattr(source, "close", None)` losing its default, by `for row in self` on `Rows` binding the iterator rather than its elements, by `type(self)._columns` reading an instance of `type`, and by `serve(m: SpaceLike)`'s contract reaching every door body that declares `space: Space`. Evidence: `ai-tmp/ai-doors-order-unit4a-123.json` through `unit4g`.

Decided: a declaration mypy verified (a gate lane here) is authoritative at the boundary it guards. A returned or passed source value the declaration cannot admit does not escape; a supplied, unknown, opaque or external value reaches a concrete parameter as the declared class, and the caller's re-entrancy or undeclared read is recorded at the caller's own site; an unknown narrowed by a positive `isinstance` is the tested class, a container class holding it as its elements. `getattr` answers its default; `type(x)` is x's class and `type(name, bases, ns)` resolves through its bases; `__iter__` answers an iterator whose elements a loop binds, for containers and source methods alike; an inherited `UserList` iterates what it stores; `x.attr is None` narrows the field in every holder; a narrowing that admits no value marks the branch unreachable rather than empty. Two owned helpers declared their real parameter types: `_spaces/subscriptions.py:_cancel_abandoned_subscription` and `_WatchIterator.__init__` take a `Subscription`, `integrate.integrate` takes a `Space`.

Rejected: keeping an empty narrowed set, because `if callable(close): close()` over `{None}` then read as an unresolved call. Revisit if a field written only by a method no door reaches is narrowed to nothing; today every writer is reached from a door or an initializer.

Measured: 122 controls pass; seat-root mypy, Ruff, the policy inventory and closed-set lanes pass. Report: 104/180/103 mixed/open/recursive, 179 defect-open, 25 numbered and 118,769 sites at the takeover; 106/141/107, 139 defect-open, 60 numbered (34 at order 0, 22 at 1, four at 2), two unordered solely by contract and 3,724 sites now, in 22s. Three doors remain blocked by owned files alone: `space:integrate` (its `target: Any` is the door's own contract and `metta_remote.serve` reads `m.transaction` off a `SpaceLike`), `space:prolog` (`space._rt._janus.prolog()` reaches `Runtime._janus`, classes-held), `space:why` (`match` answers `Rows | list` because `into=` selects the type without overloads). One 97-member helper cycle reaches 102 doors: 39 members in `algebra/`, 26 in classes-held files, 32 owned. Evidence: `ai-tmp/ai-doors-order-unit4g-123.json`, `ai-tmp/ai-doors-attribution.py`.

### The whole gate

Tried: `GATE_ONLY=1 sh check.sh` on 83e9dec80 in a provisioned battery worktree (`ai-tmp/wt-doors-battery`, node artefacts copied as audits described), 171 lanes, exit 1, 20 red. Eleven are the integrator's baseline reds on this box (engine-bench, c-bench, mork-bench, node-bench, pytest, benchmarks, instructions, memory-scale-gate, twins, extcost, door-order). Nine were not: `codespell` read `ccompiler` in the generated invocation table, because typeshed's `distutils` stub (3.0-3.11) had been read on 3.14; `deptry` refused the tool's `import mypy` as a dev dependency; `vulture` read `Order.defect_open` as dead because its readers, the door-order tool and its controls, are outside Vulture's paths; `reference`, `door-sync`, `door-sync-selftest` and `ledger-selftest` found `website/reference/metta-integrate.md` stale after `integrate` declared its `Space`; `mork-rust` could not find `ai-tmp/MORK/kernel`, a path relative to the checkout that the doubly nested battery worktree resolved one directory off; `parity-fuzz-selftest` reported no fuzz report under the run's load of 29. Evidence: `ai-tmp/ai-doors-final-gate.log`.

Decided: the invocation table reads only the stub modules typeshed's `VERSIONS` file ships for the running interpreter (607 callables become 590, `distutils` gone); `mypy` joins `griffe` in the seat's DEP004 allowance with the reason beside it; `defect_open` is whitelisted for Vulture with its readers named; the reference page is regenerated. Rerun in this worktree: codespell, vulture, deptry, reference, reference-selftest, door-sync, door-sync-selftest, door-order-selftest, ledger, ledger-selftest, mork-rust and parity-fuzz-selftest all ok; door-order red by verdict only, no drift (`ai-tmp/ai-doors-lanes-unit5.log`, `ai-tmp/ai-doors-lanes-unit5b.log`). The eleven baseline reds are not this branch's: the same lanes are red on the integrator's cut baseline and on audits' final gate.
