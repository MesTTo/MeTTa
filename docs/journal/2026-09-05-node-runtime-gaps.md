# Node runtime and wire gaps
Goal: make partial answers share the Python wire grammar and select the wasm build for the host.
Constraint: preserve the Node matching and evaluation-status surfaces; keep fixes independently reviewable.

## 2026-09-05
Tried: the baseline Node program `!(* 2)` -> `metta_node_untaggable(partial(*,[2]))`.
Tried: `04-specialize.metta` through `loadFile` succeeds, but tracing its source raises `metta_node_untaggable(partial(+,[1]))`.
Decided: use the existing expression tag for non-list compounds, following `metta_py_encode/4` in `extensions/python/metta/shim.pl`. The functor becomes the first symbol, followed by recursively encoded arguments. Improper lists use `(cons Head Tail)`, as Python does. Keep the Node flat transport and shared variable-name map.
Rejected: a separate partial tag, because Python already gives the value a structural expression representation. Decoder reconstruction of a Prolog compound would also disagree with Python's expression decoder.
Tried: `node --test extensions/node/build/test/binding.test.js` -> 63 passed, including partial equality and round trip, nested partials, zero-arity compounds, improper lists and shared variables.
Open: finish the full corpus comparison and browser loader evidence.
Tried: `CHECK_PY=$VENV/bin/python sh extensions/python/test.sh tests/ch21_another_language_at_the_seam/test_node_binding.py` -> 4 passed. The existing live-host comparator now includes `!(* 2)` and nested partial answers.

Decided: browser boot uses the same Engine and MeTTa classes, with host-specific source loading. The browser build fetches a generated runtime manifest and mounts those sources into the wasm filesystem. Node keeps its checkout and packaged-runtime filesystem loader. A browser condition selects a prebundled browser entry; runtime detection selects the dependency's web or Node factory through static dynamic imports.
Rejected: runtime detection alone, because bundlers still resolve the Node filesystem imports in the dependency graph. A browser field alone would not rewrite the dependency's own generated CommonJS imports. The bundled entry resolves that graph once, including the upstream web loader's unreachable Node branches.
Decided: esbuild and Playwright are development dependencies only. esbuild converts the pinned CommonJS web loader and isolates Node code; Playwright tests the emitted package in Chromium. The wasm dependency and engine remain the runtime implementations.
Tried: browser boot in Chromium page and module worker -> both booted and evaluated. The first fixture expected a trace from a compile-time specialization; the recorded trace did not contain that event. Replaced it with the existing partialdef corpus fixture, which actually carries partial arguments across a trace.
Tried: a separate browser satellite build -> main and satellite held distinct Atom classes, and alphaEqual disagreed on variables created by the main entry. Decided: build all browser-capable exports together with shared chunks. Remote HTTP hosting, manifest hosting and host package discovery remain Node-only satellites.
Found: the pinned wasm loader's asynchronous .data fetch can reject without settling its factory promise. Decided: fetch both wasm assets before instantiation, name missing assets, and pass their bytes through Emscripten's wasmBinary and getPreloadedPackage options.

Tried: `npm run test:browser --prefix extensions/node` -> 8 passed: page and worker boot, preserved host callbacks and matching/status APIs, missing manifest and wasm/data assets, invalid source paths, the documented example, and consumer bundling with shared satellite identities. A malformed-path test initially expected “manifest” while the error omitted that context; the error now names the manifest consistently.
Tried: `sh tools/check.sh ruff artifact-paths` -> both gates passed after building this worktree's MORK artifact with `RUSTC_WRAPPER=`. The first prerequisite build hit a stale shared sccache temporary directory and failed before compiling the artifact.
Tried: `sh extensions/node/test.sh` -> 577 passed, one existing vocabulary parity failure: the reserved Semiring table lacks budget and amplitude. The browser changes pass typecheck and source conventions.
Tried: `sh tools/check.sh node-dist` -> passed; the emitted Node package boots through dynamic import.

Tried: the full Python suite with the staged browser sources still present -> 3,030 passed, 48 skipped, one failure: `test_no_binding_carries_its_own_verbosity_setter` found the copied engine filereader under Node `_runtime/`. Decided: the browser test owns and removes its generated runtime sources in teardown, like `postpack`, including on failure. The explicit `build:browser` command still retains the assets for serving. The Python suite and its source guard remain unchanged.

Tried: return HTTP 200 with HTML as `swipl-web.data` -> the upstream loader emitted `RuntimeError: Aborted(undefined). Build with -sASSERTIONS for more info.` and left boot pending. Decided: compare the data buffer size with the loader's `getPreloadedPackage` expected-size argument before its asynchronous filesystem setup. An incorrect deployment now rejects with `ERR_METTA_ENGINE`; a regression test covers the successful HTTP response carrying an invalid asset.

Tried: rejecting the wrong data size from `getPreloadedPackage` -> still failed the named-error regression, because the containing `loadPackage` function is itself async and called without awaiting its promise. Rejected that hook as a validation boundary. Decided: the browser build records the pinned loader's colocated data size as a compile-time constant; validate the fetched buffer before invoking any upstream factory code. No checksum or secure-context requirement is added.
Tried: the completed pre-factory validation and teardown -> 9 browser tests passed. The packed consumer booted in Node and Chromium, answering `42` and `(partial * (2))`.
Tried: the next full Python run after cleanup -> the source guard passed; two unrelated asynchronous checks failed (`len(landed)` was 0 rather than 1, and a serve subprocess returned 2 rather than 0 on SIGINT). No Python implementation or test was changed. Repeat on the rebased branch with other gates idle before attributing these failures.

Resolved: the corpus sweep covered 246 current examples. Four traces exposed `partial/2` and three exposed improper lists; all seven now complete and match Python's full wire answers and trace events after consistent variable renaming. No other untaggable shape appeared, and none of those seven has an unrelated remaining failure. `loadFile` already passed before the fix; trace serialization was the failing door.
Tried: rebased through the owner's Semiring fix onto `805318384a16c2426200e30657055d1d2f2f64fb` -> `sh extensions/node/test.sh` passed all 578 tests; `npm run test:browser --prefix extensions/node` passed all 9 tests. `sh tools/check.sh ruff artifact-paths` and `sh tools/check.sh node-dist` passed.
Tried: a clean baseline at that same commit, with matching native build artifacts -> 3,038 passed, 48 skipped, one identical serve-SIGINT failure. The Python implementation and tests have no changes on this branch. The standalone main-module file passed 17 tests; the changed tree's focused CLI/scheduler check passed 28.
Found: the suite requires GNU timeout, but the default executable is uutils 0.8.0. The installed GNU executable is available as gnutimeout. Selecting it with a worktree-local PATH entry removed the shutdown failure. Its longer command path exposed a second test-environment issue: `ps -o args` truncated the wrapper path before its basename. `COLUMNS=512` preserved the complete evidence, and both process guards plus the CLI shutdown control passed.
Tried: `PATH="$PWD/ai-tmp/ai-gnu-tools:$PATH" COLUMNS=512 HYPOTHESIS_PROFILE=ci CHECK_PY="$VENV/bin/python" sh extensions/python/test.sh` -> 3,039 passed, 48 skipped. The local tools directory contains a timeout symlink to the installed GNU binary. No Python source, test, skip rule or runtime limit was changed.
Tried: npm pack and a separate installed consumer -> Node and Chromium both booted and returned `42` and `(partial * (2))`. The archive contains 287 files, including 107 runtime sources and both wasm assets, with no scratch or native cache artifacts.
Tried: `jscpd extensions/node/src extensions/node/tools --ignore '**/node_modules/**,**/build/**,**/dist/**,**/browser/**' --exitCode 0 --noTips` -> 7 small clones, 54 duplicated lines (0.32%). The three new matches are browser-test setup and assertions; extracting them would obscure the separate failure scenarios. The runtime platform split adds no duplicate algorithm.
Resolved: browser boot and the shared wire grammar are implemented. Concurrency/deadlines, effect planning, Prolog registration, public in-memory files, native host heads, callback transactions and file-dependency root modes were classified without implementing those separate APIs. The existing blank-root fix is retained.
