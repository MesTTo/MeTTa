# The Python tool lanes after the wave

Goal: make ty, pylint, refurb, vulture and bandit agree with the Python declarations, and restore the policy inventory after source moves.
Constraint: preserve generated declarations, lazy publication, engine answers and inference counts. Changes belong to the Python seat and the three assigned engine exemption sites.

## 2026-09-11

Tried: six individual `sh check.sh <lane>` runs at 96ece1056 -> ty 30 diagnostics, pylint 184 messages, refurb 26, vulture 11, bandit 90 issues, policy inventory 33. Direct analyzer commands in the read-only c66cf0c10 control -> pylint 183, refurb 26, vulture 0, bandit 89. The prior estimate that all findings appeared during the wave does not hold in this environment.
Tried: ty on two signatures sharing one imported ParamSpec -> `Defined[module.P, module.R]` reports invalid-type-arguments, while `Defined[P, R]` binds the directly imported parameter. The tracker search found the feature overview at https://github.com/astral-sh/ty/issues/1889 but no issue specifically identifying the attribute spelling. This diagnosis rests on the local probe.
Tried: `EvaluationRecord.with_options(fuel=False, accounting=True, repeatable=True, inferences=-1)` -> the three boolean fields become false/true/true and the bound becomes none. Every member of the shutdown exception tuple subclasses BaseException. These two pylint findings are inference limitations.
Tried: the focused test runner with repository-relative paths -> exit 5, no tests ran. The runner changes into extensions/python, so the corrected command uses seat-relative paths.
Rejected: editing generated faces, because regeneration would discard the repair. Rejected: dropping ParamSpec information or replacing lazy modules with eager imports, because that changes the declared calling convention or package publication contract. Rejected: replacing audited Python expression evaluation with literal_eval, because py-atom intentionally evaluates expressions and metadata includes typed record constructors.
Decided: generators bind shared typing parameters by explicit name and reuse their existing builtin binding. Optional provider protocols retain explicit missing-import guards for each checker. Public re-export aliases remain the package declaration convention, as Pylint's C0414 documentation itself permits: https://pylint.readthedocs.io/en/v4.0.8/user_guide/messages/convention/useless-import-alias.html.
Decided: resolve stale and duplicate imports at their source; use explicit context-manager parameter contracts, preserve exception instance metadata through its declared dictionary, and make the shared empty header mapping immutable. Retain load-bearing CPython lazy publication hooks with specific typing evidence and guards.
Decided: enum partitions derive from the existing family classification. Parser spellings and abstract-interpreter transfer rules are mechanism grammar, with adjacent source-grounded exemptions. Remove orphaned exemptions when their closed list became clauses. Binding generators import their declarations explicitly; the dead-code scan includes their consumer. Generated tuple fields are dynamically read by the Prolog record and retain that stated evidence.
Decided: modernisations preserve identity tests and right-biased dictionary precedence. Bandit's shipped-test assertion paths follow the moved files; each remaining intentional operation receives a tool-specific marker with its reason. Local shadowing of a same-concept public door is a module convention, not a different binding at runtime.
Tried: the remaining control commands -> ty 30 diagnostics and policy inventory 30 findings. After repair, six separate `sh check.sh <lane>` commands for ty, pylint, refurb, vulture, bandit and policy-inventory all pass with zero findings.
Tried: the first combined verification -> mypy, refurb, vulture, bandit and policy-inventory pass; Ruff finds generated alias casing and import order, ty retains one higher-order executor diagnostic, and Pylint rejects the reason separator in new directives. Named uppercase aliases and tool-native directive locations resolve those findings.
Tried: a typed local for `_apply_chunk` still leaves ty's return parameter unbound inside `Executor.submit`; casting the callable to the enclosing return parameter passes ty and mypy, including redundant-cast checking. The related composed-ParamSpec case is https://github.com/astral-sh/ty/issues/3691. The cast preserves the callable and runtime call unchanged.
Tried: corrected focused runtime command -> 31 tests pass. jscpd over the changed door analysis and generators reports five clones, 8.6 percent, all in generated `doors/_namespaces.py`; the generator already shares those protocol projections.
Decided: cache parsed parameter declarations by their source text, matching the existing binding cache, so a changed source invalidates the result.
Tried: required consistency battery -> generated projections, their mutation tests, imports, evidence, provenance, references, closed sets and documentation pass. Mypy rejects the explanation separator in two import directives, also failing the dependent root mutation tests; the corrected separator is a second comment marker.
Tried: slotscheck -> `RuntimeError: dictionary changed size during iteration`; an instrumented traversal names `metta.aio` and new `_evaluation`, `_views`, `_worker` keys. The pre-wave control passes with 153 modules and 442 classes. Importing the mirror as a lazy module exposes it to `isclass` during namespace iteration, which executes it and publishes siblings.
Rejected: replacing aio's self binding with a deferred mirror binding, because introspection executes the latter while walking package members. Retain the package self binding with a specific Pylint directive: PEP 562 resolves exports when the annotation or connect call needs them.
Tried: repaired `ruff ty mypy pylint init-stub-selftest slotscheck` -> all pass; root mutation tests 4 passed, slotscheck 155 modules and 447 classes. Parameter discovery, source invalidation, generated object identity and aio runtime annotation probes pass. The initial identity probe incorrectly required ParamSpec in the async mirror, whose sole projected parameter is `_CastT`; the corrected probe follows each face's actual declarations.
Tried: `sh check.sh pytest` -> 5586 passed, 99 skipped, 2 failed in 413.99 seconds. The identity twin costs 2586 against provisional 2478, identical to the wave measurement. The website refusal test fails with `ERR_MODULE_NOT_FOUND` because website dependencies were not installed. `npm ci --prefix website` restores the locked dependencies; that test then passes. `sh check.sh docs binding binding-selftest ruff` passes, including 85 binding mutation tests and the actual VitePress build in 17.41 seconds.
Tried: `sh check.sh twins` -> 279 findings over 282 examples, the same finding categories and claim/storage results as the wave. Seventeen measured rows differ. Eight carry empirical budgets; two source programs contain timed concurrent work; five other point rows move by at most four inferences. The remaining two import twins move by minus eight on both source and Python sides.
Tried: temporarily restore every changed Python package source and stub to 96ece1056, measure the two import twins with `twin_coverage.py --measure --rounds 3`, restore the repaired files, repeat -> identical minima on both arms. `04-minimal_metta` is 179276/188031; `13-strategy_internals` is 416247/394692. Logs: `ai-tmp/reds-tools-twin-reversal-{base,repaired}.log`. No source, claim, storage or finding-category change is attributed to this repair; no budget was re-pinned.
Open: the host or artifact mechanism behind the two minus-eight differences from the older wave run remains unidentified; the controlled reversal excludes this repair. Provisional budgets and other workers' lanes remain assigned to their owners. The final whole-gate result is recorded in the delivery receipt.

## 2026-09-11: preserve the exception attribute protocol

Tried: compare the original assignment, dictionary write and builtin setattr over plain exceptions, writable properties, a readonly property, a custom setter and a refusing setter, with remedy and ground independently absent or present. `"$PY" ai-tmp/reds-tools-exception-contract.py --require-current` with the interpreter selected by `select-python.sh` finds 11 dictionary-write regressions; all 20 setattr cases match the original return identity, attributes, setter events and raised errors.
Rejected: the earlier dictionary-metadata decision, because it bypasses descriptors and custom __setattr__ methods. Generic metadata follows ordinary attribute assignment, including its refusals. Python documents setattr as equivalent to assignment: https://github.com/python/cpython/blob/v3.12.12/Doc/library/functions.rst.
Decided: use setattr with two local Ruff B010 markers naming the generic exception protocol. The ongoing whole gate was allowed to finish without tracked edits; a second whole gate verifies the corrected committed state. The original functional and provenance commits remain in history, followed by the correction and its provenance pin.
Tried: the corrected exception probe with `--require-current` -> 20 matches, zero differences. The focused error/refusal suite -> 164 passed in 6.60 seconds. Each assigned analyzer lane passes again; `sh check.sh ruff mypy door-sync init-stub imports slotscheck` passes, including the projection mutation tests. Logs: `ai-tmp/reds-tools-exception-{contract-after,focused}.log`, `ai-tmp/reds-tools-corrected-*.log`.

## 2026-09-11: portable verification commands

Tried: the second whole gate -> 5586 passed, 99 skipped and 2 pytest failures in 417.48 seconds. The expected identity-twin failure remains; `test_no_tracked_file_cites_an_absolute_workspace_path` also rejects the interpreter path quoted in this journal.
Rejected: retaining the machine-local interpreter citation, because every tracked file must remain portable. The historical command above now spells that interpreter through the existing selector.
Decided: use the repository's `select-python.sh`, which already chooses the interpreter for `check.sh`. The portable command below preserves all 20 exception-protocol cases, with zero differences (`ai-tmp/reds-tools-portable-probe.log`).

```sh
METTA_ROOT=$PWD
. ./select-python.sh
"$PY" ai-tmp/reds-tools-exception-contract.py --require-current
```

Tried: `sh extensions/python/test.sh tests/repository/test_workspace_paths.py` after the citation repair -> 1 passed. Log: `ai-tmp/reds-tools-workspace-portability.log`. The final whole gate is rerun on the documentation-corrected tip; no implementation changes follow the exception-protocol correction.
