# The many-repo split, and the branches it has to absorb first
Goal: the tree is published as one repo per component, engine, lib, the four seats, the two example sets and the integration distributions, held together for work as one monorepo of submodules; and no committed work is stranded by the split.
Constraint: a split that runs before an unmerged branch lands strands that branch's work in a repo nobody will re-split. Three branches are outstanding against `petta` (feat/a-standard-library-for-a-language, 108 commits; fix/audit-acceptance-tests-and-repairs, 40; feat/door-bodies-compose-the-primitive-doors, 16), so they merge first. Each component's history is preserved rather than restarted, because this tree's evidence is its history: every pin cites the commit that measured it.
Plan:
1. Merge the three outstanding branches into `petta`, oldest first, each gated before the next. Established by the gate.
2. Split each component with its own history and rewrite the monorepo to carry them as submodules. Established by comparing the split repo's tree against the monorepo's subtree at the same commit.
3. The two example sets: the .metta corpus is one repo, and the Python twins are a second whose chapters sit at its root and which carries the corpus as a submodule (user, 2026-09-19).

 ## 2026-09-19
Found: the standard-library branch conflicts on 52 files, of which 31 are twins whose hunks hold two divergent cost chains from one ancestor: both branches re-pinned the same twins, the standard-library line tripling several of them by deriving their work in MeTTa (regex_lib 28,947 to 108,199 against the classes line's 35,787). Neither number is the merged tree's.
Decided: the twin conflicts resolve mechanically (ai-tmp/ai_resolve_twin_conflicts.py): every paragraph of both chains is kept, each line contiguous rather than date-interleaved because their numbers do not follow each other, the other branch's line first and this tree's last, and this tree's BUDGET stands as the placeholder the corpus re-pin replaces. The 21 remaining conflicts are resolved by hand.
Landed: the three branches merged into the classes line, 97763e7fa (standard library, 108 commits, 52 conflicts), 1d73acbbe (audit repairs, 40 commits, 3 conflicts), 4910afc33 (door bodies, 16 commits, 2 conflicts). Every conflict's choice is recorded per hunk in ai-tmp/ai-merge-plan-*.json and the two that were not unions are argued in 97763e7fa's and 1d73acbbe's messages.
Rejected: merging fix/stdlib-completeness-rows, 3 commits ahead and 1442 behind. Its own last commit records that petta closed the same three rows in parallel (a0580a1b) and names what each side has, so it is a reconciliation record rather than a branch to land. Revisit if its four additive pieces are wanted: the CSV record number, the materialised csv-space! beside the streaming door, the standard streams as handles, and temp-dir!.
Decided (the components and their sources), from the user's answers of 2026-09-19:
  engine <- engine/; lib <- lib/; MeTTa-Examples <- examples/;
  PyMeTTa-Examples <- extensions/python/examples/language-feature-examples (unpacked, chapters at its root) carrying MeTTa-Examples as a submodule at examples/;
  pymetta <- extensions/python; cmetta <- extensions/cmetta; metta-node <- extensions/node; metta-mork <- extensions/mork;
  metta-extensions <- extensions/python/ext, the seventeen integration distributions the core may never name.
Decided: each component is split with `git subtree split`, so its own commits and paths survive rather than a fresh initial commit. This tree's evidence is its history: every pin cites the commit that measured it, and a split that discards that makes every citation unresolvable.
Open: metta-extensions is a submodule of pymetta rather than of the monorepo, because git cannot track a path inside another submodule and the seat's own benchmarks import metta_benchmarking from it. The alternative, pymetta carrying ext/ directly and metta-extensions existing only for publication, gives the same files two homes and is rejected for that reason.
Found (the split exposing a coupling, first of two): `extensions/python/ext/` held the integration distributions the core may never name, discovered through the `metta.extensions` entry-point group, and also `metta-benchmarking`, the measurement harness the node and mork seats' benchmarks import. Living under one seat contradicts both halves: that seat must work without the integrations, which is the whole demonstration, and the other seats already depend on what sits there. Making metta-extensions a submodule of pymetta would have inverted the demonstration into a dependency (user, 2026-09-19: "the whole point of it is to show how you can decouple from pymetta ... if that's unable to do that, that's a flaw").
Decided: they move to a top-level `ext/`, a sibling of `extensions/`, so metta-extensions is a sibling repo and pymetta stands without it. Sixty-one live files take the new path; the dated journal entries keep the old one, because an entry is true to its date.
Found (the second coupling): the only symlink that crosses a component boundary is `extensions/python/pyproject.toml -> ../../pyproject.toml`. The Python seat is published as the `pymetta` package and needs a pyproject at its root, and the monorepo needs one for the workspace and the tool configuration; today they are one file with the seat linking to it, so a split pymetta would carry a dangling link.
Open: which way that link should point after the split. Inverting it, the real file in the seat and the monorepo root linking into the submodule, keeps one authoritative file and gives the split repo the real one, but whether uv resolves `members = ["ext/metta-*"]` against the link or its target is not something to guess; it is measured before the split lands.
Found: the `ext/` move cost more than the paths it rewrote. `extensions/python/_workspace.py` locates the distributions as `Path(__file__).resolve().parent / "ext"`, a level count rather than a spelling, so the rewrite that fixed every literal `extensions/python/ext` could not see it. It does not raise when wrong: the glob matches nothing, `members()` answers `[]`, nothing reaches `sys.path`, and the six distributions whose tests import their own module fail with `ModuleNotFoundError` while every lane stays green. Measured: 22 collection errors under `ext/`, against 238 tests collected on the pre-move tree with the same interpreter. `tools/doorgen.py` and `tools/reference.py` carried the same shape and were repaired with it.
Decided: `check_layering.py` compares the helper against the roster (`_the_checkout_finder_sees_the_roster`), so a level count that stops reaching the members fails the lane that already owns the roster instead of surfacing as an import error in a member's suite. It names `EXT` and the directories it misses. Both directions are reported, because a helper reaching a directory the glob does not name is the same defect seen from the other side. The selftest plants the real helper and rewrites `parents[2]` to `parent` to prove the rule fires; it reads the live file rather than a fixture copy, so the number stays free to change.
Found: `test_gate_completeness.py` runs ruff itself over `RUFF_SCOPES` to price the suppression burn-down, so the scopes are where code lives rather than which driver runs it. `ext` moves from the seat's scope to the repository's, beside `tests/checks`; it is the same ruff project root either way, because `ext/` carries no configuration of its own.
Found (gating the merged tree for the split, which is the first full pytest run since the three merges landed): the absolute-path lane read a URI as a Windows path. Its drive-letter pattern was `[A-Za-z]:[\\/](?:Users|home|a)[\\/]`, and in `"foo:/a//b"` the scheme's last letter reads as the drive, so the uri library's own fixtures were reported as citations of somebody's home directory, in the .metta corpus, its Python twin and the Prolog suite at once. A drive is exactly one letter, which a lookbehind states; without it the rule cannot tell a scheme from a drive. The three genuine citations, all commands recorded as evidence in dated entries, are respelled with `~`, which the shell they were run in expands, so the record stays runnable and names no user's directory.

Decided: the symlink inverts. The real `pyproject.toml` is the seat's and the monorepo root links to it, because `[project]` is pymetta's own distribution metadata and the root never had one of its own; it borrowed the seat's. The old direction would have given the split pymetta a dangling link and no manifest at all. Measured on the merged tree: `uv lock --offline --check` resolves 144 packages with the link at the root, so uv reads `members = ["ext/metta-*"]` against the link's own directory rather than its target; and a copy of the seat manifest alone does not make uv refuse a workspace glob matching nothing, so the table travels into a standalone pymetta inert rather than fatal. The root and the seat stay byte-identical, which is what the ruff lanes were measured to need on 2026-09-04.
Found: `git subtree split` does not follow a rename, so splitting `metta-extensions` at the new prefix keeps 3 commits where 23 touched the distributions. `git subtree split -P ext` walks the history and takes the subtree at that prefix, and a commit from before the move has nothing there, so it is dropped: the split repo would begin at the move itself and every pin citing an earlier commit would be unresolvable, which the plan above names as the reason for splitting with history at all. `--onto` does not recover it either, because the first half must be produced from a checkout where the OLD prefix exists: `git subtree split` reads the prefix from the working tree rather than from the commit it is given, and refuses with `does not exist; use 'git subtree add'` otherwise.
Decided: the distributions are split in two halves and grafted. The old prefix is split from a worktree at the move's parent, and the new prefix is split `--onto` that branch, so the result is one history across the rename. The other eight components have no rename and split in one pass.
Found (door-order, blocking the gate rather than the split): `tests/repository/test_door_rows.py` passes in full at 1d73acbbe and does not finish at 4910afc33, the door-bodies merge. Registering a door the generated verdict table does not name sends `orders()` to `live()`, which re-analyses every core source, so a test that registers 60 synthetic doors pays 60 whole-program analyses. The branch's own entry records this file at 261.75s with the analysis at 17.8s, so it was already at the edge of the per-test budget; the merged tree's 171 modules against the branch's 158 push one test past the 180s faulthandler timeout and the xdist worker dies with it.
Measured: `tools/doororder.py` takes 14.9s at the branch tip 14f5c43b5 and does not finish in 2m44s on the merged tree. One `CallGraph.solve()` over the shipped rows costs 7.33s from ONE entry point and exceeds 90s from two, which is not smooth growth in the entry count. Every sample of the stalled run lands on the same stack, `_protocol` under `_container_call` under `_call` under three frames of `_value`, and the final pass injects an unknown value at each entry's unbound parameters, so each additional entry floods the fixed point with another source of unknowns.
Found: the rename problem is not the distributions', it is nearly every component's. `engine/` was `src/` until 2026-08-21, `extensions/python` was `bindings/python` was `hosts/python` was `python` was `src`, the three other seats came out of `bindings/` and `backends/`, and the twins were `tests/twins`. A subtree split at the current prefix would have begun the engine's history on 2026-08-21 and thrown away the year before it.
Decided: components are split with `git filter-repo` over every path their files have lived at, and those paths are DERIVED rather than listed (ai-tmp/ai_component_paths.py). Git records a rename per FILE, so the historical directories come from asking, for the commit that first added anything under a prefix, which files it renamed into that prefix and where each came from, then repeating. The walk stops at an ancestor that STILL EXISTS, because a directory both sides share is a live sibling rather than a predecessor: `lib/` took files out of `examples/` while `examples/` carried on, and following that edge would hand lib the whole corpus's history. An ancestor that is gone is a real predecessor and both descendants keep it, which is why `src/` is in the engine's history and the seat's alike: before the split it genuinely was both.
Measured (every tree checked against the monorepo's subtree at the same commit): metta-engine 1832 commits from 2025-08-28, pymetta 3306 from 2025-08-28, metta-examples 764 from 2025-08-28, pymetta-examples 591 from 2026-08-22, metta-lib 522 from 2025-10-30, metta-node 269, cmetta 166, metta-mork 97, metta-extensions 25. The subtree split gave metta-mork 71 and metta-extensions 3 for the same components.
Decided: the commit ids in obligation headers resolve against the MONOREPO, which keeps its whole history, not against the split repositories; both `git subtree split` and `git filter-repo` write new commit ids, so no split could preserve them. What the component repositories carry is a complete history of their own files for a reader who clones one of them, which is why the renames are worth following even though the pins do not depend on it.
Decided: the twins mount the corpus and the seat mounts the twins, rather than the twins being a publication-only copy, for the reason the ext/ move was made: the same files in two repositories have two homes and drift. Verified by expanding every gitlink into the repository it names (ai-tmp/ai_expanded_tree.py): pymetta's expanded tree carries all 1191 blobs the monorepo holds under `extensions/python`, none missing, and the 422 extra are the two .gitmodules files and the 420-file corpus the twins pull.
Open: mounting turns the monorepo into a superproject, so `git ls-files` stops reaching inside a component. Eleven files ask git for the tracked set, the absolute-path lane and the conflict-marker, qlf-freshness, host-workarounds and tokenisation-parity lanes among them, and each needs `--recurse-submodules` or it silently scans less. The mount lands after that, because a lane that quietly checks a third of the tree is worse than one that fails.
Measured (door-order, the merged tree against the door-bodies branch tip 14f5c43b5): `tools/doororder.py` takes 14.9s there and does not finish in 1500s here, so the regression is at least a hundredfold. The two trees differ by thirteen modules the classes line added, and dropping all thirteen brings the whole analysis back to 14.67s over 210 entry points. No single one of the thirteen causes it: each was run alone against the other twelve dropped and every one came back between 14.46s and 16.76s. It is the combination.
Measured (where the cost is, `_put` instrumented to count growth and reader reschedules): the configuration that finishes touches 70,929 slots with 126,556 growth events and 70,456 reader reschedules; the one that does not touches 102,603 slots with 170,872 growth events and 370,189 reschedules IN ITS FIRST 120 SECONDS alone. Slots grow 1.45x and reschedules 5.25x, so the cost is not the number of slots but READERS PER GROWTH: `metta._atoms.model.encode`'s return slot reaches 3,104 references, the generator container at `_atoms/model.py:1433` that consumes it reaches 2,207 items over 465 separate growths, and 427 scopes read it, so that one slot accounts for most of the reschedules. The store is delta-driven already (`_put` computes `values - previous` and only reschedules on growth), so the quadratic is growth-steps times readers, not re-propagation of whole sets.
Rejected: a depth-first worklist (`self.queue.pop()` for `popleft()`), which is free to try because chaotic iteration reaches the same least fixed point under any fair order. It touched fewer slots, 71,488, and still did not finish in 300s. Revisit only with a real ordering strategy rather than a reversal.
Rejected for now: widening the oversized value sets to a single unknown reference, the standard Cousot remedy for exactly this shape. It bounds the cost but makes verdicts more conservative, so `_orders.py` changes, and this repository's rule is that a sound-but-incomplete change needs a differential proving the gate safe before it is the default. Revisit with that differential.
Open: the precision-preserving fix is the ITERATION ORDER, stabilising each strongly connected component of the call graph before its successors (Bourdoncle's weak topological ordering, `10.1007/BFb0039704`), which reaches the same fixed point with far fewer re-evaluations. The obstacle is that this call graph is discovered DURING the analysis rather than known before it, so the order has to be maintained as edges appear. That is the next thing to build, and it is a change to `solve()` rather than to any of the thirteen modules.
Found: `host-workarounds` is red with the same five findings at 4910afc33, before any of this work, so it is the trunk's rather than the split's. Three are `library(archive)` missing from the patched SWI build, one is `swi-ugraphs-append2` answering `absent`, which is the lane correctly asking for a workaround to be lifted. The lane also needs the patched host on PATH: `select-python.sh` puts the venv's bin there, so it must be run through the gate driver and not directly.
Decided (naming, user 2026-09-19): the engine's repository is `MeTTa-Kernel`, which is a name this project already carries rather than one invented here: it is the monorepo's own GitHub remote and the name `KERNEL.md` documents. The corpus and the twins take the spellings from the component list, `MeTTa-Examples` and `PyMeTTa-Examples`. Paths inside the tree do not move; only the repositories are named. Open for the user: the superproject and the engine component now want the same name, since `MeTTa-Kernel` is what the umbrella repository is called on GitHub today.
Landed: the mount, b88bfb4ce and after. Eight components are gitlinks in the superproject, which tracks 1209 files against the 3796 `git ls-files --recurse-submodules` reaches. Every blob was matched by content against its component's repository, with nested gitlinks expanded, before anything was written.
Found: populating by `git submodule update` would have deleted what it does not restore. The mork seat's directory is 604 MB and the Python seat's 72 MB, almost all of it ignored build output that no clone brings back. The files git tracks were already byte-identical to each component's tip, so the repository is built AROUND them instead (ai-tmp/ai_populate_submodules.py): init, fetch, point HEAD at the tip, and `reset --mixed` to read the index from it without touching the working tree. The same pass makes a battery worktree usable, cloning only where a directory is genuinely empty, and it kept battery-4's warm `atom_index.qlf` and `.so`.
Found: the root `.gitignore` says it holds repository-wide rules only and that a component's build products are ignored beside the thing they describe, and Python bytecode was the one place that was not true: it sat at the root and reached all nine components through one file. A gitlink stops it, so an untouched checkout reported `__pycache__` as untracked content. Each component carries the two rules now, the seat and the distributions carry the setuptools scratch they actually produce, and the seat drops `ext/*/build/` and `ext/*/*.egg-info/`, which named a directory that had left it.
Found: a tool's scope does not know where another repository begins. The twins mount the corpus at `examples/`, which put that component's Python fixtures under a path the seat's ruff lane walks, and the lane reported four findings in files the seat does not own. Excluded, on the same ground that the corpus at the repository root is in no ruff scope here. This is the shape to watch rather than one lane's bug: every tool with a path scope, and every enumeration of tracked files, has an invariant about component boundaries that is currently held BY HAND. The source of truth exists and is machine-readable, `.gitmodules` read recursively, so the fix is the house pattern used for the workspace roster: derive the boundaries and let a lane refuse a scope that crosses one, rather than remembering to exclude each.
Refuted (the ordering hypothesis, and this is the measurement that kills it): counting `CallGraph._evaluate` per scope, the configuration that FINISHES does 20,843 evaluations over 2,358 scopes in 13.80s, a mean of 8.8 each and a worst of 70. The configuration that does not finish does 13,557 evaluations over 2,532 scopes in 200 SECONDS, a mean of 5.4 and a worst of 26. It re-evaluates LESS, not more, so a better iteration order has nothing to remove: about 0.66ms per evaluation becomes about 14.8ms, twenty-two times dearer EACH. Bourdoncle ordering, and the depth-first worklist already rejected, both reduce the count of evaluations, which is not the quantity that grew.
Decided (the cost equation, so the next attempt aims at the right term): the analysis costs E x S, where E is scope evaluations and S is the work inside one, which is proportional to the SUM OF THE VALUE-SET SIZES at the sites that evaluation touches. E barely moved and S is what exploded, which is consistent with the store instrumentation: `encode`'s return slot holds 3,104 references and every `frozenset` union, difference and membership over it is linear in that size. The target class is an S independent of set size.
Open (the fix, with its prior art): points-to analysis met exactly this and answered it with REPRESENTATION rather than precision, because sets of thousands of abstract references are its normal case. Binary decision diagrams are the published route (Berndl, Lhotak, Qian, Hendren and Umanee, PLDI 2003, `10.1145/781131.781144`; Whaley and Lam's bddbddb, PLDI 2004, `10.1145/996841.996859`), and sparse bit vectors are the pragmatic one LLVM uses. In Python the cheap form of the same idea is to intern each `Reference` to an integer and carry `Values` as an int bitmask, where union, difference and intersection are single machine-word-parallel operations at any size. It is semantics-preserving, so `_orders.py` must come out byte-identical, which is the differential that would prove it. It is also a representation change through the whole of `_analysis.py`, which is why it is named here rather than attempted between two other pieces of work.
Found (profiling by SELF time, which is what named the real cost after the evaluation count refuted ordering): `_protocol` held 61.9s of 150s, and one generator expression inside it ran 466,823,472 times against 494,204,810 `Reference.__hash__` calls. It is the `__iter__` expansion: for every reference in the answered set, on every call, it rebuilt `self._get((name, "<items>")) | self._get((name, "<unknown-items>"))` and iterated the result. `_assign`'s destructuring branch rebuilt the same union per element per reference, 133 million visits and 16.7s.
Decided: both go through `_memo`, the mechanism this class already has for a pure store lookup. Its bookkeeping is what makes that safe rather than my care: `_get` appends every slot it reads to the collector `_memo` pushed, `_memo` stores those reads with the value and registers the key against each slot, `_put` evicts every memo keyed against a slot that grows, and a result that read a NARROWED slot is not cached at all. The union is a pure function of the two slots, so eviction on their growth is the whole condition.
Measured (cProfile, same 150s window, merged tree): `_protocol` self time 61.9s to 11.7s, `Reference.__hash__` calls 515M to 158M, `_assign` and its 133M-call expression both out of the profile, and the same volume of store writes reached in 120s where it had taken 150s. `Reference` already caches its own hash in `__post_init__`, so the hashing that remains is attribute reads rather than tuple hashing: there is nothing left to win there.
Rejected: caching the classification of a value set into generators, concrete references and builtin instances. It is pure and a frozenset cannot go stale, so it is safe, but the profile was UNCHANGED by it, and machinery that buys nothing measurable is longer than the three passes it replaces.
Open: after both memos the dominant cost is `_put`, 24.8s over 10.6M calls, because `previous | added` copies the whole slot on every growth. That is the set-algebra cost the points-to literature answers with a bit representation, and the module has 149 sites that treat a value set as a set (68 `frozenset(...)`, 40 `frozenset()`, 36 unions, 5 differences over 1812 lines), which is the size of that change. Whether the two memos are SUFFICIENT is a separate question from whether they are correct: the same code analysed the branch tip in 14.9s without them, so they remove a pre-existing inefficiency that the merged tree exposed rather than the thing that changed.
Plan (the memo work, written before it was applied): the claim is an EQUIVALENCE, so it needs both directions. Forward, no verdict may change: `tools/doororder.py` in check mode regenerates all 227 shipped verdicts from the analysis and diffs them against the committed table, so exit 0 is the differential. Backward, nothing may be dropped: the door-rows suite registers synthetic doors whose verdicts are outside that table and asserts their behaviour. The residual risk is a verdict that is neither shipped nor exercised by that suite, which nothing here covers and which is stated rather than closed.
Found (the first lane the mount broke, and it broke by noticing): `tests/shell/test_worktree_configuration.sh` exists to show that a worktree runs one backend fewer until `worktree.sh` provisions it, and it refused with "a fresh worktree already reports the backend as ''; this test can no longer show the difference it exists for". The probe answered NOTHING rather than `absent`, because `git worktree add` on a superproject leaves one empty directory per component and there was no `engine/main.pl` to consult. The test's setup was broken, not its subject.
Measured (two facts about git that decided the shape of the fix): `git submodule update --init` refuses a local path with "transport 'file' not allowed", which is CVE-2022-39253 and wants `protocol.file.allow`; and it refuses a directory that already holds the files with "destination path already exists and is not an empty directory". The second is the one that matters here, because the populated case is every checkout that predates the mount, the main one included, and the untracked build output under the components is 676 MB that no clone brings back (604 MB of Rust target/ under the MORK seat, 72 MB under the Python one).
Decided: `components.sh`, one script for both cases. Empty, it clones; full, it builds the repository around the files and reads the index from the pinned commit with `reset --mixed`, so matching files come out clean and differing ones are REPORTED as modified rather than silently replaced. It recurses for the twins' corpus, and it needs no transport flag at all, because it clones and fetches directly rather than through the submodule machinery the CVE restricted. `worktree.sh` calls it first, the worktree test calls it before its first probe, and the population lane's remedy names it instead of the git command that refuses the common case.
Found (a defect in that script, worth recording because the shape recurs): the recursion reassigned the directory the caller's loop was walking, since this shell has no scoping, and every component after the first was looked up under the wrong tree and reported "declared and not mounted". The fix is a SUBSHELL around the recursive call, which makes the clobbering impossible rather than guarded against.
Open (the untested branch of components.sh): a component whose files differ from the pinned commit is designed to come out reported as modified rather than corrected, and that path has not been exercised; every tree it has run against matched.
Measured (the cost the memo removes, in the named quantities): each of the three sites cost Theta(sum over references of (|slot| + |unknown-items|)) REBUILT on every evaluation, so over E evaluations with R references per call and S the mean slot size the analysis paid Theta(E x R x S) for a value that changes only when a slot grows. Memoised it is paid once per (container, slot) until one of the two grows, which is G times rather than E, and what remains per call is the update itself. The generator expression that carried it leaves the profile entirely and `Reference.__hash__` falls from 515M calls to 158M.
Found (a defect in the differential, not in the code, and the more expensive of the two): the first comparison dumped `repr(Order)` and reported 137 of 227 orders changed with IDENTICAL repr lengths, which is what a frozenset whose iteration order moved looks like; str hashing varies between runs. The canonical dump, every set written as its sorted contents, shows all 227 identical. Worse, the first canonical attempt raised on `Order._fields` -- it is a dataclass, not a NamedTuple -- wrote two EMPTY files, and `cmp` duly reported them identical. A verdict from a run whose output was never checked for existence is not a verdict; both dumps are now counted before they are compared.
Open: that differential covers the module set that COMPLETES. The full set, where the analysis still does not finish, is not covered by it, and a narrowing interaction that only occurs among the thirteen dropped modules would not be caught. `_memo` refuses to cache a result that read a narrowed slot, which is the mechanism that would have to fail.
Measured (the memo is NOT sufficient, which answers the question the record was opened for): with `_stored` in place, `tools/doororder.py` over the full module set had still not finished at 25 minutes, the same wall the unmemoised baseline hit, against 14.9s on the branch tip. So c5 is answered in the direction it predicted: the memo removes a pre-existing inefficiency the merged tree exposed and moves the threshold; it does not remove the superlinearity. The analysis remains too slow for the door-order lane and for `tests/repository/test_door_rows.py`, whose slowest case exceeds the 180s per-test timeout.
Open (what the evidence now points at, with the two cheap directions refuted): iteration order, a depth-first worklist and the bitmask representation are each refuted by measurement and recorded above. What is left is PRECISION -- the analysis carries a 3,104-reference set for one function's return value, and every consumer iterates it -- which means making the analysis context-sensitive enough that `encode`'s return is not the union over all of its callers. That changes verdicts, so it needs the differential this thread built, run at full size, before it could be a default.
Found (THE root cause of the door-order regression, and it is one line): `_external_call` returned `Reference("unknown", "<site>: external value <external name>")` for an unresolved member on an external value. The abstract value carried the NAME as part of its identity, so two values meaning the same thing, "something unknown from this site", were distinct whenever different external names reached the site. The unknowns at one site became that site CROSSED WITH every external name that ever reached it, and they flow into every set the value reaches, where every union, difference and membership test is linear in the set. Measured: one slot held 3,627 unknowns, 3,619 of them `Expression.__init__:1433`'s, differing only in the name in the middle (`external value builtins.list.extend.* (parts)` and 3,618 others). That is why thirteen more modules cost a hundredfold rather than eight per cent: the second factor of the cross-product grows with the program.
Decided: the value carries the SITE and `open` keeps the name. The name was never needed in the value -- the published `Verdict` reads `bool(self.open)` and never its contents, and the detailed reason with the name is added to `open` on the line above, where it is read. The general shape is worth naming because it recurs: provenance belongs beside the result, not inside the value. Points-to analysis calls the other way an unbounded abstract domain, type inference meets it as fresh-variable blow-up, and Souffle makes provenance an opt-in mode for exactly this reason.
Measured (the fix): stored references 10,780,534 -> 3,005,161 in the same 200s window, the biggest value set 5,158 -> 1,801, the unknowns in it 3,627 -> 81 which is one per site as intended, and `tools/doororder.py` COMPLETES in 460.7s where it had not finished in 1500s. Equivalence: all 227 published verdicts identical over the module set that completes, and `tests/repository/test_door_order.py` passes whole, 123 tests, including the three that assert the text of an open reason.
Found (what the completed run says, which nothing could say before): the committed verdict table is STALE. That is the merged tree's own drift rather than this change, which alters no verdict, and it was invisible while the generator could not finish.
Open: the scaling is still wrong. 158 modules analyse in 15.6s and 171 in 460.7s, thirty times the cost for eight per cent more program, so a second growing term remains. The biggest value set is now 805 `container_method` and 440 `container` references, which are keyed by allocation SITE and therefore grow linearly with the program; a set that grows linearly, operated on linearly, is the quadratic that is left.
Found (two defects in components.sh, both from USING it rather than reading it): checking out the mount commit in a battery that held the flat tree makes git remove every tracked file under a path that is now a gitlink, and `reset --mixed` does not bring them back, so the battery's seat came out with 866 deleted files and no `tools/` directory at all -- a component that cannot be used. It checks the tracked files back out now, and refuses rather than overwrites a MODIFIED one, because that is somebody's work. The guard then refused on the seat naming one modified path, `examples/language-feature-examples`, which is a GITLINK at a different commit and not a file: a nested component off its pin is exactly what the recursion is about to set, so gitlinks are excluded by reading the raw format's destination mode. This is the untested branch this thread recorded as open, exercised; both are fixed and the worktree lane still passes.
Found: no other reference in the analyser embeds a growing name. The only two constructions with a computed identity are `supplied parameter {scope}.{arg}`, bounded by the program's parameters, and the class or instance of a scope's owner, bounded by its classes. The cross-product was the one site.
Found (what the first completed regeneration says, and it is worse than staleness): 31 of 227 verdicts change, and every one collapses to the SAME maximally degraded value. `answers:arrow` was `Verdict(1)`, `answers:group-by` was `Verdict(2)`, `answers:build` and `answers:into` were clean dependencies; all of them now read `Verdict(None, mixed=True, open=True, recursive=True, dependency=True)`. Thirty-one doors becoming identical and maximally degraded is the signature of the analysis losing its grip rather than of the code changing.
Decided (that it is not this thread's change, on a strict argument rather than a measurement, because the pre-collapse baseline is the run that does not finish): coarsening an unknown's NAME cannot make `open` non-empty where it was empty, because one or more unknowns reaching a call becomes exactly one and never none; and `cycles` are derived in `derive` from a graph built out of door-to-door calls, which unknown references never enter. Both fields are therefore unchanged by construction. What the completed run shows is the MERGED TREE's own analysis, which nothing had ever been able to compute.
Measured (the same three doors on each tree, which says the degradation is REACHABILITY and not a precision wobble): at the branch tip 14f5c43b5, `answers:arrow` has calls 1, native 0, open 0, cycles 0 and reads `Verdict(1)`, and the one call is `rows:arrow`, its own counterpart; `answers:build` calls `rows:build` and nothing else. On the merged tree the same door has calls 19, native 30, open 12,527 and 3 cycles, one of them an SCC of 152 scopes in `metta._atoms.factories` (`_map_atoms`, `_mapped_candidate`, `_read_one`, `_resolve_binding`, `_to_atom`, `parse`). A door that delegated to its counterpart now reaches the parser.
Found (and it corrects an assumption this thread nearly acted on): collapsing the undeclared-member VALUE cannot restore those verdicts. The 12,527 entries come from `self.open.add(self._where(node, reason))` on the line ABOVE the value, which is untouched: the value's identity is what multiplies inside sets, the open entry is what the verdict reads. The two are separate effects of one site and only the first is a domain problem. Measured: stored references 3,005,161 -> 2,921,081, three per cent, while the open sets are unchanged by it.
Measured (the scaling law, in the named quantities): branch tip 69,326 slots holding 546,774 references, mean 7.9 per slot, 13.74s; merged tree 104,329 slots holding over 3,005,161, mean 28.8, 460.7s. Slots grow 1.5x and mean set size 3.6x, and the time follows their product rather than either alone: 5.5 x 3.6 is 20x against 33x observed. So the term to attack is MEAN SET SIZE, and the composition of the biggest set says where it went: `opaque` 15 -> 142 and `external` 24 -> 129 are the disproportionate ones, and both are bounded by the external API surface the classes modules genuinely use (`inspect`, `dataclasses`, `typing`), not by a cross-product.
Found (the established fix for the term that is left, and it matches the measurement rather than being reached for): collapsing strongly connected components. Every pointer in an SCC of an inclusion-constraint graph provably converges to the SAME set, so one representative can stand for all of them, and the literature is emphatic that this is THE scaling requirement: Fahndrich, Foster, Su and Aiken measure that "the bulk of work and execution time is attributable to strongly connected components in the constraint graph" and that "without cycles, the points-to analysis scales very well" (Partial Online Cycle Elimination in Inclusion Constraint Graphs, PLDI 1998, `10.1145/277650.277667`); Pearce, Kelly and Hankin give online detection by keeping the graph topologically sorted so a check is needed only when a new edge violates the order (SCAM 2003); Hardekopf and Lin add lazy and hybrid cycle detection, triggering only when identical sets appear (PLDI 2007, `10.1145/1250734.1250767`). The trigger here is measured rather than assumed: the degraded doors sit on an SCC of 152 scopes in `metta._atoms.factories`, and the cost model is stored references times mean set size, which collapsing an SCC divides by the size of that SCC.
Open: that is a change to how the store is keyed, not to a call site, so it is named here with its citations rather than attempted between two other pieces of work. It is also the one remaining direction the evidence supports: ordering, a depth-first worklist and a bit representation are each refuted by measurement above, and the two cross-products that WERE artefacts are fixed.
Decided (a distinction this thread had been blurring, corrected by the user 2026-09-19, "with SCC we don't prevent the dev to create an infinite while loop"): there are TWO cycles here and only one is a cost artefact. A cycle in the VALUE-FLOW graph means several slots provably converge to the same set, so collapsing it is a change of representation and nothing else. A cycle in the CALL graph is what `Order.cycles` publishes as `recursive=True`, a real property of mutually recursive bodies, and must survive any such collapse. Neither is a termination check: the analysis never claimed to stop a developer writing a loop that does not end, and collapsing constraint cycles cannot make it miss one. The 152-scope SCC measured above is the SECOND kind, so it is a REPORTED fact rather than the thing to optimise away, and the cost work belongs on the value-flow graph, which this analyser does not materialise as an inclusion graph.
