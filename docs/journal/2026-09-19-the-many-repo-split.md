# The many-repo split, and the branches it has to absorb first
Goal: the tree is published as one repo per component, engine, lib, the four seats, the two example sets and the integration distributions, held together for work as one monorepo of submodules; and no committed work is stranded by the split.
Constraint: a split that runs before an unmerged branch lands strands that branch's work in a repo nobody will re-split. Three branches are outstanding against `petta` (feat/a-standard-library-for-a-language, 108 commits; fix/audit-acceptance-tests-and-repairs, 40; feat/door-bodies-compose-the-primitive-doors, 16), so they merge first. Each component's history is preserved rather than restarted, because this tree's evidence is its history: every pin cites the commit that measured it.
Plan:
1. Merge the three outstanding branches into `petta`, oldest first, each gated before the next. Established by the gate.
2. Split each component with its own history and rewrite the monorepo to carry them as submodules. Established by comparing the split repo's tree against the monorepo's subtree at the same commit.
3. The two example sets: the .metta corpus is one repo, and the Python twins are a second whose chapters sit at its root and which carries the corpus as a submodule (user, 2026-09-19).

 ## 2026-09-19
Found: the standard-library branch conflicts on 52 files, of which 31 are twins whose hunks hold two divergent cost chains from one ancestor: both branches re-pinned the same twins, the standard-library line tripling several of them by deriving their work in MeTTa (regex_lib 28,947 to 108,199 against the classes line's 35,787). Neither number is the merged tree's.
Decided: the twin conflicts resolve mechanically (ai-tmp/ai_resolve_twin_conflicts.py): every paragraph of both chains is kept, each line contiguous rather than date-interleaved because their numbers do not follow each other, the other branch's line first and this tree's last, and this tree's BUDGET stands as the placeholder the corpus re-pin replaces. The 21 remaining conflicts are resolved by hand.
Landed: the three branches merged into the classes line, 97763e7fa (standard library, 108 commits, 52 conflicts), 1d73acbbe (audit repairs, 40 commits, 3 conflicts), 4910afc33 (door bodies, 16 commits, 2 conflicts). Every conflict's choice is recorded per hunk in ai-tmp/ai-merge-plan-*.json and the two that were not unions are argued in 97763e7fa's and 1d73acbbe's messages.
Rejected: merging fix/stdlib-completeness-rows, 3 commits ahead and 1442 behind. Its own last commit records that petta closed the same three rows in parallel (a0580a1b) and names what each side has, so it is a reconciliation record rather than a branch to land. Revisit if its four additive pieces are wanted: the CSV record number, the materialised csv-space! beside the streaming door, the standard streams as handles, and temp-dir!.
Decided (the components and their sources), from the user's answers of 2026-09-19:
  engine <- engine/; lib <- lib/; MeTTa-Examples <- examples/;
  PyMeTTa-Examples <- extensions/python/examples/language-feature-examples (unpacked, chapters at its root) carrying MeTTa-Examples as a submodule at examples/;
  pymetta <- extensions/python; cmetta <- extensions/cmetta; metta-node <- extensions/node; metta-mork <- extensions/mork;
  metta-extensions <- extensions/python/ext, the seventeen integration distributions the core may never name.
Decided: each component is split with `git subtree split`, so its own commits and paths survive rather than a fresh initial commit. This tree's evidence is its history: every pin cites the commit that measured it, and a split that discards that makes every citation unresolvable.
Open: metta-extensions is a submodule of pymetta rather than of the monorepo, because git cannot track a path inside another submodule and the seat's own benchmarks import metta_benchmarking from it. The alternative, pymetta carrying ext/ directly and metta-extensions existing only for publication, gives the same files two homes and is rejected for that reason.
Found (the split exposing a coupling, first of two): `extensions/python/ext/` held the integration distributions the core may never name, discovered through the `metta.extensions` entry-point group, and also `metta-benchmarking`, the measurement harness the node and mork seats' benchmarks import. Living under one seat contradicts both halves: that seat must work without the integrations, which is the whole demonstration, and the other seats already depend on what sits there. Making metta-extensions a submodule of pymetta would have inverted the demonstration into a dependency (user, 2026-09-19: "the whole point of it is to show how you can decouple from pymetta ... if that's unable to do that, that's a flaw").
Decided: they move to a top-level `ext/`, a sibling of `extensions/`, so metta-extensions is a sibling repo and pymetta stands without it. Sixty-one live files take the new path; the dated journal entries keep the old one, because an entry is true to its date.
Found (the second coupling): the only symlink that crosses a component boundary is `extensions/python/pyproject.toml -> ../../pyproject.toml`. The Python seat is published as the `pymetta` package and needs a pyproject at its root, and the monorepo needs one for the workspace and the tool configuration; today they are one file with the seat linking to it, so a split pymetta would carry a dangling link.
Open: which way that link should point after the split. Inverting it, the real file in the seat and the monorepo root linking into the submodule, keeps one authoritative file and gives the split repo the real one, but whether uv resolves `members = ["ext/metta-*"]` against the link or its target is not something to guess; it is measured before the split lands.
Found: the `ext/` move cost more than the paths it rewrote. `extensions/python/_workspace.py` locates the distributions as `Path(__file__).resolve().parent / "ext"`, a level count rather than a spelling, so the rewrite that fixed every literal `extensions/python/ext` could not see it. It does not raise when wrong: the glob matches nothing, `members()` answers `[]`, nothing reaches `sys.path`, and the six distributions whose tests import their own module fail with `ModuleNotFoundError` while every lane stays green. Measured: 22 collection errors under `ext/`, against 238 tests collected on the pre-move tree with the same interpreter. `tools/doorgen.py` and `tools/reference.py` carried the same shape and were repaired with it.
Decided: `check_layering.py` compares the helper against the roster (`_the_checkout_finder_sees_the_roster`), so a level count that stops reaching the members fails the lane that already owns the roster instead of surfacing as an import error in a member's suite. It names `EXT` and the directories it misses. Both directions are reported, because a helper reaching a directory the glob does not name is the same defect seen from the other side. The selftest plants the real helper and rewrites `parents[2]` to `parent` to prove the rule fires; it reads the live file rather than a fixture copy, so the number stays free to change.
Found: `test_gate_completeness.py` runs ruff itself over `RUFF_SCOPES` to price the suppression burn-down, so the scopes are where code lives rather than which driver runs it. `ext` moves from the seat's scope to the repository's, beside `tests/checks`; it is the same ruff project root either way, because `ext/` carries no configuration of its own.
