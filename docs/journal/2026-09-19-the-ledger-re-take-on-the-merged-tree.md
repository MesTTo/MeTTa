# The ledger re-take on the merged tree
Goal: every benchmark ledger (extensions/python/benchmarks/{baseline,cost-baseline,scaling-baseline,memory-scale-baseline,extension-baseline}.json, engine/bench-baseline.json, extensions/cmetta/benchmarks/baseline.json, extensions/mork/benchmarks/baseline.json) pinned to the merged tree, every moved row carrying prose that places its move on the commits that made it, so the eleven ledger lanes are green on the tip the trunk fast-forwards to.
Constraint: a moved row without a placed step is reported, never pinned (ai-tmp/ai_compose_ledger.py refuses it); the ledgers keep a hand order the update doors would destroy, so numbers go in row-locally; the user accepts small cost differences from the merge (2026-09-18) but a move of a class, not a constant, is root-caused first.
Plan:
1. Base: the branch's own re-take on d761b9c43, preserved as commit object afb8a7d74 (its baseline.json rows placed by a 27-point ladder), restored into the editing tree. Established by reading its prose.
2. Three fixed points per row: afb8a7d74 (d761b9c43), the merge parent 89bd5dc41, the tip 4c0533704; each lane measured at the last two (wt-battery-4 and -5). A row's move splits into K = d761b9c43..89bd5dc41 (the branch's commits) and M = the merge with its batch fix. Established by measurement.
3. K is placed by a ladder over the branch's engine-touching commits (d761b9c43 e59104ace fd0af38f7 55368cb4e 0d0455d22 12609a929 7cc8fb863 a9e2c06d3 f06186a96 49478d67a 89bd5dc41) restricted to the moved rows (ai_ledger_ladder.sh, ai_seat_ladder.sh, ai_costs_ladder.sh); M is one step whose mechanism the class_values profile names (ai-merge-mechanism.txt). Established by measurement.
4. Any move that is a class rather than a constant is bisected to its commit and root-caused before it is pinned. Discharged for query-where and register-op below.
5. Composition: ai_compose_ledger.py for the record ledgers, ai_repin_rows.py and ai_top_comment.py for the hand-ordered ones; then the lanes on the composed tree, then the commit and its pin.

## 2026-09-19
Found: every lane on the tip is red against baselines last pinned on the branch weeks ago (wt-battery-4 ai-ledger-measure.log): benchmarks 31 rows, instructions 11, memory 15, costs 15, c-bench 5 counters, mork 26, extcost 1, engine-bench refusing its workload stamp.
Found: query-where 72,755 -> 1,323,707 is a visibility correction placed at 98540fdb2 by the probe ladder (ai-tmp/ai_probe_query_where.py over the bracket 0bd522b50..7ead07e09): 689745c3b's message records that a guarded len() had fallen back to the cursor route, whose work runs in an SWI engine the caller's counter cannot see, so the old pin was blind and the count route has been flat since 6872eee94. The merge moves it -1,680.
Found: register-op 123,931 -> 204,912 is +42,203 at 2398951d3 (compiled constructor checks), +8,831 in 7942599d2..6678a53c1, +28,900 at 10ef2f695 (expanded-call contracts), -6,038 in 0bd522b50..7ead07e09; flat over d761b9c43..89bd5dc41; the merge -400.
Measured (M, the merge alone): alpha-unique +49,998 over 50,000 terms, one inference per stored term; add-batch, add-table-rows, subscribe-tax +14; query-limit-guarded +500; run-source -20,000, source-load -6,937, annotated-relation -4,000, foreign-match and table-bridge-match -2,000, let-heavy -1,104, register-op -400; the memory rows within a dozen inferences except stored-atoms-mork 4,820,992 -> 3,104,768 bytes at 10,000; the cost rows -2..-13; mork-write -1 at 200; the instruction rows +0.2% to +1.1% and mork-native-add +3.1%.
Measured (K, the branch since the ledger's pin): the bulk of every inference move: alpha-unique +350,014, sort-atom +100,014, handle-round-trip +22,000, annotated-relation -27,000, run-source +6,000; the cost ledger's atom family +256 inferences per element (car-atom, cdr-atom, size-atom, union-atom, intersection-atom, cost-control-work); load-metta 270,769 -> 300,771, save-metta 221,402 -> 240,137, support-drop-one 447,776 -> 496,165, atom-reclamation 8 -> 17, hyperpose-branches 399,549 -> 272,893, stored-atoms-mork 15,736,832 -> 4,820,992; c-bench space-pair +20,000; mork-native-add +1 per atom, mork-per-atom-add -4 per atom; extcost m-define-no-annotations 102,129 -> 117,133.
Found: the instruction-only moves are older than the branch's window and the ledgers' own stamps hid it. The cost ledger was pinned under SWI-Prolog 10.1.13 and refuses a comparison under 10.1.14 unless it records; the engine ledger's parse row reads 128,944,822 -> 224,517,421 instructions (+74.1%) at the same 152 inferences with engine/reader.c unchanged since 8d6730740 (2026-09-07), its workload tests/data/prelude-spec.metta having gained ten lines at 97e30e94d (2026-09-16), whose byte digest refused every comparison since; the host moved to the patched 10.1.14 on 2026-09-17 and the instruction counter counts the host's own work. Open: parse measured on the pre-97e30e94d text, to split the text from the host.
Decided (the placement rules, ai-tmp/ai_ledger_prose.py): the chain of points is every commit of the first-parent line c66cf0c10^..89bd5dc41 that a ladder wrote a file for, in line order, so a ladder that lands later (the parse bisect, the resumed window halves) joins without a list to edit. A step is placed when a point reads past the row's band from the last placed point (four inferences; the row's instruction_noise_percent; a memory record's noise.absolute_max), charged to the interval since the last measured point, the drift the points before it accumulated inside the band stated first; a row inside its band at the tip keeps its pin, in the generator and in ai_compose_ledger.py alike. A row whose placed steps, in-band drift and merge step do not sum to its move is refused and nothing is written.
Rejected: charging a placed step to the span since the last placed step, because the C seat's space-pair +100,006 read as "560aaa42b..c5bdd73e0 (169 commits)" while 22 measured points inside the span all read 1,620,046; the move is at c5bdd73e0 alone (the native @owned-record declarations).
Rejected: naming every commit of an unsplit bracket, because a bracket the ladder never split is the range, which already names them; a bracket of four or fewer is spelled out.
Measured (K, placed by the eleven-point ladder): alpha-unique +350,014 and sort-atom +100,014 and handle-round-trip +22,000 in d761b9c43..fd0af38f7 (e59104ace, 9f32fe6a6, fd0af38f7, the Python boundary through the seam's codec); annotated-relation -29,500 in fd0af38f7..55368cb4e; the cost ledger's atom family +2 inferences per element in the same d761b9c43..fd0af38f7 bracket (car-atom +2,062/+4,108/+8,204/+16,396 at 1,024..8,192 elements).
Open: the memory rows' window brackets (the window halves paused at 3 and 4 points on the user's RAM ruling, resumed after the twins lanes); the parse +90,265,818 instructions in c66cf0c10..7942599d2 (the bisect over its 13 commits queued behind the seat ladder).
Decided (composition, ai-tmp/ai_compose_all.sh): 15 cost rows, 15 memory rows, 26 baseline.json rows (instruction rows at three points), 1 extension row, 9 engine rows, 4 C rows and 16 MORK rows re-pinned; scaling's mork-write held inside its band ([-1, 0, 0, 0]). The engine and C boot rows keep their pins: both refuse to compare from a battery worktree (checkout path 49 bytes against the canonical 29; the C row read 549,599 inferences there against its pin of 457,890), so they stay until measured from the canonical checkout. The C row's governed QLF inventory is attributed at 26 units (engine/atom_index.qlf and engine/host_transactions.qlf the branch's, engine/host_listeners.qlf the trunk's).
Deviation from the plan (user ruling 2026-09-19, "you're overpinning"): the parse bisect over c66cf0c10..7942599d2 stopped at 5 of 13 points, which narrows the parse row's +90,259,741 instructions to b29acf394..7942599d2 (8 commits); the memory window halves stopped at 6 and 5 points, so the memory rows' window steps are brackets of up to 173 commits. Each bracket is stated as a bracket in the row's comment.
Rejected: the pre-97e30e94d text control for the parse row (separating the spec's ten new lines from the host), because the ladder places +3,991,151 at 97e30e94d itself (the spec change, 4.4% of the move) and the host step inside the row's band; the bracket carries the rest.
Measured: the remaining seat rows' instruction moves are inside the trunk's per-write cost and the host (json-wire -679,821,534 of which -674,740,657 at the ladder base; space-digest -110,077,311; subscription-dispatch +4,780,456), each placed from its pin over the three instruction points.
Found (the gate on 10da82e4a, 173 lanes, 14 red; the trunk controls on fafab2703 in wt-battery-3): the cost-rows lane read CONTROL BROKEN because ai_compose_ledger.py copied whole record rows over the ledger's hand-shaped rows, so cost-control-work's answers, pinned [1, 1, 1, 1] from another row so the work gate fires, became the measured counts; the composer now merges only the measurement fields (counter, fit, noise or spread) into the committed row, stamps the measured date, keeps the document's own keys, key order and indent, and the diff is the moved rows. The ledgers were measured on 4c0533704 while the counters remedy was uncommitted beside them, so the commit that carried both moved the seat rows after their measurement (the counters journal, 2026-09-19); they are re-pinned on the tip with the A/B as their mechanism.
Decided: door-order and the automatic-tabling growth test are red on the trunk itself and stay the trunk's; parity-perf's unmeasurable rows are the box's; its five TREE DRIFT rows are the branch's ch22 examples, re-frozen through --rebaseline; KERNEL.md gains the branch's match-under row; EXTENDING.md's tables are regenerated from the tool since their first column derives from the extension pins.
Measured (parity-perf, `--rebaseline` in wt-battery-3, ai-parity-rebaseline.log): the lane's null process, a fresh swipl of the same path shape, reads 1,455,278,425 instructions on the patched 10.1.14 host against the frozen 1,048,438,941 on 10.1.13, which is why 124 rows read "the box would not measure" on the tip and on the trunk alike; re-frozen, the lane checks 5 examples and passes. Two cross-engine excesses surfaced by the re-freeze, both the branch's: combinatorics_lib 92,299,870 instructions against upstream's 47,760,640 and class_decorators 253,906,909 against 144,014,469; the lane's own run after the re-freeze does not raise them (the rows are outside its measurable set on this box), so they stand as findings, not verdicts.
Found (method): an A/B that reverts files with `git checkout <rev> -- <file>` moves the INDEX, so a later `git checkout -- .` restores the reverted versions rather than HEAD's, and every later round measures the same tree: round two of the counters A/B (ai-ab-remedy-2.log) read both halves at the base for that reason, and the repairs lanes inherited the four reverted files across a `checkout --detach` that carried them as local modifications. Restore with `git checkout HEAD -- .` or `git reset --hard`, and print `git status --short` into the log before each run.
Found (method): a benchmark test that pins a row's count and its slope, and the extension tool that holds every tier of its table, stopped at the first comparison that failed, so the second was never read: direct-join's and prepared-join's slope pins (585,480 and 1,348,992 from afb8a7d74) and the extension add-atom tier's pin (42,132 from the tokens merge) sat stale across the whole re-take because the point-count assertion failed first at every ladder point and on 4c0533704, and the lane printed nothing about them until the counts were re-pinned. Fixed in the harness: BenchmarkBaseline.collecting() records every failed comparison inside its block and raises one error naming all of them (test_collecting_reports_every_finding); the four slope tests and extension_cost.compare run under it. The three stale pins are re-pinned to what the tip and 4c0533704 both read (581,760; 1,347,120; 40,133), with the masking as their mechanism and no step placed.
Decided: source-load 260,317 to 260,332 (+15) is pinned with the counters commit as its mechanism, every single-file reversion having left it; the C seat's space-pair CPU is re-pinned at 0.28171 s measured at 0.09 runnable processes per core, the condition its own policy names, against the 0.53353 s taken at 1.00.
Found (once every tier reached the log): three more extension tiers had sat behind the m-define tier's failure: add-atom through an accept-all pre-add hook 59,132 to 57,133 and into a pool with a declared admits type 71,132 to 69,133, both the -2 per add every add-atom tier shares, and into a pool with a declared capacity 79,132 to 96,133, that -2 and +19 per add for the capacity check's own work, a constant per add inside the accepted cost difference. All three re-pinned with the masking as their mechanism and no step placed; the capacity check's +19 is the one worth a bisect over d761b9c43..4c0533704 if the pool path is ever optimised.
Decided (the seat rows, finally): the counters' join doors return to the export list as declared services (the counters journal), so engine translate, the C term-in and MORK's ten native rows are re-pinned to the final tree's readings with the A/B rounds as their mechanism, a constant-factor price the seam's laws require and the user has accepted; the class twins, pinned with the exports present, read their pins again.
Found: an engine or seat row's instruction count moves with the engine image's layout, not with its path: on three trees differing only in the order of four names in metta_engine's export list and in comment lines, translate read 332,927,436, 327,048,270 and inside its band, match inside its band twice and 164,110,782 (+3.1%), MORK conjunction-400 5,011,045 twice and 4,807,003 (+4.2%), match-first-400 1,352,911 twice and 1,332,644, the C term-in +0.41% or inside (wt-battery-2 ai-ab-remedy-3.log to -6.log, wt-battery-3 ai-seat-final.log and ai-seat-final-2.log). What the A/B rounds attributed to the three door names was where those names land in the host's procedure tables; the same names in another position moved other rows.
Decided: the rows that showed it (engine translate, match and evaluate; MORK's ten native rows; the C term-in) declare the measured layout spread as their band (instruction_noise_percent 5.0, term-in 1.0, `layout_band_comment`), and their pins stay what the rows' paths cost; a move past the band is a path change. Rejected: re-pinning to each tree's reading, because the next unrelated change re-pins again and the ledger stops saying anything.
Decided: source-load, exact on each run and 260317, 260332 or 260341 by tree with its loader path unchanged, declares inference_allowance 32 beside those readings (the loader's own second mode, the class twins' in miniature) and keeps the pin 260332; the reference_loading envelope pools 376423, read past its top with the lane alone, to 372287..376423 over 11.
