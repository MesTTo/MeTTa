# Source-derived operator consumers

Goal: derive Python operator names, aliases, signatures and compiler projections
from the existing pinned source inventory, retaining one MeTTa meaning per row.
Constraint: native class dispatch and its fallback equations remain separate
required work; invoking CPython is an oracle and the existing borrowed-value
boundary, not evidence of those native rules.

## 2026-09-15

Decided: `protocolgen.py` projects immutable source records and generated twin
programs through the existing `artifacts.py` and `doorgen.py` mechanisms.
`operators.py` joins those records to the existing MeTTa atom policies. Exact
runtime callable identities join only exported attributes. The compiler shares
that join for bare aliases and module attributes, and retains the exact host
call when Python's signature rejects a positional shape or requires keywords.

Rejected: separate callable, arity, reflected-name, comparison and composite
image rosters. Their facts are already present in source records and the atom
policy. A second copy adds independent drift. Retained MeTTa decisions include
numeric native heads, Boolean atom meanings and the fourteen math adaptations;
CPython cannot decide their engine images.

Tried: source projection controls discriminate ambiguous joins, guarded concat
and iconcat bodies, optional and variadic signatures, replaced aliases, missing
exports and stale generated bytes. Generated native programs cover direct
syntax, module attributes and bare imported aliases, with CPython value and
exception observations and integer fuzz inputs. Runtime verification is
recorded below when the integrated tree supplies it.

Decided: successful results use the preceding shared result boundary. The
consumer keeps imported-call witnesses for held results and borrowed mutation;
the existing three-grain result suite owns declaration image and native method
rewrite coverage. Each new program fixture releases its own native space.

Tried: `python -m pytest -q -n 0 --benchmark-disable
--randomly-seed=1125382488 --tb=short` over `test_protocol_programs.py`,
`test_operator_results.py`, `test_protocol_projections.py` and the operator
documentation sweep initially
reported 39 failures and 132 passes. Thirty-six fixtures assumed the twin
wrapper preserved function identity; its actual contract preserves the code
object. The None witness used unsupported identity syntax. Those assertions
now observe their intended callable/result contracts directly. The documentation
sweep found missing ceil, round and trunc rows, so the table is now another
projection of the existing operator join. Every printed form is evaluated by
`test_door_documents_build_each_atom_operator`, with planted image and row drift.

Measured: the corrected two-file runtime command passes 161 tests with peak
resident memory 284276 KiB. `test_protocol_source.py` passes 13 source controls
and `test_protocol_projections.py` passes seven projection controls. Logs are
`ai-tmp/ai-classes-c61b-{focused-repaired,source,projections}.log`.

Tried: isolating the remaining container failure with
`python ai-tmp/ai-classes-c61b-containers.py` shows that `values: list = [2];
return values` yields no native answer before reaching an operator. The same
fixture does so at pristine `c75181adc999adf0028616ee69565e2bbfbf739f`.
The unannotated feature chain agrees with Python. Its pristine concat path
raises `can only concatenate list (not "tuple") to list`. Local annotation
projection remains a compiler obligation; this consumer witness tests the
unannotated chain. Existing native local ascriptions are not removed.

Measured: `sh ai-tmp/ai-classes-c61b-verify.sh` passes 85 native tests and
15 subtests. Its Python selection passes 1800 tests and 326 subtests, with one
failure: the old `floordiv.*floor-math.* /` diagnostic assertion does not match
the canonical `(floor-math (/ x1 x2))` image. The assertion now names that image.
`sh ai-tmp/ai-classes-c61b-final-checks.sh` passes the entire affected file's
17 tests, then door-sync, door-sync-selftest, reference, Ruff, mypy, ty, layering,
policy-inventory and evidence. Production code did not change between these
runs. Native peak resident memory is 41484 KiB; the broad Python selection peaks
at 2043760 KiB. Logs are `ai-tmp/ai-classes-c61b-{native,python,
mentions-repaired,checks}.log`.

Tested: `GATE_ONLY=1 sh tools/check.sh artifact-sync artifact-sync-selftest
protocol-sync protocol-sync-selftest` passes all four lanes. The protocol lane
runs 177 tests and 326 subtests; the artifact lane runs 13 controls. Log:
`ai-tmp/ai-classes-c61b-generated-gates.log`. The clone scan records one unchanged
eight-line `factories.py` pair and no changed-line overlap in
`ai-tmp/ai-classes-c61b-clones.log`.
