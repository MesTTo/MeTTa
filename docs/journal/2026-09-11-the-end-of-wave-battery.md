# The end-of-wave battery
Goal: the merged tree after BINDING (765c60587), W-OBSERVE (8377451e7) and PERF (b29acf394) runs every gate lane and every cost lane; every red is attributed to its mechanism and then fixed at the cause or re-pinned with the mechanism named; the 277 provisional twins and the bench baselines re-pin on the merged tree under the normalised protocol.
Constraint: the batteries run alone on one checkout; a red is never baselined; door-order stays a GATE by the 2026-09-08 layout ruling and reports until SEAT-REVIEW lands.

## 2026-09-11
Tried: `GATE_ONLY=1 sh check.sh` on a850f1641 after warming `engine/*.qlf` (`ai-tmp/integrator-849a9e/wave-gate.log`) -> exit 1, 26 lanes red: corpus-coverage, engine-bench, boot-determinism, dev-typed, cumulative-syntax, c-bench, mork-bench, node-bench, pytest, benchmarks, instructions, memory-scale-gate, packaged, twins, vulture, ty, pylint, refurb, bandit, deptry, spec-status-selftest, policy-inventory, parity-perf, process-bounds, door-order, codespell. The pre-wave tip's battery (a97494776, 2026-09-10) ran the same lanes green, so every red is the wave's or the box's.
Tried: the evidence lane's tracked-set rule (landed in b29acf394) against `test_nested_package_evidence_rejects_a_missing_test` and the spec-status selftest -> both plant trees that are not repositories, so `owned()` kept nothing: the layout case found 0 claim sites and P90.9/P90.11 read OPEN/UNKNOWN. A second defect underneath: `tracked()` cached its first answer for the whole process, so a monkeypatched ROOT still read the checkout's set.
Decided: `tracked(root)` is keyed by the scanned root and `owned(paths, root)` takes it explicitly, every walk passing its own module's ROOT; a root that is not a repository raises naming the rule; a planted tree is a repository (`git init -q` in its builder, `git add -A` right before the checker runs, as `check_evidence_selftest.py` already did). Verified: `sh check.sh evidence evidence-selftest provenance-pin-selftest spec-status-selftest` green, the three layout cases pass.
Tried: correlating the artifact set with the gate's timeline -> `lib/lib_nars/lib_nars.metta`, `lib/lib_pln/lib_pln.metta` and `lib/lib_roman/lib_roman.metta` were rewritten with identical content at 04:47:38 by a lane that has not been identified, which stales the governed set (`qlf_source_newest/2` reads the corpus), and every `engine/*.qlf` was rewritten at 05:20:47 by parity-perf's `prepare_artifacts()` (`purge_all_qlf` then `qlf_load_engine`, by design). The boot-determinism reading that came back empty and the c-bench inventory of 22 governed artifacts against a pin of 21 sit on that timeline.
Open: packaged crashes because `setup.py`'s `RUNTIME_RESOURCES` enumerates the binding's Prolog files by name and BINDING added `provides_engine_user.pl`, which `surface.pl:57` includes; the runtime set is to be derived from what the extension's Prolog loads, with a selftest that plants an include. dev-typed fails four `compiled_sources.plt` cases and one `engine_modules.plt` case because `dev_typed.pl` forces `source=true` for the whole session. cumulative-syntax reports `return` first used at 20-02-04 while its row says 10-00-02; scanner, example and table are unchanged since a97494776, so the engine's reading of `02-throwing_and_tracing.metta` changed in the wave. corpus-coverage: lib_thread's `scope`, `capture` and `scope_body` (c6e1198c4) are called by no example. process-bounds: `check_upstream_parity.py:263` spawns swipl without `bounded`. codespell: `lib_thread.pl:301` `exitted`. The Python seat's tool lanes carry 30 ty, 45 pylint, 26 refurb, 11 vulture, 2 bandit, 21 deptry and 33 policy-inventory findings from the regrouped binding, the generated faces and the tools directory. Cost lanes: engine-bench boot +2,582 with evaluate, parse-prolog and translate improved; benchmarks annotated-relation +103,462, source-load +462, py-method-call +31, let-heavy +22, run-source -3,035 and the automatic-tabling pins off; memory-scale support-drop-spaces 9,053,162 -> 9,801,162; node-bench define-call, host-op and query-rows moved; mork-bench native rows +1% instructions; instructions save-load-metta and sort-atom outside band; twins 279 of 282 provisional. Each cost row is bisected across the four tips before any pin moves.

## 2026-09-11, the ladder
Tried: one detached, provisioned worktree per landing tip and one script per tip (`ai-tmp/integrator-849a9e/tip-measure.sh`: engine-bench, the Python benchmarks named red, the automatic-tabling observations, memory-scale `support-drop-spaces`, node-bench), folded by `tip-ladder.py` -> `ai-tmp/integrator-849a9e/tip-ladder.md`. Tips: 3e5855a35 (pre-FROM, PERF's and BINDING's base), da3c5de65 (PERF's tip), f22fdc640 (FROM landed), c66cf0c10 (closed sets and the two host services), 765c60587 (BINDING), 8377451e7 (W-OBSERVE), b29acf394 (PERF), 96ece1056 (trunk).
Measured, inference counts, deterministic on a box at load 20-30:
- boot 296,761 -> FROM 318,191 (+21,430) -> closed sets 318,369 (+178) -> BINDING 318,360 (-9) -> W-OBSERVE 318,360 -> PERF 318,369 (+9) -> trunk 318,378 without library artifacts and 318,350 with eight `lib/*/*.qlf` present. PERF's own tip reads 296,185 (-576 on its base). The pin 315,795 is FROM's branch value; FROM's merge onto a trunk that had moved cost the further +2,396.
- evaluate 559,106 -> FROM 558,932 -> BINDING 558,928; parse-prolog 3,517,359 -> FROM 3,539,934 (pin 3,540,184 is FROM's branch value); translate 313,725 -> FROM 315,334 -> BINDING 315,275 (pin 315,698, FROM's branch); match, match-skew and parse unchanged across every tip.
- annotated-relation 1,002,846 (= PERF's pin) -> FROM 1,107,808 (+104,962) -> BINDING 1,106,308 (-1,500); eval-arith, op-raw and op-encoded +5,962 at FROM and -6,000 at BINDING (net -38); py-method-call +30,031 at FROM, -30,000 at BINDING; let-heavy +27/-5; run-source 441,234 at every landing against a PERF pin of 444,269; source-load 258,686 -> +519 FROM -> -54 BINDING -> -12 PERF.
- support-drop-spaces at n=1000: 9,146,171 -> PERF's tip 9,053,162 (= its pin, -93,009) -> FROM 9,894,171 (+748,000) -> PERF's merge 9,801,162 (-93,009 carried intact).
- node define-call, host-op, query-rows: PERF's pins equal the pre-FROM tree; FROM +19/+20/+22; nothing after.
- automatic-tabling: the `_AUTOMATIC_TABLING_PINS` equal the pre-FROM tree; FROM moves plain -12 and automatic +54; BINDING -2 each.
Decided: no merge lost a gain; every row moves for FROM's landing (reference rows, four engine units: algebra_operations, properties, reference_loading, references) with BINDING giving part back, and PERF's pins priced its base. Each re-pin names those deltas. The boot pin states its artifact protocol (governed set purged, warmed once, no library artifacts beside the engine), because the row moves 28 with them present.
Verified: the receipts engine-destroy reproduction, 20 of 20 exit 0 on the merged tree (`ai-tmp/integrator-849a9e/wave-receipts-repro-20.log`).
Verified: after merging reds/policy-and-corpus-lanes (f11e8a6a4), `sh check.sh deptry process-bounds codespell cumulative-syntax corpus-coverage evidence provenance-pin-selftest example-origins llms` all ok; the new ch17/07 example passes its eight assertions under `sh test.sh`; W-OBSERVE's `source_observation.plt` and `source_observation_artifacts.plt` (purge, cold run, warm run) 20 of 20 exit 0 (`ai-tmp/integrator-849a9e/wave-observe-artifacts-20.log`).

## 2026-09-11, the three repairs land
Decided: three worker branches cut at 96ece1056 carried the 26 reds: reds/policy-and-corpus-lanes (merged f11e8a6a4), reds/python-tool-lanes (merged 1f2b0f3a8), reds/engine-and-packaging-lanes (merged 8e6968ecb). Each branch's own journal thread records its attempts; the integrator's lane runs on trunk after each merge were green (`ai-tmp/integrator-849a9e/wave-post-{policy,tools,engine}-lanes.log`).
Superseded, the 2026-09-11 attribution above of the empty boot-determinism reading to the library rewrite: the writer was `test_translation.py`'s derived-form comparison (pytest lane), but the governed set's MeTTa inputs are `engine/*.metta` and the library globs cover the Prolog halves, so rewriting `lib/*/*.metta` stales nothing; the engine worker's lane observer attributes the `lib_datetime.qlf` cache to the shell, dev-typed, no-autoload, plunit, pytest and parity-perf lanes in turn, and the empty sample did not reproduce standalone (the script now keeps the driver's diagnostics). The five dev-typed failures also reproduce on the pre-wave control: the typed runner's global `source=true` against the compiled-sources unit's artifact claims, and PlDoc's per-module tables in the shadow census, older than the wave. c-bench's 22nd governed artifact is `engine/source_positions.qlf`, required at boot since FROM's properties.pl imports `source_positions/3`; the shared loader `engine/source_loading.pl` is the 23rd.
Decided: the worktree census. 96 registrations under scratch roots and 158 elsewhere removed by exact path after saving each one's status, its patch of tracked modifications and copies of untracked non-build files (`ai-tmp/integrator-849a9e/worktree-census/`); three remain: the checkout, `PeTTa-base` (the upstream reference the parity lanes read) and pettagrapher's v0.8.0 checkout.

## 2026-09-11, the whole gate on the merged trunk and the re-pins
Tried: `GATE_ONLY=1 sh check.sh` on 8e6968ecb after purging and warming the governed set (`ai-tmp/integrator-849a9e/wave2-gate.log`) -> red only on engine-bench, boot-determinism, c-bench, mork-bench, node-bench, pytest (the identity twin), benchmarks, instructions, memory-scale-gate, twins, parity-perf (45 rows refused at loadavg 13) and door-order; every repair lane of the three branches green on trunk.
Tried: boot-determinism -> the lane parsed only the driver's passing (`samples=[`) and improvement (`every sample of [`) shapes, so a boot outside its pin in the regression direction (`minimum of [319090, 319090, 319090]`) read as "no sample" in both gates. The reading now comes from all three shapes; the property (eight equal samples, the gate's TMP and the bare TMP agree) never depended on the pin. Verified: `sh check.sh boot-determinism` green with the pin still stale.
Decided: the re-pins, each row named, under the purge-and-warm protocol (`ai-tmp/integrator-849a9e/wave2-repin.sh`, logs `wave2-repin-*.log`): engine boot 315,795 -> 319,090 (FROM +21,430 over its base and +2,396 over its branch pin, closed sets +178, BINDING -9, PERF +9, the REDS shared loader +712), evaluate 559,106 -> 558,928, parse-prolog 3,540,184 -> 3,539,934, translate 315,698 -> 315,275; the Python rows annotated-relation 1,002,846 -> 1,106,308, eval-arith/op-raw/op-encoded -38, py-method-call +31, let-heavy +22, run-source 444,269 -> 441,234, source-load 258,677 -> 259,133, automatic-tabling with its pytest pins (plain -14, automatic +52); memory-scale support-drop-spaces 9,053,162 -> 9,801,162; node define-call/host-op/query-rows +19/+20/+22; the C boot 457,890 -> 484,757 inferences with the inventory at 23. Every mechanism is written beside its row as `end_of_wave_repin_comment` (or in the memory-scale cause chain and the tabling comment).
Rejected: the drivers' rewrites of advisory fields (`wall_seconds_per_operation`, `cpu_seconds`), restored from HEAD because the box was loaded; and the memory-scale subset update's document, which had pruned the 19 unmeasured cases (restored from HEAD around the one measured row). Revisit the subset updater if a single-case re-pin is meant to keep its siblings.
Open: mork-bench, instructions and the C runtime rows count instructions on a box that was never quiet today (semantic-search indexing and the editor's language servers hold it near load 8); they are re-measured alone before any pin moves. The twins re-pin runs next.

## 2026-09-11, later: the boot row's artifact state becomes the driver's own
Tried: `sh check.sh boot-determinism engine-bench benchmarks memory-scale-gate node-bench c-bench` after the re-pins -> engine-bench boot read 319,073 against the fresh pin of 319,090 (five library artifacts left by the earlier lanes) and node-bench's `answers-lazy` instructions +2.9% (deferred to the quiet box); everything else green.
Measured: purge, warm through the shipping loader (`qlf_load_engine`, the hermetic child), list, boot twice -> 21 artifacts, a first bench boot of 332,050 that compiles `engine/identity.qlf` and `engine/source_loading.qlf` (the child loads those two units from source before it turns qcompile on), 23 artifacts, then 319,090. Purge, warm through the bench's own boot (`metta_bench:bench_run(boot)`), list, boot twice -> 23 artifacts at once, 319,073 both times. Same set, different readings: the umbrella compiled against source-loaded units reads 17 more when loaded than the umbrella compiled against artifact-loaded ones.
Decided: the boot row prepares its own state. `metta_benchmarking.prepare_governed_artifacts(root)` is the one purge-then-warm-through-the-ordinary-boot preparation, listing the governed set through the boot's own inventory door; engine/bench.py runs it before the boot row's samples and the C fixture's `prepare_boot` calls the same function (its two subprocess calls deleted), so both boot rows read one state whatever ran before them. The boot pin is 319,073 under that protocol, written by `sh engine/bench.sh --update-baseline boot` with the mechanism beside it. The parity harness keeps its hermetic-child preparation because it prepares a corpus run, not a boot measurement; its inventory of 21 is that loader's set. Verified: the harness's tests (75 passed), `sh check.sh engine-bench boot-determinism c-bench`.
Rejected: pinning the shipping loader's state instead, because a bench boot in that state mutates it (compiles two artifacts) on its first run, so the state cannot be reproduced without the two-boot dance; the driver's own warm is reproducible in one step and is what the C fixture already measured against.
Why the two sets differ in content and not only in count: the producer's process shapes the artifact (its init file, its packs, the flags it runs under), which is why the shipping loader compiles through a hermetic child (`-f none`, `--no-packs`, the child marker; `2026-09-09-runtime-units-compile-beside-their-source.md`). The bench's warm boot is not that child: it is the ordinary `swipl` the bench measures under, so its artifacts are the ones a `bench_run` loads, 17 inferences apart from the hermetic child's. The row prices the bench producer's set on purpose, because that is the only set the measured process does not rewrite on its first load.
Open: making engine/bench.pl's `bench_boot_prepare` load identity and source_loading from source before qcompile, as `qlf_load_engine` does, would let the boot row price the hermetic child's set without rewriting it; the row's meaning would change (those two units would leave the measured window), so it is a new pin with a new mechanism rather than a correction of this one.

## 2026-09-11, the twins re-pin on the merged tree
Tried: `python extensions/python/tools/twin_coverage.py --repin --rounds 3 --reason "<the wave's landings and the REDS repairs>"` over every twinned example (282; `ai-tmp/integrator-849a9e/wave2-twins-repin.log`) -> 265 twins re-pinned, 0 stored-content divergences, 8 envelope twins left to `--observe`; then `--observe --rounds 10` (`wave2-twins-observe.log`), every envelope twin 10 of 10 observations with 0 failures under `full-lane/282/workers=32/file-search-cache-time=9223372036854775807/before-boot`.
Tried: the twins lane on that tree -> 15 findings: six twins whose declared OVERRUN the tree no longer needs (08-unify_eval_branches 1405, 01-segments 140, 01-he_error 969, 06-memo_dependency_invalidation 278, 04-translatorrule_cost 310, 06-translatorrule_fib 283), eight envelopes measured under the 277-example protocol (67 observations) or FROM's 280-example one, and one twin past its band (02-callquoteevalreduce2, 43,465 against a ceiling of 43,284).
Measured: `python extensions/python/benchmarks/probes/twin_authoring.py` on the merged tree -> 7, 1672, 3019, 4382, 5757 for 0..4 definitions, the fit 303 once plus 1362 each. The first definition's premium fell from 1,482 to 303 (PERF's 7/2853/4202/5567/6944 read 1482 plus 1364): the binding's boot import loads what the first compiled definition used to autoload, so a twin no longer pays it and the band no longer grants it. `DEFINITION_WARMUP` 1482 -> 303, `DEFINITION_COST` 1364 -> 1362, with the probe cited beside them.
Decided: the six overruns and their paragraphs dropped, as the lane asks; the eight envelopes re-pooled from the ten new observations under the 282 protocol (each RE-ENVELOPED paragraph names the old range and count beside the new: mutex_and_transaction 16,693..16,701, thread_lib 286,948..305,706, thread_linda 135,435..135,549, channels_pools 98,770..98,922, the_prolog_rung 123,687..124,929, git_import 26,247 flat over ten, reference_loading 331,616..335,619, 01-measure 131,890..131,989); 02-callquoteevalreduce2 declares OVERRUN 1372 with `twin_floor.py`'s attribution (example 30,562, ceiling 42,093 under the new constants, floor 30,598, twin 43,465: six interpreter-step definitions cost 2,144 each to author against the 1,362 the band grants; the library is not slower than the engine here).
Rejected: keeping the 277-protocol envelopes with their 67 observations pooled into the new ones, because the protocol string is part of the claim (the lane's size is the scheduler the counter answers to) and pooling across protocols is the widening the lane refuses; the old ranges stay in the paragraph as history.
Tried: the lane again with the constants re-derived -> 13 findings, twelve twins past a band that had stopped granting the 1,179 the fixture no longer pays while they still pay part of it, each by its own amount (if3 361, if4 1,120, fibsmart 635, segments 51, translatorrule_cost 273, translatorrule_fib 69, memo_dependency_invalidation 398, and five with a declared overrun already: specializecyclic +1,110, eval +77, memo_aggregate +155, memo_stats +16, tabling_fib +1), and channels_pools one observation below its ten-round envelope (98,766 against 98,770).
Decided: the residual is each twin's own, the first use of the lowering paths its definitions take beyond the fixture's `return x + 1`, and a per-path warmup would be a second constant the band does not carry; so each twin declares it as OVERRUN (or raises the one it has) with the measured cost, the ceiling and its components in the paragraph, four of them the overruns retired an hour earlier priced again at their new values (segments 140 -> 51, memo_dependency_invalidation 278 -> 398, translatorrule_cost 310 -> 273, translatorrule_fib 283 -> 69), and the channel twin's envelope extends its minimum to the new mode with the count at 11 rather than widening blind.
Tried: the lane a third time -> 4 findings: three twins exactly at their printed ceiling (eval 17,690, fibsmart 10,172, memo_dependency_invalidation 22,399), because the ceiling is a float (example times 1.1 plus authoring plus the declared overrun) compared with `>` and printed rounded, so a residual computed from the print is one short; and channels_pools at 99,528, above the envelope it had just extended downward, the timer-cancellation mode PERF's 64-process trace attributed on this twin.
Decided: the three residuals round up (3,150, 636, 399); the channel twin's envelope takes both lane observations (98,766..99,528 over 13) and is re-observed with more than ten rounds in the next battery, since ten rounds under the 282 protocol did not reach that mode. Verified: `sh check.sh twins twins-selftest` -> 0 findings over 282 twinned examples (`ai-tmp/integrator-849a9e/wave2-twins-lane-4.log`).
Open: the first-definition premium is path-dependent (the fixture's arithmetic body no longer pays it; conditionals, matches and lets still pay some of it per twin); the band carries one constant by design, so the residuals live in twelve twins' overruns. A probe with one fixture per lowering path would say whether a per-path table is shorter than twelve paragraphs.

## 2026-09-11, the final gate's unexpected reds, and a second host defect underneath
Tried: `GATE_ONLY=1 sh check.sh` on b1d175f13 (`ai-tmp/integrator-849a9e/wave3-gate.log`) -> `mork-bench node-bench pytest instructions twins bandit parity-perf door-order`; the cost lanes after it (`wave3-cost.log`) -> `mork-bench node-bench instructions`, the instruction rows on a box carrying two Codex jobs. Expected by the protocol: mork-bench, node-bench, instructions, parity-perf (loaded box), door-order (ruling). Unexpected: bandit, pytest, twins.
Decided: bandit -> the boot-artifact helper's two `subprocess.run` calls and its import carry `# nosec B603` and `# nosec B404` beside the `# noqa: S603` they already had, the way `metta/cli.py` marks the same shape; the reason is the comment above them (fixed argument vectors built from this tree's own file names). pytest, `test_the_band_pays_for_authoring_but_only_what_was_measured` -> the test hardcoded a twin cost of 2,000 chosen against the old authoring constants and pinned it as the fixtures' BUDGET; it now derives the cost from `BAND_PERCENT`, `DEFINITION_WARMUP` and `DEFINITION_COST` and writes that number into both fixtures, so it follows the constants wherever a re-derivation moves them (`a-constant-outlives-its-mechanism`).
Tried: pytest, the seven `test_materialization.py` failures -> root-caused as a SWI 10.1.13 defect, the same one behind the 2026-09-07 `prolog_wrap:member/2` intermittent; the evidence, the mechanism, the rejected shapes and the workaround are in `docs/journal/2026-09-07-every-intermittent-root-caused.md`, the 2026-09-11 section, and the ledger entry `swi-autoload-cut-installs-the-undefined-supervisor`.
Measured, while proving the workaround: a findall over budgets that each bound a goal running a nested findall collects 13 of 200 in bare SWI 10.1.13, while a thrown ball through the same nesting collects 200 and a depth limit collects 200. Mechanism, read in the source: `findall/4` is `cleanup_bag/2`, which is `'$new_findall_bag'` followed by `'$call_cleanup'`; the bag is on the thread's bag stack from the push on and the cleanup that pops it is registered one call port later, so a limit that trips on that port unwinds with the bag in place (the swi-cleanup-window rule, seen in the host's own findall); every later answer of the enclosing findall is added to the stale bag, and the enclosing findall collects its own bag, which is short. `findnsols2/5` pushes the same way through `setup_call_cleanup/3`. The Python seat collects a bounded evaluation's answers with `findall/3` around it (`_binding/control.pl`, `metta_py_hold_captured`), so a cut landing on that port inside a MeTTa evaluation silently truncates the answers a Python caller receives.
Decided: the second ledger entry, `swi-findall-bag-push-window`, in the same file. Both shapes rest on one host rule read in src/pl-prims.c: a trip on the call port of `catch/3` is not raised there but at the next call port, which is inside the catch. findall's loop is deterministic and never fails, so its bag is pushed and the loop and the pop are caught together with the pop in the recovery, the host's own three call ports at the same cost; findnsols's loop yields chunks and is cut by its callers, so it keeps a registered cleanup, registered before the push, with the push followed by `catch/3` and the record of the push as the first goal inside it, and a cleanup that is itself a `catch/3` term whose drop records before it pops. A C predicate making the push and its record atomic was designed and rejected: the engine's C half calls Prolog through nested queries whose top frame runs the same limit check, so the window moves inside the C call instead of closing.
Measured: the findall shape costs one inference more than the host's own, the catch (16 against 17 per findall of three answers, bare SWI), and the engine's match row rose 2.1% with a helper frame around it (267,402 to 273,002) before the body was inlined. Paying it only where a trip can land: wrapping `call_with_inference_limit/3` to count open regions per thread costs the same one inference per findall for the check, so instead the first `call_with_inference_limit/3` of the process installs the two wrappers and they stay. Rejected: unwrapping them when the last region closes, because `unwrap_predicate/2` releases the closure blob that another thread may be executing at that moment, and the wrappers' only cost is the one inference. A process that never bounds runs the host's shape untouched.
Tried: twins, three findings on point pins and one envelope (`04-minimal_metta` 188,051 against 188,043; `13-strategy_internals` 394,712 against 394,704; `06-the_prolog_rung_under_lib_thread` 124,957 above 123,687..124,929). The +8 is one interrupt-poll tick's charge, so the poll's subtraction was the first suspect: the probes under `ai-tmp/integrator-849a9e/probe-heartbeat-twin*.py` ran the minimal_metta twin with the poll at its default, off (the seat refuses 0), and dense at 1,000 inferences (3 runs each), with fixed hash seeds 0 to 20 (27 runs), instrumented with the poll, GC, atom-GC and clause-GC counters (12 runs), and under 24 concurrent children with the dense and the default poll (48 runs): every run read 188,043 except one default run early on, which read 188,071. An engine's inferences do not land in its creator's counter (measured: 9 for a 6,000-inference engine), so the receipts engine cannot carry a tick into the twin's count; the child has no Python threads and three Prolog threads (main, gc, the suspended receipts engine's alias).
Open: the twins' excursion mechanism. It is wall-clock dependent and rare (1 in 90 quiet runs, 3 of 282 twins in the gate under load), not hash-order dependent, not the poll, not GC, and not a joined thread. The gate on the repaired tree is the next observation; the pins do not move for it.
Measured, the tree with `engine/metta/limits.pl` against the re-pinned trunk, three identical samples per row: engine boot 319,073 to 319,383 (+310, the file's own load: `use_module(library(prolog_wrap))`, three `wrap_predicate/4` installs with their idempotence checks, the hook's multifile and dynamic declarations); engine translate 315,275 to 315,331 (+56, one inference per first-use resolution for the catch around the trap's query); C seat boot 484,757 to 485,074 (+317, the engine's +310 and seven resolutions of its own); Python query-limit-guarded 37,707 to 38,107 (+400, one inference per findall under the row's own bound, plus the first bound's install of the findall scope in the first sample only, +125, outside the minimum); Python source-load 259,133 to 259,175 (+42 resolutions). Evaluate, match, parse, parse-prolog and match-skew did not move: an unbounded process never installs the findall scope.
Tried: the exception hook as a static clause -> the C seat's error-ball row +2,000 (634,015 to 636,015), the Python foreign-match row +1,965 (793,269 to 795,234) and the extension-cost add-atom row +997 per call, every one of them a ball raised per operation that the hook was consulted on, one inference each. Decided: the hook is asserted by the process's first bound with the findall scope, since only a bound can need it; with that, all three rows read their pins again. Open, as a performance observation and not this thread's: the foreign-match row raises about one ball per query and the plain add-atom door about a thousand per call, which the static hook priced by accident; whatever raises them is control flow by exception on a hot path.
Twins: under the static hook, 40 point twins read 5 to 350 above their pins and three envelopes above their maxima. With the hook installed only by a bound, the lane's own check reads 25 of those 40 at their pins again (minimal_metta 188,043, strategy_internals, 01-math among them: their excess was one inference per ball raised, not resolutions), and `--repin --rounds 3` re-pinned the 15 that carry a real cost, the first-use resolutions and, for the twins that bound, one inference per findall: constraint_domains, regex_lib, documentation_as_data, the_prolog_rung, time_and_pragmas, hyperpose_primes, thin_forms, memo_controls, restricted_spaces, the-module-doors, reference_rows, reference_maps, pln_roman, pln_formulas, pln_derivation_control. The eight envelope twins were re-observed for ten rounds and their envelopes pooled with the earlier observations.

## 2026-09-11, late: the twins' excursion, two of its three modes named
Tried: the final gate on the finished tree (`ai-tmp/integrator-849a9e/wave5-gate.log`) -> green on pytest, the C seat, the Python benchmarks, extension costs, llms, bandit and the ledger lanes; red on the four loaded-box lanes and door-order as expected, on engine-bench translate (315,273 against the intermediate pin 315,331: the earlier +56 was the static exception hook's inference per ball the translate workload raises, now installed only by a bound; re-pinned to the measured 315,273 with that mechanism), and on the twins: `04-minimal_metta` 188,049 and `13-strategy_internals` 394,710, both exactly 8 above the value ten full-lane observation rounds read for them (188,041 and 394,702, spread 0), the same two twins and the same 8 as in the two gates before.
Measured, one twin child at a time with the lane's own environment: with `lib/minimal_metta_lib/minimal_metta_lib.qlf` absent, 188,069; with it present and fresh, 188,041 three times; with the `.pl` newer than the artifact, 188,066; after the artifact is compiled by the corpus runner under `NO_AUTOLOAD=1` or by an ordinary run, 188,041 either way; after touching the library's Python face or its `.metta` half, 188,041. So the +28 mode is the first importer after a purge paying `qlf_compile_aside/1`'s own inferences in the parent (the shell command it assembles and runs for the hermetic child), which the 2026-09-09 note beside that predicate claims lands in no measurement; it lands 25 to 28 inferences in the first importer, and `13-strategy_internals` shares it because `lib_strategy.metta` imports `minimal_metta_lib`. Both probes that read +28 earlier today were the first child after a purge or after an artifact was removed, which is what "first run of a batch" looked like.
Measured, the artifact truncated to its first hundred bytes: the child dies with `FATAL ERROR: Unexpected EOF on QLF file at offset 22` and dumps core, and the truncated file stays, so every later boot dies the same way until the file is deleted. The engine's own writes are a temporary file and a rename, so no importer race produces that file; a killed process, a full disk or a stray copy can, and nothing repairs it.
Open: the +8 mode, which every gate shows on these two twins and no standalone run of the lane reproduces, the ten-round observation and the three re-pin rounds included; the launcher itself (`sh check.sh twins`) is under test as this section closes. The pins stay at the standalone values.

## 2026-09-12: the +8 named, the two memo residuals, and the first-run mode re-attributed
Tried: `sh check.sh twins` alone on the finished tree (`ai-tmp/integrator-849a9e/twins-via-check.log`) -> the two ch20 twins at their pins (188,041 and 394,702); 2 findings, both band residuals read past in waves 4 and 5 beside the +8 pair: `02-memo_aggregate` 25,689 against a ceiling of 25,688.9 (20,709 x 1.1 + 1,665 + the declared 1,244) and `08-memo_stats` 24,806 against 24,805.9 (18,999 x 1.1 + 1,665 + 2,242).
Measured: the clean tip b1d175f13 in a fresh worktree (`ai-tmp/wt-minus2`, `probe-pair.py`) reads 19,001/24,808 and 20,711/25,691, the values the residuals were priced against; the tip plus `engine/metta.pl` and `engine/metta/limits.pl` reads 18,999/24,806 and 20,709/25,689, example and twin each 2 lower. Variants of limits.pl (`make-variant.py`, `minus2-variants.log`): without the `'$undefined_procedure'` wrap 18,999; without the `call_with_inference_limit` wrap 18,999; without the hook declarations 18,999; without all three, the `use_module(library(prolog_wrap))` alone, 19,001. SWI's profiler counts the same 13,835 calls over the same 990 predicates in both configurations (`probe-profile.py`), the block runs no first-use resolution in either (`probe-undef.py`), and the same one file (`lib/lib_import/lib_import.pl`) loads inside it; so the two are retry or unwind steps the VM counts and no predicate owns (src/pl-vmi.c counts one at each clause retry and at each frame an exception leaves), and they leave once the file has run one `wrap_predicate/4` at boot. Which two is not attributed.
Decided: the band scales the example's move by 1.1 and the twin's by 1, so a uniform drop of 2 moves a ceiling 2.2 and its twin 2, and a residual pinned 0.1 inside its ceiling reads 0.1 past it; both round up under the morning's rule (2,242 -> 2,243; 1,244 -> 1,245) with the move named in their paragraphs. The lane printed the ceiling with `:.0f`, so both findings read "24806 past a ceiling of 24806"; it prints `.1f` now, the tenth the comparison used.
Tried, the +8: the artifacts written by the bench producer (`prepare_governed_artifacts`, 37) and by the shipping loader's hermetic child (22) -> 188,041 and 394,702 under both, and under a library half compiled by the child against the bench producer's engine units (`probe-producer.sh`); `set_prolog_gc_thread(false)`, so every collection runs on the requesting thread -> 188,041; the process pinned to one core beside a busy loop, three runs -> 188,041; the gc thread requested before the block -> 188,041 (`probe-gcthread.py`). Two clause collections run inside the block in every configuration (`statistics(cgc)` 11 before, 13 after); 158 erase events fire on the gc thread and 35 inside a held cursor's engine, none on the main thread (`probe_erase.pl`); the seat's tick record is exact on both reads and SWI's heartbeat is a per-thread countdown checked at exit ports (src/pl-vmi.c exit_checking_wakeup). None of it is the +8.
Tried: every GATE lane before twins in the gate's own order (`prefix-lanes.txt`, 61 lanes, `prefix-gate.log`, 23:08 to 00:01), then the two twins standalone in the state it left (`probe-after-prefix.sh`) -> 188,049 twice and 394,710: the mode is on disk and survives the prefix. The state: 43 artifacts against the hermetic 22. Removing the six the bench producer's state lacks one at a time -> 188,049 each time; moving `repos/` (the git-import lanes' tree) away -> 188,049; purging every artifact and rebuilding through the boot -> 188,066 once, then 188,041; the kept 43 put back -> 188,049. Overlaying the kept engine units alone on the fresh set -> 188,041; the kept library halves alone -> 188,049; `lib/lib_import/lib_import.qlf` alone -> 188,041; `lib/minimal_metta_lib/minimal_metta_lib.qlf` alone -> 188,049 (`bisect-artifacts.sh`). The import itself: 14,520 through that file, 14,512 through one compiled in place.
Measured, the two files: `strings` shows the kept one compiled from `ai-tmp/check-runs/run.iPzRx0/mork-missing-artefacts.gc3pdY/unbuilt/lib/minimal_metta_lib/minimal_metta_lib.pl`, mtime 23:15:53, the minute `mork-seat` ran; the header's saved path, read by `tests/checks/qlf_header.py`, is the scratch tree's. `extensions/mork/tests/test_missing_artefacts.sh` linked the checkout's `lib/` DIRECTORY into its scratch tree (its `engine/` was a per-file link tree already, for the `..` reason its header gives), so the library halves its boots compiled were written into the checkout under the scratch path: three of them (`lib_gitimport`, `lib_memo`, `minimal_metta_lib`), the check flags. Mechanism, read in src/pl-qlf.c at fc7ef84b: a .qlf records the absolute path it was saved under (`pushPathTranslation`); loaded from another directory it `has_moved`, every recorded source path is rewritten (`qlfFixSourcePath`) and `popPathTranslation` calls `system:'$translated_source'/2` once per rewritten path, a Prolog predicate, so the load counts 8 more for a one-source library in every process. Why no run outside a gate saw it: `parity-perf` (root lane 478, after twins) purges and rebuilds the governed set through the hermetic child, so the post-gate tree never held the scratch-written files; `13-strategy_internals` shares the mode because `lib_strategy.metta` imports `minimal_metta_lib`.
Decided: the test builds `lib/` the way it built `engine/`, one `link_sources` for both (real directories, a link per file, `*.qlf` and `__pycache__` skipped), and checks at its end that no artifact under the checkout's engine/ or lib/ is newer than its start; `tests/checks/check_qlf_provenance.py` (+ selftest) is a GATE lane reading every artifact's saved directory and refusing one written elsewhere, with the purge door as the remedy; the header reader is shared with the parity selftest's `qlf_content_digest`. The pins stay at 188,041 and 394,702.
Measured, the first-run mode named yesterday as "the first importer after a purge paying qlf_compile_aside/1": with the artifact's presence and mtime printed before boot, after boot, before the block and after it (`probe-artifact-phase.py`, clean worktree): `minimal_metta_lib.qlf` deleted -> written during boot, the block reads 188,041; the library's `.pl` touched newer than the artifact -> rewritten during boot too, the block reads 188,066 once and 188,041 from the next process; the engine's units purged with the halves kept -> rebuilt at boot, 188,041; everything purged -> 188,066 on the first process. No compile lands inside the block in any of these; the +25 is what a process reads when its boot found a source newer than its artifact or found no umbrella, not attributed further, and the pins do not carry it.
Routed: the PUBLICATION job's finding (23:19), an inference limit tripping inside `spaces:metta_receipt_frame_finished/1` after a transaction's commit, which leaves the scope's global, its erased rows and the standing engine's reservations behind (its reproducer stops at budget 77 on the cut and at 77 against this tree, so limits.pl does not cover it), went to GUARD, whose site class it is; the site, the requirement per budget and the shapes limits.pl established are in the steer.
Decided: the 2026-09-11 pooling (`pool-envelopes.py`) had rewritten the eight envelopes' numbers without the paragraph the convention asks for; each carries its POOLED paragraph now, the second population's extrema and samples beside the first's range and count (`pool-paragraphs.py`). The lane run that verified the repair read `01-mutex_and_transaction` at 16,717, 3 above its pooled 16,714, on a box carrying three Codex jobs' benchmarks; the envelope takes the observation with its count (16,693..16,717 over 21) and the next battery's ten rounds re-observe it. Verified: `sh check.sh mork-seat qlf-provenance qlf-provenance-selftest qlf-freshness parity-perf-selftest process-bounds evidence twins twins-selftest` -> every lane ok except that envelope reading (`ai-tmp/integrator-849a9e/fix-gate.log`), then `evidence twins-selftest` ok after the paragraphs and the fixture tags (`evidence-after.log`); the +8 pair and both memo twins at their pins.

## 2026-09-12, the publication merge and a fourth host defect under it
Decided: PUBLICATION merged as `c00465ae4` (parents `dfe0a6a62` and `16ad383cf`), nineteen files, no conflict; `CHANGELOG.md` and `docs/host-workarounds.md` merged with both sides intact. Its deliverable is `ai-tmp/ai-source-owned-publication-once.md` and its receipts `ai-tmp/ai-publication-receipts/` (1,134 files).
Tried: the merged tree's correctness battery with the display withheld (`env -u DISPLAY`, `ai-tmp/integrator-849a9e/mergepub-*.log`) -> `engine/test.sh` 0, twenty-two check lanes 0 (worktree, build, qlf-freshness, qlf-provenance, evidence, provenance-pin-selftest, conflict-markers, prolog, prolog-static, plunit, lib-surface, layering, examples, shell, host-workarounds(+selftest), llms(+selftest), parity, petta, docs), the Python suite 15 failed / 5,577 passed.
Measured, the fifteen: eleven are one host defect, reproduced on the pristine cut as well. SWI's `profile/2` is `call_cleanup('$profile'(Goal, How, Ports, Rate), show_profile(Options))` and the report divides each predicate's ticks by the total and the net time by it again (library/prolog_profile.pl time_data/7 and its caller), so a goal that finishes inside one 5 ms sampling period raises `evaluation_error(zero_divisor)` from the CLEANUP of a goal that already answered, and the ball unwinds the goal's bindings on the way out: `tests/checks/host_workarounds/swi-profile-report-divides-by-zero-samples.pl` reads `samples=0 answer=discarded present`. `top(0)` does not avoid it, and the report consults `prolog:show_profile_hook/1` first, which SWI autoloads from xpce whenever DISPLAY is set. The door's old absorber tested `nonvar(Out)`, which cannot work: the unwinding removed the binding before the catcher ran. Measured here: a 3,000,000-inference loop profiles to samples=6 ticks=26 at loadavg 4, so the whole class of short profiled goals reads zero, and the suite passed yesterday only because the box was loaded.
Decided: the fourth ledger entry `swi-profile-report-divides-by-zero-samples`, and `extensions/python/metta/_binding/profiling.pl` calls the primitive `profile/2` itself calls, `'$profile'(Goal, cputime, Ports, Rate)` with the flags profile/2 reads for its defaults, and never the report; the rows still come from `profile_data/1`, whose own division was already guarded. The fourteen tests pass (eleven profile doors, the three that reach a profile door through the template, register-prolog and aio surfaces), with `test_a_profile_with_no_samples_still_answers` as the regression. Rejected: keeping `profile/2` and recording the answer in a mutable cell before the report runs (the shape GUARD wrote on its branch) because the report is work nothing here reads, it consults the xpce hook, and the primitive is the documented layer under it; when GUARD's branch merges, its cell version reconciles to this one.
Measured, the identity twin: 2,586 to 2,500 on the merged tree (publication's own levers, -60 the compiled typing witnesses and -26 the publication context), re-pinned with that mechanism. The other pins PUBLICATION owes (engine-bench evaluate, translate, match, match-skew; the twins lane; the two obsolete overruns) are the end-of-wave re-pin's, once CLASSES, LIBRARIES and GUARD land.
Open, the box: the X server on `:0` has refused every GLX context since about 07:30, and SWI indexes xpce for autoload whenever DISPLAY is set, so two lanes (`prolog-static` through SWI's own `check/0`, `parity` through the seat's shadow repair asking `imported_from/1` for `send/3`) die with an X error on any tree and pass with the display withheld. Every gate here runs `env -u DISPLAY` until the display recovers; the two autoload sites are in PUBLICATION's deliverable as open engine notes.
Verified, the profile repair: `sh check.sh evidence llms llms-selftest docs prolog-static shim-surface host-workarounds host-workarounds-selftest` exit 0 (14 ledger entries, 21 sites, every reproduction `present`), and the whole Python suite 5,592 passed / 100 skipped / 1 failed (`ai-tmp/integrator-849a9e/profilefix-*.log`). The one red is `test_retired_memo_owners_release_their_event_and_dispatch_clauses`, whose census read `Removed` one above the count it started with; it passes alone twice, it is not on any path this change touches, and it failed the same way with the same counter on 2026-09-11 in the REDS policy battery (`ai-tmp/ai-reds-policy-receipts/logs/ai-policy-whole-gate.log`, `{'Removed': 2} != {'Removed': 3}`, a different tree and a different worker order). Open: a retirement clause is still present when that census reads, in about one whole-suite run of two; the mechanism is unattributed and the counter is process-global, so the next attribution wants the worker's own ordering, not a rerun.
