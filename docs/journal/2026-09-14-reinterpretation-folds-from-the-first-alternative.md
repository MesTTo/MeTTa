# Reinterpretation folds from the first alternative
Goal: `TaggedAnswer.under()` answers what direct evaluation answers for every shipped carrier that reads stored tags.
Constraint: a preset's symbolic zero is carrier vocabulary and stays; `examples/ch20-extending-the-engine/20-04-modules-and-the-catalog/09-carrier_vocabulary.metta` asserts `infinity` as budget's zero.

## 2026-09-14
Tried: an external consumer's probe (CalDar, `ai-notes/ai-pymetta-abilities.md`, pinned at `b80d21b40`), rerun here: `METTA_PATH=$PWD PYTHONPATH=$PWD/extensions/python` with the janus venv's python over the consumer's own `probe_budget.py` (its scratch, outside this repository) -> direct `under=budget` 0, `.under(budget)` raises `algebra_operation_error(budget, min): (Error (min infinity 0) "min expects two numbers")`; the same for tropical. The consumer's control, an identical algebra declared with `zero=G(float("inf"))`, reinterprets to 0.
Found: `_interpret_alternatives` seeded its fold with `declaration.zero` and passed it to `combine_values`; `_fuse`, the direct path's fusion of equal conclusions, combines from the first tag and never touches the zero. `testing/_properties.py` already names the gap: a symbolic identity is outside its operation's domain, which the engine's algebra scope short-circuits and the law sweep does not.
Rejected: a numeric zero in the two presets, because `infinity` is deliberate vocabulary the carrier example asserts. Rejected: a zero short-circuit inside `combine_values`, because it would make the Python operation differ from the engine's operation and hide the domain gap the sweep documents. Revisit if a carrier is declared without `combine-zero-identity`, where fold-from-first and fold-from-zero can differ; every shipped preset declares it, and the direct path already folds from the first tag.
Decided: fold from the first alternative, zero only for an empty alternative set, so both paths have one fold shape.
Tried after: `test_reinterpretation_under_a_raw_tag_carrier_answers_what_direct_evaluation_answers`, parametrised over tropical, budget, prob and ranked with four derivations costing 5, 7, 6, 8 under `+` and weighing 3, 5, 6, 10 under `*`: reinterpreted equals direct equals 5, 5, 24, 10. bool, bag, set and counting are outside the property by design: `_carrier_input` maps every source occurrence to their `one`, so they count derivations rather than read stored tags.
Not a defect, recorded for the consumer: `under=counting` answering one aggregate row with value `()` is the documented contract (root `llms.txt`, the website reference), and `_spaces/query.py` refuses a streaming `under=counting` by name for that reason; the seat `llms.txt` now says so beside `.under()`, since a reader of `metta.llms()` had no way to know.
Open: `counting` and `bag` are byte-identical presets distinguished only by name dispatch in `_spaces/query.py:444,688` and `_spaces/evaluate.py:686`; the aggregate is an operation over an answer set, not an algebra, and belongs on a door of its own. After the classes package lands, because it rewrites `_spaces/evaluate.py`.
Open: accepting an external law certificate for an infinite carrier. The consumer's lexicographic grade laws are provable in Lean; the sweep certifies by exhaustion over a finite carrier only, so that algebra runs unfused.
