# `metta.pytest_plugin`

Source: `extensions/python/metta/pytest_plugin.py`.

<!-- Generated by extensions/python/tools/reference.py from the named source and its imports; lane=reference. -->

> The shipped pytest fixtures every project testing against MeTTa
> otherwise rewrites: an engine and a scratch space, registered as a pytest11
> entry point so a user's test file starts at the assert.

The entries below reproduce the source signatures and docstrings.

## `metta`

```python
def metta() -> Space:
```

> The process home space, one per session, because there is one per process.

## `scratch_space`

```python
def scratch_space(metta: Space):
```

> A fresh anonymous space per test, dropped afterwards: stored
> state is isolated; registrations stay process-wide, exactly as
> the anonymous space factory documents.
