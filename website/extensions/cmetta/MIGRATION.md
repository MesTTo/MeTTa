---
navigation: false
---

<!--
Purpose: publish the C ABI migration guide the CMeTTa README reaches from prose.
Assumes: VitePress resolves an @include path against this file's directory, so the
  target is the extension's own shipped document and there is one copy of it.
Guarantees: the README's `[MIGRATION.md](MIGRATION.md)` resolves in both places,
  beside the file in a checkout and at this page on the site
  [tested: test_every_site_include_resolves].
Decides: unlisted, because a migration guide is reached from the page that needs
  it rather than from the navigation.
-->

<!--@include: ../../../extensions/cmetta/MIGRATION.md-->
