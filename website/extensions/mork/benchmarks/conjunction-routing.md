---
navigation: false
---

<!--
Purpose: publish the conjunction-routing measurement the MORK README reaches from prose.
Assumes: VitePress resolves an @include path against this file's directory, so the
  target is the extension's own committed measurement and there is one copy of it.
Guarantees: the README's `benchmarks/conjunction-routing.md` resolves in both places,
  beside the file in a checkout and at this page on the site
  [tested: test_every_site_include_resolves].
Decides: unlisted, because a measurement is reached from the page that cites it.
-->

<!--@include: ../../../../extensions/mork/benchmarks/conjunction-routing.md-->
