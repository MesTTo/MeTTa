; Purpose: retain handwritten equations beside a generated native face.
; Guarantees: the generator preserves this equation byte for byte
; [tested: tests/checks/check_prologface_selftest.py; commit=WORKTREE].

(: face-square (-> Number Number))
(@doc face-square (@desc "Square a number through a handwritten equation"))
(= (face-square $x) (* $x $x))

; begin generated Prolog face
; GENERATED by extensions/python/tools/prologface.py --write from lib/*/*.pl, tests/data/prologface/*.pl, extensions/python/tools/prologface_source.pl: the prolog-face artifact in extensions/python/tools/artifacts.py, whose prolog-face lane refuses drift.
!(import! &self (library lib_import))
!(import_prolog_functions_from_file (library "../tests/data/prologface/lib_face_fixture.pl")
  (face-add face-choice face-count face-zero))

(: face-add (-> Number Number Number))
(@doc face-add
  (@desc "Add Left and Right; unbound arithmetic operands raise.")
  (@params ((@param "Left") (@param "Right")))
  (@return "Sum"))

(: face-choice (-> Expression %Undefined%))
(@doc face-choice
  (@desc "Enumerate Items in order, retaining duplicate answers; an empty list fails.")
  (@params ((@param "Items")))
  (@return "Item"))

(: face-count (-> Expression Number))
(: face-count (-> Expression Number Number))
(@doc face-count
  (@desc "Count Items, optionally adding Extra.")
  (@params ((@param "Items") (@param "Extra")))
  (@return "Count"))

(: face-zero (-> Number))
(@doc face-zero
  (@desc "Return zero without any input argument.")
  (@params ())
  (@return "Zero"))
; end generated Prolog face
